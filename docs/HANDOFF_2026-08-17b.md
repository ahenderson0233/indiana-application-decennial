# HANDOFF — 2026-08-17 (session B): G29 distance, and G30 closed as not-our-defect

> **Read `docs/SESSION_START.md` first, run `python scripts/checkpoint.py`, then this file.**
> Supersedes nothing — `HANDOFF_2026-08-17.md` (session A) still holds for the front-end revamp.

---

## ⚠ 1. AGENTS STILL RUNNING AT HANDOFF

| agent / process | state | what it owns |
|---|---|---|
| **"Backfill 18 remaining watersheds"** (`scripts/pull_nhd_geometry.py --append-missing`, PID seen live) | **RUNNING.** ~2,440 of 3,848 flowline keys requested, **100% publisher return rate.** Then ~15 min of waterbodies | `in_nhd_flowline_geom`, `in_nhd_waterbody_geom` — closing the 59-of-76 HUC8 coverage gap. **DATA-COLLECTION-ONLY brief:** no downstream tables, no exports, no front-end, no checkpoint, no git |
| **"Acquire PJM injection + MISO withdrawal"** | AEP footprint harvest may still be in flight | ⛔ **Never run two QueueScope instances at once** — this is why **G23 is blocked**, not forgotten |

⚠ Several `ingest.load_*` processes (CAISO LMP, GA probate, EDGAR ABS, IL PTAB) belong to the
**platform session**, not to us. Do not touch them; `energy` is read-only.

**Operator ruling this session:** *"we will deal with the water agent's outcomes as they come in"* —
so do not block on it, and do not wire a half-loaded table. Its oversized-file **push** failures are
the operator's to handle and are a **separate** email source from Pages deploys (see G30).

---

## ⭐ 2. THE CORRECTION THAT MATTERS MOST — G30 WAS NOT OUR DEFECT

The backlog recorded *"Pages deploy fails on size"* and proposed moving `data/sites/` to
PMTiles/CDN/git-lfs — **large, risky work that its own note said would break the map.**

**I read the actual failure instead of inheriting the diagnosis.** Run #128 (`9361a16`):

```
build   SUCCEEDED (39s) — artifact "github-pages", 363 MB, uploaded fine
deploy  FAILED — HTTP 503
  "No server is currently available to service your request."
  "is githubstatus.com reporting a Pages outage? Please re-run the deployment at a later time."
```

**A transient GitHub-side outage.** Exactly **4 failures ever — #125, #126, #127, #128 — matching the
operator's 4 emails — and #129 succeeded with no change from us.** The 363 MB artifact uploaded
without complaint, no file exceeds 100 MB (largest 15.6 MB), and the site is under the 1 GB limit.
**Size was never the blocker.** Verified after: the deployed site loads and serves.

> **The lesson, and it is the standing rule working:** a plausible diagnosis with real numbers
> attached to it (`size-pack 2.91 GiB`, `data/sites/ 334 MB`) survived a whole session because
> nobody opened the log. **Read the error.**

**G30b** is the honest residue: the 2.91 GiB pack is real, is hygiene rather than a failure, and
⚠ **shrinking the files does not shrink a pack** — a new copy *adds* to it; only history rewriting
shrinks it.

---

## 3. WHAT SHIPPED

| # | item | evidence |
|---|---|---|
| **G29** | **Exact parcel→asset distance** — `in_asset_distance_parcel`, 532,868 rows, fan-out 1.000 | **41,986 parcels have a transmission line physically on them.** ST_INTERSECTS agrees **400/400**; the point method returns median 0.088 / max 0.772 mi where truth is 0.0 |
| **G29 step 3** | **The filters were hiding ~2.5× the qualifying parcels** | "within 1 mile of transmission" showed **103,159 of the 269,961 that qualify — 38%** |
| **G12d** | **Water surfaced at parcel grain** with its own "so what" | 79,168 parcels with water on them; Great-Lakes compact flagged separately |
| — | **`rc()` coordinate rounding never ran** | called as `rc(dict)`, handled only float/list → `return x`. Fixed: **334 MB → 219 MB** while *adding* 11 properties |
| — | **`enrichDistances` could lose every distance silently** | threw `state.poiList is not iterable` inside a `.then()`; the rejection was swallowed. Guarded |
| — | **Boot assertion added to the audit suite** (operator ask) | static check for MapLibre constant-only paint properties + `NO_BOOT_MARKER` guarding `document.body.dataset.ready` |
| **G32** | **Map layers belong inside the themed dropdowns** — opened | operator, this session |

---

## 3b. THE OPERATOR'S REFERENCE FILES — yardstick only, NEVER a source

**Not in the repo.** They live on the operator's machine at
`C:\Users\ahend\Downloads\Decennial Summer Work\Bus Analysis\Data Input Files\`:

| file | rows × cols | what it gives us |
|---|---|---|
| `Greenfield Interconnection Capacity, Buses-2026-06-23T16-58-00.csv` | **19,846 × 51** | ⭐ **THE BASELINE SCHEMA.** All 19,846 rows are Indiana, over **2,023 distinct buses**. Grain = **(bus × direction × upgrade tier)**: Injection 9,907 / Withdrawal 9,939, tiers 0–4 at ~3,950 each |
| `Greenfield Interconnection Cost, Buses-2026-06-23T16-57-46.csv` | **331,383 × 49** | per-upgrade cost, `Total Upgrade Cost ($MM)`, `Individual Upgrade Cost ($MM)`, **`Min/Max Lead Time (Years)`**, `Achievable Rating After Upgrade (MVA)` |
| `Substations-2026-06-23T14-49-04.csv` · `Transmission Lines-2026-06-23T14-50-32.csv` · `Buses-2026-06-23T14-22-49.csv` | | asset completeness benchmark (G20) |

⛔ **Operator ruling, restated 2026-08-17:** *"we do NOT want to copy them, but would like to use them
as a reference point to derive the same numbers as they have."* So we replicate the **method, column
set and grain** and compute every value from **our own** sources. `scripts/benchmark_vs_orennia.py`
writes a markdown report only — no BigQuery table, no `data/` payload — by design.

**What profiling their Indiana slice establishes** (measured, not read):

| their field | value | why it matters to us |
|---|---|---|
| `Existing Overload Flag` | **true on 8,232 of 19,846 (41.5%)** | ⭐ **They do NOT drop pre-existing overloads — they FLAG them.** This settles the G26 question: our choice is not "exclude or not", it is "flag and still report a capacity" |
| `Number of Upgrades Assumed` | 0,1,2,3,4 at ~3,950 each | **the dimension we do not have at all** |
| `Powerflow Case` | MISO `DPP-2025-Cycle_SUM_D_ERIS-mitigated_Final` 17,006 · PJM RTEP 2028 2,754 · SERC 86 | one file spans **three ISOs**, each with its own case |
| `Bus Interconnection Capacity (MW)` | median **43**, max 5,000, **11,729 of 19,846 > 0** | their tier-0 median is 0, but pooled across tiers the median is 43 MW |
| `Risk Level` | Low 8,232 · High 7,909 · Medium 3,705 | Low Risk == exactly the 8,232 existing-overload rows |

## 4. TWO THINGS THAT WILL MISLEAD YOU

1. ⚠ **The map does not boot in the in-app browser, and that is NOT a regression.**
   `map.getStyle()` returns undefined and `document.body.dataset.ready` is absent — **on the deployed
   site running the OLD code too.** Proven by loading production and checking the same two values.
   `SESSION_START` already records a previous session shipping a "fix" for this on a wrong diagnosis
   and reverting it. **Do not chase it again.** Verify app logic by calling functions directly in the
   console, which is how the `poiList` bug above was found.

2. ⚠ **`in_screener_candidates` distances were ALWAYS exact.** The screener used
   `ST_DISTANCE` from the start; only the **map console** approximated. So "the app measured distance
   wrong" is half true and the half matters: **transmission line distance did not exist anywhere in
   the estate** before this session, and a LineString is the one asset that can cross a polygon.

---

## 5. START HERE NEXT SESSION

1. `python scripts/checkpoint.py`.
2. **Check the watershed agent** (§1). If it landed, re-run `scripts/build_water_parcel.py` — it is
   idempotent — then re-export, so the ~18 previously uncovered watersheds get true distances.
3. Read `docs/BACKLOG.md`: governing principle → G-index (G1–G32).
4. Highest value open: **G21** (~14 surfaces still without a "so what" — `.sowhat` now exists in
   `style.css` as the reusable treatment), **G32** (layers into themed dropdowns, ⚠ move ids, never
   rename), **G17** (tariff model), **G29 buses** (the last unmeasured asset on the map console).

⚠ **Verify, do not inherit.** Every figure here was true when written. The checkpoint is the only
thing true now — and G30 is this session's proof of what inheriting costs.
