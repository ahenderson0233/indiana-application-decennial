# HANDOFF — 2026-08-18, evening (the tariff + dossier + bus session)

> Read `docs/SESSION_START.md` first, run `python scripts/checkpoint.py`, then this file, then the
> **⚠ IN FLIGHT** row at the top of `docs/BACKLOG.md`.
>
> This supersedes `HANDOFF_2026-08-18.md` (the morning session) for anything they disagree on.

---

## ⚠ 1. RUNNING RIGHT NOW — READ BEFORE TOUCHING ANY PJM TABLE

**A PJM MW-ladder harvest is running.** It survived one shutdown already and was resumed.

```bash
powershell -ExecutionPolicy Bypass -File scripts\run_pjm_ladder.ps1
```

⭐ **That is also the resume command, and it is the ONLY thing you need to do.** Run it again after
any interruption — a shutdown, a crash, a closed laptop. It is safe to run even if one is already
going: it polls for the **absence** of a QueueScope process rather than waiting on a handle, which
is the exact failure that spawned a duplicate harvester on 2026-08-18.

**Progress at last check (18:25 local):** 5,000 MW WITHDRAWAL, **47 of ~74 batches, 296,759 rows,
1,175 of 1,826 buses**. Five harvests remain queued behind it:

| rung | direction | target table | state |
|---|---|---|---|
| 5,000 MW | WITHDRAWAL | `in_pjm_qs_c23_wd_5000` | **running** |
| 5,000 MW | INJECTION | `in_pjm_qs_c23_inj_5000` | queued |
| 1,000 MW | both | `in_pjm_qs_c23_{wd,inj}_1000` | queued |
| 500 MW | both | `in_pjm_qs_c23_{wd,inj}_500` | queued |

**Resumability was verified, not assumed:** the MW is *in* the marker filename
(`23__WITHDRAWAL__5000__1568__<batch>.done`), so rungs cannot read each other's marks and skip
work. Only completed batches are marked, so an interrupted batch retries. Proven in practice — the
resume picked up at batch 47 rather than restarting.

⛔ **Never start a second QueueScope process.** ⛔ **Never delete `data/`** — archive markers,
never delete. ⛔ **Owner 1568, not 739**: 739 is AEP in the *default* case and loads 0 rows while
exiting successfully.

⚠ **The unregistered table the honesty audit reports IS this harvest.** It registers on
completion. That plus the wiring census are the two standing checkpoint failures; anything else is
real.

### ⭐ What to look for when the rungs land — the single most important thing in this file

**NEW FACILITIES, not new numbers.** A 1-batch probe at 5,000 MW matched the 100 MW harvest on
**4,673 of 4,673 constraint keys, max delta 0.0** — `available_mw` does not vary with the requested
MW. The operator's reasoning for the ladder is the thing to test: a larger request may cause
QueueScope to **report facilities** a 100 MW request never binds. We monitor **89.5 facilities per
bus**; the vendor reports **9.4**; and **146 of their 298 binding facilities are absent from our
harvest entirely**. If the ladder closes that, PJM parity follows. If it does not, the gap is the
monitored-facility scope itself and needs a different attack.

**The comparison to re-run when they land** is in §4 below.

## 2. WHAT CHANGED TODAY, IN ONE PARAGRAPH EACH

### Tariffs — right for the five IOUs, wired everywhere, 22 utilities costed

**No rider was ever counted**, on 17 of 18 IOU schedules: a rider's `applies_to` was being used as
its service-class key, so the renderer dropped every one. It survived a full session of auditing
because `n_riders_attached` reported them correctly attached — *a count is not a total*. Seventeen
further defects followed (see **G54**), including a thousands-separator comma read as a class list
(Duke HLF **+92% → −4%**) and NIPSCO's **$0.62/kW-DAY maintenance service billed 365 days a year**
(**+$106.22M/yr** on one schedule). `scripts/audit_tariff_costing.py` is now a standing check and
is **down to one finding across all 73 utilities** (G57).

### Per-utility isolation — proven, not asserted

`scripts/tariff_fingerprint.py` hashes every field that can change a bill and names which
publishers moved. **Perturbing only Duke's adapter moved exactly 1 of 73 utilities**; the shared
block-detection change moved 9 municipals and **0 IOUs**. Run `export_tariffs.py` first or you
fingerprint a stale build.

### The dossier — 10 findings, 9 closed, and it now carries a real rate

See `docs/DOSSIER_AUDIT_2026-08-18.md`. It had **no tariff input at all** while printing "no
component-level Indiana tariff is held yet". It now prices the parcel's own utility — Vanderburgh →
SIGECO **8.03¢**, Marion → AES **10.51¢**. ⭐ **D-11, the operator's mid-session catch, was the most
consequential**: resolve the utility from the parcel **footprint**, never a point. On 2 of 4
counties sampled the point resolved *nothing* while the footprint resolved the utility.

### Buses — both directions, both ISOs, for the first time

`in_bus_capacity_tier0`: **2,117 rows → 7,102 / 3,557 buses**. MISO from the licensed Orennia
DPP-2025 proxy, PJM from our own case-23 harvest. ⭐ **MISO load-side headroom now exists** — 1,731
buses, 1,079 non-zero — a question the app could not answer anywhere this morning, across the two
thirds of Indiana inside MISO.

---

## 3. ⛔ THREE THINGS I GOT WRONG TODAY, CORRECTED

Recorded because each one shaped a decision:

1. **"D-2 is blocked on the G26 headroom method."** It is not. The method is settled and
   implemented — pre-existing overloads are flagged and reported, not dropped. Rebuilding tier0 was
   a re-run, not a research question.
2. **"gridsiting is built from `in_bus_capacity_tier0`."** It never read tier0 at all; it queried
   the old tables directly. The stale-case conclusion was right, the mechanism was wrong. **And
   there was a THIRD path** — `grid.geojson.gz`, which feeds the map — that still had DPP-2021
   after both others were fixed. The operator caught that one.
3. **"The county files drop the SI detail fields."** They do not. I had inspected an unflagged
   parcel; the writer omits nulls.

---

## 4. WHERE THE BUS PARITY WORK ACTUALLY STANDS

⭐ **Capacity: the derivation is solved and centred.** Their figure is
`(rating − base flow) / |shift factor|` on the binding facility — verified against their own
published row for bus 243209 (2844 MVA, 2550 MW, −0.0745 → **3,946 vs their 3,943.59**). We do not
ship the rating but can recover it: `rating = impact_mw / ((post − pre)/100)`.

| on the 283 shared buses | exact | within 20% | median ratio |
|---|---|---|---|
| `MIN(available_mw)` — what we shipped this morning | 0 | 0 | **0.136** |
| the derived formula — shipped now | 1 | 84 | **1.010** |

⚠ **Binding-facility selection is the open half**, and it is a coverage problem, not a ranking one:

- their binder is **present and passes our filters** on 134 buses → genuine ranking work
- their binder is **absent from our harvest entirely** on **146 of 298**
- excluded by our `|dfax| ≥ 0.05` filter on 18

⭐ **Placement: the method is proven, the gazetteer is the ceiling.** Bus label → substation name
(stripping the AEP area prefix, `05EUGENE` → `EUGENE`) reproduces the vendor's coordinates at a
**median 0.03 mi**. Topology hop-1 from the branch graph adds buses at ~5.3 mi; hop-3 is junk at
33 mi and is not used. Coverage went 82 → **111 of the 283 Indiana buses**. The ceiling is that
`in_substations` **does not contain** many AEP station names — MODOC, FOWLER, STUDEBAKER, BOUNDARY
and ADAMS are all absent, while DELAWARE and SOUTH BEND are present and match cleanly. **Extending
the gazetteer is the unlock, not a better matcher.**

⚠ Note the denominator: our harvest is owner=AEP, whose footprint spans Ohio, WV, VA, KY and
Michigan. `in_substations` is Indiana-only. **283, not 1,826, is the number that matters.**

---

## 5. ⚠ THE ONE PLACE WE DEPEND ON A PRIVATE SOURCE

Operator: *"we want to use Orennia as a yardstick, and do not want to use their data verbatim,
since we ideally don't want to ever rely on a private source for our data."*

**MISO headroom is the exception, and it is deliberate.** MISO publishes no load-side figure at all
and four independent sweeps found no public route, so without the licensed proxy the tool simply
cannot answer the load question across most of Indiana. Every such row carries
`provenance_class = 'vendor_licensed_proxy'`, the dossier says so in words, and **the licence lapses
late 2027** — after which those rows return to "not published".

**Nothing in PJM is theirs.** The capacity is our harvest through our own derivation; the placement
is our own name match; their file is only the scoring set.

---

## 6. WHERE TO PICK UP

1. **When the ladder lands** — check for NEW FACILITIES, not new numbers, and re-run the
   binding-facility comparison. That is the parity gate.
2. **Extend the substation gazetteer** with the missing AEP station names. This is the single
   highest-leverage placement fix: the matcher is already exact where the data exists.
3. **G57** — two I&M rows carry `rate = 0.0` with `value_status = 'published'`. Read the source
   sheets; do not guess.
4. **G55** — 50 utilities still have only a URDB floor, and the 15 costable municipals beyond the
   six with adapters have never been audited.
5. **Retire the duplicate PJM pair** (`in_pjm_qs_tc2phii_*` vs `_c23sens_*`, ~1.1M identical rows)
   and add the powerflow filename to the harvest schema so the next refresh is visible without
   diffing values.

## 7. THE ONE-LINE VERSION

Tariffs are **right for the five IOUs and wired into the dossier**; buses now carry **both
directions for both ISOs** with MISO licensed and PJM ours; the PJM capacity derivation is
**centred on Orennia (ratio 1.010)** but binding-facility selection is open, and the ladder running
tonight is the experiment that decides whether it closes.

---

## 8. ⭐ STATUS BOARD — what is DONE, what is IN FLIGHT, what is NOT STARTED

Read `docs/FEATURE_INVENTORY.md` alongside this: it lists **every feature in the tool, what it
does, how it works, and which BigQuery table it comes from**. That is the fastest way to get
oriented without reading the code.

### ✅ Complete and deploy-verified

| item | what landed |
|---|---|
| **G54** tariff costing | 17 defects found and fixed; **right for the five IOUs**; audit down to 1 finding across all 73 utilities |
| **G56** per-utility adapters | one declarative adapter per publisher, and `tariff_fingerprint.py` **proves** a change to one cannot move another |
| **G58** silent cap | proxy card showed 14 of 432 rows sorted cheapest-first; now all 35 utilities, exclusions counted on the page |
| **G60** dossier audit | 10 findings, 9 closed; the dossier now prices the parcel's own utility |
| **G39** screener | per-site depth, deep link to the map, dossier link, and the sites as a map layer |
| **G63** bus rebuild | tier0 2,117 → 7,102 rows, both directions both ISOs; three separate surface paths repointed |
| **G64** IOCS self-heal | the exporter that silently emptied the Community court table now repairs itself |
| **§13(8)** | recorded as impossible for months; a component-level Indiana tariff now exists and acceptance says so |

### 🟡 In flight

| item | state | method |
|---|---|---|
| **PJM MW ladder** | running, resumable | see §1 |
| **G61** capacity parity | derivation **solved** (ratio 0.136 → 1.010); binding-facility selection open | recover the rating from the loading delta, then `(rating − base flow) / |shift factor|` |
| **G62** placement | method **proven** at 0.03 mi median; coverage 111 of 283 | bus label → substation name, AEP prefix stripped; topology hop-1 as a weaker tier |
| **G55** municipals | 22 utilities costed, 50 on a URDB floor | per-utility adapter, then a reconciliation check that does not need EIA-861 |

### 🔴 Not started

**G43** border clipping · **G48** existing-DC-reads-green · **G50** MISO bus data on the dossier's
utility row · **G51** caller sweep · **G52** map legend · **G53** withdrawn queue as a signal ·
**G57** the two I&M `0.0` rows · **G59** retire the duplicate PJM pair (~1.1M identical rows)

---

## 9. METHODOLOGIES — how the recurring problems get solved here

These are the moves that actually worked today, stated so they can be reused:

1. **Probe before committing hours.** A 1-batch probe killed a multi-hour harvest that would have
   duplicated existing data. It is also how the case-23 re-harvest was justified — and, later, how
   it was shown to have produced byte-identical rows.
2. **Measure the blast radius before changing shared logic.** "Adding these two words to `MAX_RE`
   changes exactly 1 of 31 eligibility rows" is what made that change safe to ship.
3. **Use the vendor as a scoring set, never as a source.** Their file has a labelled answer for
   every bus — capacity, binding facility, coordinates — so our own derivation can be scored
   against it without ever shipping their numbers. The one exception is MISO headroom (§5).
4. **A count is not a total.** `n_riders_attached` reported 8–13 riders correctly attached while
   every one contributed $0. Check the money, not the tally.
5. **Render the document, do not read the code.** A parse check passed `esc is not defined`, which
   would have made the Dossier button do nothing. Rendering it caught it in one call.
6. **An alarming number is a claim about the instrument first.** "0 of 298 buses match" was an
   int-vs-string join; "1,419 of 1,826 agree" was mutual zeros; "max delta 1,841 MW" was join
   fan-out. All three would have been published as findings.
7. **Turn each fixed defect into a standing check.** That is what `audit_tariff_costing.py` and
   `tariff_fingerprint.py` are — the signatures of bugs already found.
