# BACKLOG — the running task ledger

<!-- CHECKPOINT:BEGIN -- generated by scripts/checkpoint.py, do not hand-edit -->
> ## STATE — generated 2026-08-19 18:12 UTC by `python scripts/checkpoint.py`
>
> **Every figure here is a live query. Nothing in this block is hand-typed — if you are about to
> edit it, edit the generator instead.** The previous version of this block WAS hand-typed, inside
> a document whose own header says not to quote a count from it.
>
> | | |
> |---|---|
> | registered objects | **309** |
> | reaching a surface | **291 of 309** |
> | honesty audit | **13 checks, 1 failures** |
> | front-end audit | **0 findings, 0 severe** |
> | §13 acceptance | **3 PASS, 3 PARTIAL, 1 FAIL, 2 N/A** |
> | SI signals in coverage | **25**, of which **19** admit at least one parcel |
> | parcels flagged | **24,277** (payload and warehouse agree) |
>
> **Checkpoint result: 3 FAILING — do not build on this state**
<!-- CHECKPOINT:END -->

> ## ✅ THE §2a BACKLOG IS COMPLETE
>
> **Figures live in the generated block above — do not restate them here.** This section had its
> own hand-typed copy of the same numbers and it went stale within a day: it still read
> "255 of 255" and "23 signals wired" while the warehouse held 268 and 24. That is the whole
> reason the block above is generated.
>
> Every item the last session listed as open is closed. What follows is real, and short:
>
> **Waiting on a clock, not on work** — F3 (Howard, City of Elkhart: council votes **2026-08-17**)
> and F4 (Marion: MDC final action on Proposal No. 238, **2026-08-19**). Neither is doable early.
>
> **Both agents finished and BOTH ARE LOADED.**
> - **55-county codified sweep** → `in_ordinances_county_codified` (55) + `_provisions` (16) +
>   `_walls` (8). **92 of 92 counties now attempted.** Three name a data centre — Hancock
>   (Ordinance 2026-6E, Data Center Overlay District), Martin (**no zoning code at all**; home-rule
>   permitting + 12-month moratorium), Rush (UDO names *Data Center Tier 1/2* with **every district
>   cell blank** = permitted nowhere). All 35 negatives passed a control check; 17 counties ran no
>   search at all (BLOCKED/NOT_REACHABLE) and are **not** silent.
> - **IDEM dates** → `in_si_d22_idem_dated`, 22,565 rows, **3,925 exact printed dates**, 97.9%
>   agreement between the two routes. `load_idem_dates.py` is **idempotent** — if the agent is
>   ever resumed and extracts more, just re-run it.
>
> **Cannot close as specified, and are not defects:** §13(5) needs an AI docket summary and this
> app has no LLM feature; §13(8) NEEDED a component-level Indiana tariff that existed nowhere in the
> estate. Record them as not-achievable-as-specified rather than converting them to passes.
>
> **Front-end pass is deferred to the next session by the operator.**

**Maintained continuously. Nothing leaves this file except by being DONE or by being ruled
out with a reason.** Opened 2026-08-16.

---

## ⭐ THE GOVERNING PRINCIPLE — read this before any other item in this file

> **Operator, 2026-08-17:** *"The entire point of this application is really answering the 'so
> what' question, so everything should be insightful and/or actionable that we display anywhere to
> the user."*

**This is not a backlog item. It is the product thesis, and it outranks every task below.**

The application's job is not to *hold* Indiana's siting data — the warehouse already does that. Its
job is to tell a data-centre or BESS developer **what to do next, and why**. A number that is
correct, sourced, freshly built and beautifully rendered is still a **failure** if the reader cannot
say what it changes about their decision.

**Apply it as a veto, not as a polish step:**

| ask of every single thing on screen | if the answer is no |
|---|---|
| Can a developer say what this changes about their decision? | it does not ship |
| Is it in their units, not ours? | translate it (minutes → hours of backup; MMcf/d → MW supportable; acres → MW at a stated density) |
| Would they know which end is good? | order or band it |
| Does volume stand in for value? | collapse it — 1,017 gas rows said about 40 distinct things |
| Is it context rather than a finding? | say so explicitly, and keep it small |

**Consequence for the roadmap:** G21's inventory is the checklist for *existing* surfaces, but the
principle binds **new** work too. Nothing gets added to any page — a map layer, a table, a chart, a
screener column, a dossier row — without its "so what" written at the same time. A surface that
cannot earn one is not a surface we build.

⛔ The counter-example to avoid is already in the repo's history: **four ordinances rendered while
107 verified county actions existed.** That was correct data, correctly sourced, and useless —
because nobody asked what a reader was supposed to conclude from it.

---

## 🔴 OPEN WORKSTREAM — FRONT-END REVAMP, opened 2026-08-17 by the operator

**The §2a backlog above is closed. This is a NEW and LARGER body of work, and none of it is done.**
Operator brief, verbatim in substance: *"user flexibility and functionality as the highest
priority, understanding that the user may NOT be fully knowledgeable of this tool's maximum
function or how everything ties together, so everything should be simply explained, and the user
should be able to easily pick up what each function does"* — plus *"every table/feature should have
a feature, but that doesn't mean we need irrelevant tables scattered throughout; everything should
be actionable and insightful to the user, and in a simple, easy to digest manner."*

### ⛔ STANDING RULE G25 — CHECK THE WAREHOUSE BEFORE YOU EXPLORE OR SCRAPE ANYTHING

Operator, 2026-08-17: *"always look at what we have in BQ (either in indiana_app or energy) before
completing any exploration or scraping tasks — this is a rule that should be followed through this
project's duration."*

**Binding for the life of the project.** Before proposing an acquisition, a loader, a trial, or an
agent: enumerate `energy.__TABLES__` and `indiana_app._registry` for the subject. It costs one query.

**It has already paid twice in one session:**
- I recommended building a CartoVista loader as the top grid priority. **We already hold the
  CartoVista MISO data** — `cartovista_miso_poi_locations` (8,219), `miso_poi_capacity_surface_geotiff`
  (3,390,912), `miso_poi_monitored_facilities` (904,486), `miso_poi_headroom` (12,845),
  `miso_poi_attributes` (12,845), `miso_poi_location_crosswalk` (8,219).
- F1/F2 were queued as open work that had already been done.

The failure mode is always the same shape: **a plan built on what we remember instead of what we
hold.**

### G29 — 🔴 DISTANCE TO GRID ASSETS IS MEASURED WRONG ON THE MAP (operator, 2026-08-17)

*"A parcel that has transmission running through it should say 0.0 mi from transmission, and it is
currently stating ~0.55 mi... it should be measuring the closest point of transmission to the
closest point of the parcel."*

**Diagnosed, and the operator is right. Root cause is in `app.js`:**

```js
function repPt(geom) {           // "any coordinate ON the feature (a vertex)"
  let c = geom.coordinates;
  while (typeof c[0] !== "number") c = c[0];
  return c;                      // <- THE FIRST VERTEX. Not the nearest point.
}
```

`enrichDistances()` measures **first-vertex-of-parcel → a representative vertex of the asset**.
Both ends are wrong for the question being asked:

| end | what it uses | what it should use |
|---|---|---|
| parcel | its **first vertex**, an arbitrary corner | the nearest point on the parcel — **0 if the asset crosses it** |
| line | one binned vertex per line | the nearest point **on the segment**, not on a vertex |

Error is therefore up to *(parcel diagonal) + (vertex spacing on the line)*, always **overstating**
distance, and it never returns 0 even when the asset physically crosses the parcel. It affects the
map's grid filters, the evidence panel, uploaded sites, and the Power Plan's substation/line rows.

⭐ **Second finding: we already have this right in one place, and wrong in another.**
`build_screener_candidates.py` uses `ST_DISTANCE(parcel_geog, bus_point)` in BigQuery — true
polygon-to-point geodesic distance, which correctly returns **0** when the point is inside the
parcel. So the **screener's distances are exact while the map console's are approximate**, on the
same parcels, from the same data. That is the *same defect class* as PJM and MISO headroom being
computed by different rules and shown side by side.

**Fix, in order:**
1. **Compute distances in the warehouse and ship them**, exactly as the screener already does —
   `ST_DISTANCE` against the real geometry for substations, lines, and connection points. One
   method, exact, and no client-side approximation to drift from it.
2. Where a client-side distance is unavoidable (an uploaded CSV with no parcel geometry), use
   **point-to-SEGMENT** distance, not point-to-vertex, and label it as a point measurement.
3. **Re-measure the fan-out of "within X miles" filters after the fix** — every distance shrinks,
   so counts will rise. That is the correction landing, not a regression.
4. ⚠ Do NOT apply the no-centroids rule as an objection here: measuring to the *nearest point on a
   parcel* is exact, not a derived centre.

#### ✅ G29a — TRANSMISSION AND SUBSTATIONS ARE DONE, AND THE OVERSTATEMENT IS NOW MEASURED

`in_asset_distance_parcel` — **532,868 parcels, fan-out 1.000**, exact `ST_DISTANCE` against real
geometry.

| | |
|---|---:|
| ⭐ **transmission line PHYSICALLY ON the parcel (0.0 mi)** | **41,986** |
| within 1 mile of a line | 269,961 |
| nearest line is **≥ 345 kV** | 139,376 |
| nearest line voltage **unknown** (flagged, never rendered as 0) | 54,411 |
| median line distance | **0.983 mi** |
| substation on the parcel | 479 |
| median substation distance | 2.695 mi |

**Two independent verifications, because a clean number is a claim about the instrument first:**
1. **`ST_INTERSECTS` cross-check: 400 of 400** sampled `line_on_parcel` rows genuinely have a
   conductor crossing them. The flag means what it says.
2. **The overstatement, measured on the same parcels:** measuring from a point instead of the
   polygon returns a **median 0.088 mi and a maximum 0.772 mi where the true answer is 0.0** —
   consistent with the operator's observed ~0.55 mi.

⭐ **Why transmission was the one that mattered.** `in_screener_candidates` already carried exact
`ST_DISTANCE` for buses (`inj_mi`, `wd_mi`) and substations (`sub_mi`) — the screener was always
right. But it had **no transmission-line distance at all**, and a line is the only one of the three
that is a *LineString*: a point can be near a parcel, a line can run straight through it. **The asset
with the largest first-vertex error was the asset nobody had measured.**

⛔ **`asset_class='unknown'` (739 substations) is KEPT, not dropped.** Taps (503) and dead ends (27)
are excluded because G19/G20 established they are line structures, not substations. But *unknown* is
a **missing label, not evidence the thing is not a substation** — dropping it would push a parcel's
nearest substation further away for a reason about our metadata. Carried as `sub_class_unknown`.

#### ⭐ G29 STEP 3 — THE FILTERS WERE HIDING ~2.5× THE QUALIFYING PARCELS

The backlog predicted *"every distance shrinks, so counts will rise — that is the correction landing,
not a regression."* Measured, the rise is much larger than that wording suggests:

| "within X miles" filter | approximate | **exact** | newly visible |
|---|---:|---:|---:|
| within 0.5 mi | 60,999 | **175,509** | **+114,510** |
| within 1 mi | 103,159 | **269,961** | **+166,802** |
| within 2 mi | 155,807 | **391,350** | +235,543 |
| within 5 mi | 205,432 | **508,800** | +303,368 |

**Mean overstatement is only 0.1375 mi — but it moves ~2.5× the parcels**, because the distance
distribution is dense exactly where the thresholds sit (median 0.983 mi). A small systematic bias at
a crowded threshold is not a small error in the answer.

⚠ **These are LOWER BOUNDS.** The comparison above measures the approximate side from a parcel point
against the **full line geometry**. The app's real method is worse on both ends — it uses the
parcel's *first vertex* and bins each line to *one representative vertex* — so the map was hiding
**more** than this table shows.

⭐ **Read as a product statement:** a developer filtering for "parcels within 1 mile of transmission"
was shown **103,159 of the 269,961 that qualify** — about 38% — with nothing on screen to say so.
That is the single largest correction this application has shipped.

**Still open under G29:** buses on the *map console* (the screener's are already exact), and the
client-side `repPt()` fallback for uploaded rows, which must be labelled as a point measurement.

### ✅ G33 — DONE. THE OWNER-SIGNALS PAGE WAS A BUILD LOG; IT NOW LEADS WITH THE ANSWER

> **Shipped 2026-08-17.** The five numbered requirements below were the spec and all five were met. Recorded here because the *finding* matters more than the layout: **the ranked-parcel evidence this page exists to show was already computed and sitting in `si_v2.json.gz` as `top_evidence`, rendered by nothing.** A surface can fail the governing principle not only by showing the wrong thing but by shipping the right thing and never drawing it. ⚠ One measurement trap worth keeping: an initial check for leaked codenames used `innerText`, which returns **nothing for content inside a closed `<details>`** — it reported the page clean while half of it was unscanned. `textContent` is the correct instrument, and it found the one remaining leak.

**The original spec, for the record:**

*"That entire page needs a complete overhaul, and the information there needs to be much more
streamlined (the tables are all over the place and there is seemingly no structure whatsoever — many
of the tables are irrelevant and don't provide material insights or actionable results)."*

**Measured, not asserted:** `si.html` carries **26 tables across 24 top-level `<h2>` sections, with
exactly 1 sub-heading and 0 accordions.** Everything is at the same level, so nothing is subordinate
to anything and the reader has no path through it. G5 gave the map console eight named accordions
for the same reason; this page never got that pass.

⭐ **But layout is the symptom. The disease is AUDIENCE.** Sorting the 24 sections by who they serve:

| serves a DC/BESS developer | serves US — provenance, QA and build history |
|---|---|
| Signal inventory by signal | "The flag was a vacancy flag — what changed" |
| Per-signal coverage (*what we can and cannot see*) | "Lane D — columns that were pulled and never wired" |
| D22 environmental compliance | "D5 — vacancy is two different things" |
| Highest-vacancy census tracts (context) | "Marion placement, checked by two instruments" |
| Owner-grain signals (*why some signals cannot reach a parcel*) | "Admitted by operator sign-off — what was let in" |
| | "Staged acquisitions — awaiting the subject test" |
| | Per-source D11 / D27 / D25 / land-bank snapshot dumps |

**At least seven sections exist to prove the corpus was built honestly.** That was exactly the right
artefact *while building it* — several of them record genuine, hard-won corrections. It is not what a
developer opens the page to find, and volume standing in for value is the specific failure the
governing principle names.

**The rebuild, and the test each surface must pass:**
1. **Lead with the question the page exists to answer:** *which owners might sell me land, and how
   strongly do we believe it?* Ranked parcels with a plain-language reason, not a signal taxonomy.
2. **Then: what we can and cannot see.** Coverage is genuinely actionable — a developer must know
   that 90% of our largest signal is undated (G14) and that some signals cannot reach a parcel at
   all. Keep it, framed as a limit on the answer rather than as a statistic.
3. **Collapse everything else into ONE `<details>` — "How this was built and checked."** Do not
   delete it: `SI_COVERAGE.md` asserting D4 absent while 17,617 rows sat in the warehouse is exactly
   what this material prevents. It simply is not a top-level surface. The **Data & sources** page is
   already marked internal-by-design and exempt from G21 — much of this belongs there.
4. **Apply G21's five requirements** to whatever survives, and the negative test: a table that cannot
   answer "so what?" comes out.
5. ⚠ **Preserve every element id that carries a filter or a payload key** — G5's rule, learned the
   hard way: a renamed id kills a control **silently**, and `audit_frontend.py` reads source, not
   runtime, so it will not catch it.

### G32 — MAP LAYERS BELONG INSIDE THE THEMED DROPDOWNS (operator, 2026-08-17)

*"For the map layers, those should be embedded [in] the 1-6 dropdowns in the dashboard, so everything
stays consistent with their function."*

**The problem is one subject described by two organising systems.** G5 renamed the rail into eight
plain-language accordions — Owner motivation, Power & grid, Land & size, Environmental constraints,
Community & local rules, Market & cost — and those own the **filters**. The map's **layer** toggles
are a separate flat list. So "power & grid" exists twice: a user sets a grid filter in one place and
switches on the substation layer in another, with nothing tying them together. That is precisely the
"user may NOT be fully knowledgeable of how everything ties together" failure the operator's opening
brief named.

**The work:** each layer moves into the theme that owns its subject, so one dropdown owns one subject
end to end — its filters, its layers, and its "so what" in one place.

| theme | layers that belong to it |
|---|---|
| **Power & grid** | substations, transmission lines (coloured by kV), connection points/buses, interconnection queue, utility territories, planned grid investment |
| **Environmental constraints** | flood, wetlands, protected land, nonattainment, watersheds, water-stress basins, rivers & lakes |
| **Land & size** | parcels, federal surplus, logistics/rail/road |
| **Community & local rules** | county actions, ordinances, sentiment shading, data centres |
| **Market & cost** | GHGRP emitters, gas assets, bonus geographies |

⚠ **Every one of ~65 element ids must survive the move.** G5 established that a renamed id kills a
filter **silently** rather than erroring — the audit reads source, not runtime, so it would not catch
it. Move the DOM nodes; do not rename them.

⭐ **Pairs with G21, and do not ship one without the other.** A layer that arrives in a themed section
still needs its why-it-matters line — the G21 inventory marks *every* map layer ❌ today, and
**"a legend is not a 'so what'"**.

### G28 — 🔴 EXTENSIVE ACREAGE AUDIT: the inflation is not where it looks (operator, 2026-08-17)

*"A full-scale extensive audit of our parcel acreage, since it seems inflated in many instances,
and I want us to be fully truthful in our representation of space (including outdoor space for
BESS)."*

**First pass measured, and it rules out the obvious suspect.** Across 532,868 candidates:

| | |
|---|---:|
| both recorded and measured acreage present | 532,865 |
| **disagree by >10%** | **131 (0.02%)** |
| disagree by >50% | 111 |
| recorded more than 2× measured | 82 |
| **median recorded vs measured** | **23.75 ac vs 23.75 ac** |
| max recorded vs measured | 6,374 ac vs 6,374 ac |
| `outdoor_acres` exceeding parcel acres (impossible) | **0** |

So the two measures **agree**, and the reconciliation logic (take the smaller, flag the dispute) is
working. **If acreage looks inflated, it is not because our two numbers disagree.** The real
candidates, in order of likely impact:

1. ⭐ **The MW figure, not the acreage.** 6,374 acres × 4 MW/acre = **25,496 MW** is arithmetically
   correct and absurd as a presentation — nobody builds a 25 GW campus. The screener and the Power
   Plan both print it. **Suspect this first: the acreage may be right while the number derived from
   it is meaningless.** Cap or band the displayed capacity, and say what the cap is.
2. **Parcel area is not buildable area.** We treat 100% of a parcel as usable for a data centre.
   Real buildable area is net of setbacks, easements and rights-of-way, internal water, steep
   slope, and existing structures we do not intend to demolish. A flat density on a gross parcel
   overstates every site, uniformly and invisibly.
3. **Aggregated or mis-shaped source geometry** — the 111 parcels disagreeing by >50% and the 82
   where recorded is more than double measured are a finite, auditable list. Work them individually.
4. **Outdoor space for BESS.** `outdoor_acres` is parcel minus building footprints and never
   exceeds the parcel, so it is not obviously wrong — but it is subject to exactly the same
   buildable-vs-gross problem, and a battery pad has its own setback and access requirements.

**Deliverable:** a documented buildable-area basis, applied consistently, with whatever we deduct
stated on the face of the number. ⛔ Until then, do not present a derived MW as if it were a site
capacity — it is an arithmetic upper bound on gross land, and should be labelled as one.

### G27 — 🔴 WE UNDER-CLIPPED 401 COLUMNS ACROSS 24 TABLES (audited 2026-08-17)

Operator: *"It is likely that we didn't scrape the full columns from any of these tables, so please
explore this."* **Confirmed, and it is systemic rather than isolated.**
`scripts/audit_clip_completeness.py` walks all 276 registered tables, resolves the 148 that clip an
`energy.*` parent, and compares column sets (normalised for case, punctuation **and aliases** —
the first run cried wolf on `latitude` vs `lat`, which is rule 9 reappearing in a new script).

**Result: 24 clips are missing at least one parent column; 401 columns total.** Full detail in
`docs/CLIP_COMPLETENESS.json`.

| clip | held / parent | what was lost that matters |
|---|---|---|
| `in_nonattainment` | 13 / 62 | **`classification`** — the actual attainment status, plus its citation, URL and effective date. We hold the areas without the finding |
| `in_gas_processing_plants` | 11 / 48 | `latitude`, `longitude`, `county`, `status`, `type` — **the gas assets cannot be plotted or filtered** |
| `in_gas_lng_terminals` / `in_gas_storage` / `in_gas_compressor_stations` | 11 / 44, 44, 41 | same shape: coordinates, county and status all dropped |
| **`in_bus_headroom_miso`** | **18 / 34** | **`percent_loading_before`**, `percent_loading_after`, `mw_available`, `derived_rating_mva`, `mw_impact` |
| `in_miso_poi_ladder` | 18 / 34 | the same fields, in the table the agent built today |
| `in_pjm_bus_locations_candidate` | 14 / 28 | `name`, `city`, `county`, `state`, `type`, `status` from the HIFLD parent |

⭐ **`percent_loading_before` is the important one, and it is the exact column G26 needs.** It is how
you tell *"this facility is already over its rating before your project"* from *"this facility is
genuinely full"*. Without it, the app cannot distinguish the two and renders both as a bare zero.
**The column we needed to explain our biggest data discrepancy is one we chose not to take.**

**Not all 401 are defects.** `in_si_plottability` reads 3 of 25 **by design** — it is a deliberate
one-row-per-geom_kind aggregate, not a row clip. Triage the list before re-clipping; the rule is to
justify each exclusion, not to take every column reflexively.

##### ⭐ G27 MEASURED DOWN, 2026-08-17 — the four "gaps" collapse to ONE real one

The audit above matches by name and **cried wolf on three of the four flagged tables.** Verified
each against its parent before touching anything:

| flagged table | audit said | MEASURED truth |
|---|---|---|
| `in_gas_processing_plants` | coordinates dropped | ❌ **FALSE — Indiana has ZERO gas processing plants** (parent 840 national, 0 IN). Nothing to clip; the empty clip is correct |
| `in_gas_compressor_stations` | coordinates dropped | ❌ **FALSE — already carries `geog` on all 24 Indiana rows.** Already plottable; the clip and parent are *different sources* (clip has `ngppid`, parent has `latitude`), matched only by name |
| `in_echo_cwa_facilities` | `faclong` but no `faclat` | ⚠ **TRUE but a PARENT defect** — `energy.echo_cwa_facilities` has **no latitude column of any name**, only `faclong`. Cannot be re-clipped. Plots at county grain via `facstdcountyname`. Tell the platform session |
| **`in_nonattainment`** | lost `classification` | ✅ **THE ONE REAL FIX.** Parent carries `classification` on 29 of 35 Indiana rows; clip took only `classification_pub_date`. Areas-without-finding (G21) |

**✅ DONE 2026-08-17 — and the recipe this section used to carry was WRONG. Read why.**

⭐ **ROOT CAUSE: a slice index, not a mapping error.** `build_census_wires.py` line 65 read
`keep = […][:12]`. `classification` is **column 16** of the parent, so the clip stopped four columns
short of the only field a reader needs. Nothing was mis-named and nothing failed — the schema was
**truncated at an arbitrary index**, and `app.js` had been rendering an empty `classification` row on
the map popup ever since. ⚠ **The same idiom is still live in `build_gas_facilities.py` `[:10]`,
`build_gas_market.py` `[:14]` and `export_full_wiring.py` `[:12]`** — that is the mechanism behind
the "401 under-clipped columns" headline, and those three are unaudited.

⛔ **The fix this section previously prescribed would have shipped a silently wrong table.** It said
to join a new `in_nonattainment_classified` to the parent's **Indiana slice** (35 keys). Measured
against the live clip: **15 of the 62 rows have no Indiana-slice key at all** and would have taken a
NULL. The clip is a **spatial** clip, so it holds interstate areas whose polygons reach into Indiana
— Chicago-Gary-Lake County, Cincinnati, Louisville, plus Michigan/Ohio/Kentucky border areas. Worse,
collapsing the join to one row per `(area_name, pollutant_name)` destroys real per-state findings:
Cincinnati 2015 ozone is **Moderate in Kentucky and Marginal in Ohio**.

✅ **What was actually done — no join, and no second object.** The clip is a direct projection of
parent rows, so simply **naming the columns** gives every row its own state-correct classification.
`in_nonattainment` was rebuilt in place by the script that already owns it *and* its overlay export,
so no `in_nonattainment_classified` exists to drift against it (§2.15c). Columns are now named
explicitly and the build **fails loudly** if the parent stops carrying one.
- **Measured result: 62 rows, geog on 62, `classification` on 56.** The 6 without one are **all
  `current_status='Maintenance'`** — redesignated areas where no classification applies. That NULL is
  the honest answer, not a gap, and `row()` already renders it as *"cannot assess"*.
- ⚠ **All five date columns were Esri epoch-MILLISECONDS FLOAT64** (`1083283200000.0` = 2004-04-30),
  measured on each. They are now decoded to `DATE`. Shipped raw, the popup printed
  **"1,087,300,800,000"** at the reader. An ISO parse would have returned NULL on every row.
- Vocabulary now carried (Indiana rows): Former Subpart 1 (14), Marginal (5), Moderate (4),
  Serious (2), Not Classified (2), Subpart 2/Moderate (1), Subpart 2/Marginal (1), none/Maintenance (6).
- **So what:** the classification is the *severity band* (Marginal → Serious → Severe) that drives
  new-combustion permitting burden — a Serious ozone county needs LAER + emission offsets a Marginal
  one does not. "You're in nonattainment" is a flag; "you're in a **Serious** ozone nonattainment
  area" is a cost and a schedule.
- ⛔ The 401-column figure is a NAME-match artefact; the real remaining set is small. Do not
  re-clip 401 columns reflexively.

**Also from the operator:** *"ALL endpoint types, URLs, and loaders should be readily available in
either of the BQ sets."* They are — `energy.registry_sources` carries `endpoint`, `endpoint_kind`,
`acquisition_method` (often a literal `RE-SCRAPE COMMAND:`), `what_it_provides` and `status`, and
`energy.endpoint_truth` (1,281 rows) exists alongside it. **Read those before writing any loader**
(see G25).

#### G26 — ⭐ WHY OUR HEADROOM UNDER-REPORTS: PRE-EXISTING OVERLOADS, AND AN *UNMITIGATED* CASE

Operator, 2026-08-17: *"There may be some discrepancies with the data that we hold, since we likely
didn't scrape for ALL columns, which may underrepresent our findings. Audit the differences,
determine any reasoning and/or rationale, and follow Orennia's datasets as a baseline."*

**Audited. The hypothesis is half right, and the real cause is sharper than a missing column.**

**1. We did drop columns, and one of them mattered.** Our clip `in_miso_poi_300mw` (27 columns)
took the raw API field names while the parent `energy.miso_poi_monitored_facilities` (34) carries
the publisher's own metadata we never took: `_invariant_columns`, `_probe_dependent_columns`
(**the request-invariance proof the agent later had to rediscover experimentally**), `_grain` and
`_vintage`. We also never clipped `energy.miso_poi_headroom`, which carries **`headroom_state`**,
`n_facilities_overloaded_base` and `max_facility_mw_available`.

**2. What `headroom_state` reveals.** Nationally, 12,845 MISO POIs split
**`ZERO_HEADROOM` 10,393** (avg **23.0** facilities overloaded in the BASE case) vs
**`HAS_HEADROOM` 1,427** (avg **482.3 MW**, zero base overloads). The zeros are not measurements of
capacity — they are POIs where a monitored facility is **already over its rating before any new
request exists.**

**3. Measured on Indiana: 26.3% of the 40,007 monitored-facility rows are pre-existing overloads**
(`percent_loading_before >= 100`), averaging **16.4 per POI**. And on the 641 zero POIs,
`max_facility_mw_available` averages **45,800 MW** — the most permissive facility is wide open while
the binding one reads zero because it was already overloaded.

| method | Indiana POIs with injection headroom > 0 |
|---|---|
| A. raw MIN over all facilities — **what we ship** | **1 of 642 (0.2%)** |
| B. exclude pre-existing overloads | 642 of 642 (100%) |
| C. DFAX ≥ 5% only | 1 of 642 (0.2%) |
| D. full PJM recipe (both) | 642 of 642 (100%) |
| **vendor baseline, tier 0** | **39.3%** |

⚠ **Note our own inconsistency:** `in_pjm_bus_withdrawal` is built with *"dfax>=5pct, pre-existing
overloads excluded+counted"*. **Our PJM and MISO numbers are computed by different rules**, which
alone makes them non-comparable — and we present them side by side.

> **OPERATOR RULING, 2026-08-17 — do not fix the PJM/MISO inconsistency in isolation.**
> *"We should be following exactly what Orennia does to derive their numbers, and we should resolve
> this task once we gain near 100% parity with their data."*
>
> So the sequence is **parity first, then unify**: reproduce their derivation — mitigated case,
> actual at a stated request size, per direction, per upgrade tier, with pre-existing overloads
> handled the way they handle them — and let BOTH RTOs adopt that single rule as the outcome.
> ⛔ Picking one of our two current rules and applying it to the other would be choosing between two
> methods we already know are not theirs, and would bake in the wrong answer twice.
> Until parity lands, **state the rule on the face of every headroom number** so the two are never
> silently compared.

**4. THE CAUSE, and it is in the vendor's case name.** Their powerflow case is
**`DPP-2025-Cycle_SUM_D_ERIS-mitigated_Final`** — **ERIS-*mitigated***. The mitigations are applied
in the model, so the pre-existing overloads are resolved. Ours is an **unmitigated DPP-2021** case
carrying four-year-old overloads, many of which have since been fixed by MTEP upgrades actually
built. That is the mechanism by which the vintage matters, and it also explains the held capacity
raster being deeply negative: those negatives are the **deficit from unmitigated overloads**.

So all three observations reconcile: our 0.2% is *correct for an unmitigated 2021 case*; the raster
agrees because it is the same unmitigated picture; and the vendor's 39.3% comes from a **mitigated,
current** case.

**5. What to do — and what NOT to do.**
- ⛔ **Do NOT simply drop pre-existing overloads to reach 100%.** That is not their method either,
  and it would replace an over-pessimistic number with an over-permissive one.
- ✅ **Get the mitigated, current case.** We already hold candidates (G25) — start there, per G25.
- ✅ **Make PJM and MISO use ONE rule**, whatever it is, and state it wherever a number appears.
- ✅ **Clip the dropped columns**, especially `headroom_state` and `n_facilities_overloaded_base`:
  *"zero because already overloaded"* and *"zero because genuinely full"* are different findings and
  the app currently cannot tell a user which one it is showing.

#### G25a — 🔴 WHAT THE HELD CAPACITY SURFACE SAYS, AND HOW IT DENTS MY OWN DIAGNOSIS

`energy.miso_poi_capacity_surface_geotiff` is MISO's published capacity heat map **rasterised** —
3,390,912 pixels at 1,247 m carrying `value_mw`, coordinates and an explicit
`is_publisher_nodata` flag, from `giqueue.misoenergy.org/poi/api/gridLayer/mainGeoTiff`,
**pulled 2026-08-02**. That is a fundamentally better instrument than 642 points: it answers
*"what capacity exists at THIS parcel"* with no bus matching at all.

Indiana slice — 86,850 pixels in the bounding box, 84,174 carrying data:

| | value_mw |
|---|---|
| p10 | **−12,052** |
| **median** | **−7,873** |
| p90 | **−4,001** |
| pixels ≥ 300 MW available | **0 of 84,174** |

⚠ **This complicates the vintage story I gave the operator.** I attributed our 641-of-642 zeros to
our DPP-2021 case being four cycles behind the DPP-2025 baseline. But this surface was pulled
**2026-08-02** and is *also* deeply negative across Indiana — so the near-zero picture is
corroborated by a recent MISO publication, not merely an artifact of our stale case.

**Do not over-read it either.** Pull date is not study vintage, and the semantics of a negative
`value_mw` (a deficit? an over-subscription margin? against what request size?) are **not yet
established.** Establish them before this raster is used in scoring or shown to a user.
**What it does justify right now:** treating "Indiana MISO injection is genuinely constrained" as
the working assumption, and demoting "our number is only stale" from explanation to hypothesis.

The open question becomes the interesting one: **why does the vendor extract show 39.3% of buses
with tier-0 injection headroom when two separate MISO publications say otherwise?** Answer that
before tuning anything toward their numbers.

### ⚠ THE INDEX IS THE CONTRACT. Every operator request in this session has a row here.

An item that exists only in prose further down this file **is invisible in practice** — the operator
could not find the water item (G12) on 2026-08-17 because it had no row in this table, and the
49-missing-bus item had reached the task list but never this document at all. If you add a G-item
below, **add its row here in the same edit.**

| # | task | state | notes |
|---|---|---|---|
| **G105** | **⭐ FULL-SCALE AUDIT OF THE TOOL, INCLUDING EVERY CLICK AND HOVER** | 🔴 **OPEN — operator, 2026-08-19. THE NEXT SESSION'S HEADLINE ITEM** | Operator: *"run a full-scale audit of the tool and fix anything that is not complete, including all of the clicking/hovering actions throughout the tool."* ⭐ **This is the one instruction from 2026-08-19 that was NOT started**, and it should lead the next session. Scope: every layer's click handler and hover tooltip on the map console; every expandable row on the screener; every table link on grid / market / community / owner-signals; the dossier's own controls. ⚠ **`scripts/audit_map_clicks.py` already exists — run it FIRST** rather than starting by hand. ⚠ **The map does not boot headless**, so a click cannot be simulated by loading the page: verify by calling the handler functions directly with a real feature, which is how every front-end fix this session was checked. ⭐ **Precedent for what this finds:** the G65 sweep found four layers DRAWN and unclickable, and three logistics layers with a hover and no click at all — visible for weeks, unexplainable by the reader. |
| **G106** | **BATCH THE REMAINING NON-SCRAPING BACKLOG** | 🔴 **OPEN — operator, 2026-08-19 (standing instruction)** | Operator: *"batch everything in the backlog (except the scraping tasks) and complete all of them."* Recorded as a row so the instruction survives a context change rather than living only in a chat. ⭐ **What that means concretely today:** G70, G71, G72 (the remaining ~226 objects), G75, G78, G79, G80, G81, G82, G83, G84, G86 (sweep), G87, G88, G89, G90 (the normalise half), G92, G93, G96, G101, G104, G105. ⛔ **Explicitly EXCLUDED as scraping:** G15's cost re-extraction, G53/G98, G97, G99, G100, G102, G103. ⚠ And note the operator's standing preference recorded in G107: scraping work goes to an Opus agent. |
| **G107** | **OPERATOR RULINGS THAT ARE NOT TASKS — record them so they are not re-litigated** | 📌 **STANDING — operator, 2026-08-19** | ⛔ **RADIUS-FROM-A-POINT SEARCH IS DECLINED.** Operator, 2026-08-19: *"We do NOT need radius from a point in this analysis."* ⚠ **This matters because `REFERENCE_TOOL_GAP.md` still ranks it #1** and both the handoff and the session prompt starred it as the biggest missing mechanism. It is not. Do not re-propose it. ⭐ **SCRAPING GOES TO AN OPUS AGENT.** Operator, twice: *"if you need to rescrape anything, please use an Opus agent for usage concerns"* and *"use Opus (or some other non-Fable model) to conserve tokens."* Brief it with the write boundary, the no-CAPTCHA/no-UA-spoof rule and BLOCKED-is-a-success, because **agents do not inherit them**. ⭐ **13 DECIMAL PLACES STAY** (see G30b). ⭐ **NEW ITEMS BATCH INTO THE LAST GROUP** — the operator adds work mid-flight and expects it folded in, not queued separately. |
| **G108** | **THE SUPERSEDED-TABLE CHECK CANNOT SEE A BUILD SCRIPT'S SOURCE** | 🔴 **OPEN — found 2026-08-19, and it let a real defect live for days** | `audit_frontend.py` has a superseded-table check, and it did **not** catch that `in_screener_candidates` was reading `vw_pjm_bus_withdrawal_located` and a 300 MW MISO probe long after G63 replaced both with `in_bus_capacity_tier0` — the defect that capped the screener at 132 MW and hid MISO load data entirely (G85). ⭐ **The blind spot is precise: the check inspects PAYLOAD KEYS, not which warehouse table a BUILD SCRIPT reads.** A build can quietly keep sourcing a retired table forever. **Fix:** parse `scripts/build_*.py` and `export_*.py` for the tables they read, compare against `_registry` for objects that a newer table supersedes, and fail on a stale source. ⚠ The registry row is the other half — `in_screener_candidates` still NAMED the old sources in its `source` field, so provenance would have lied even after the code was fixed. Check both. |
| **G109** | **19 LOCATED GRID-PLAN PROJECTS RESOLVE TO A SUBSTATION WITH NO COUNTY** | 🟡 **OPEN, small — found 2026-08-19 while building G15** | `in_grid_plans_located` matched 119 of 618 projects to a substation, but only **100 carry a county** — 19 matched a gazetteer entry whose `county` is NULL. ⚠ **So "located" and "has a county" are different counts and a surface must not conflate them.** Cheap fix: fall back to a spatial county lookup from the substation's lat/lon, which we hold for all of them (`with_coords` is also 100, so the same 19 lack coordinates too — check whether it is the same 19 before assuming a spatial fallback works). |
| **G102** | **STATE-LEVEL SURPLUS PROPERTY — explore what can be scraped** | 🔴 **OPEN — operator, 2026-08-19 (fifth batch), explicitly DEFERRED** | Operator: *"State surplus may also be held at the state level, and it would be interesting to explore the options available to us to scrape this data (we do not have to do this now, but it should be noted in the backlog as something we will eventually want to do)."* **Checked the warehouse first (G25).** We hold three surplus sources and **none of them is state-level**: `gov_surplus_frpp` (307,919 — FEDERAL real property), `gov_surplus_nces` (102,178 — schools) and `agis_indy_landbank_surplus` (595 — the Indianapolis land bank). So the operator is right that this is a gap. ⭐ **And it pairs directly with G97**, which is promoting federal surplus from a map layer to a seller-intent signal — state surplus is the same signal from a different owner, and should land in the same pipeline rather than as a parallel one. The likely publisher is IDOA (Indiana Department of Administration), which handles state land disposal. ⛔ **Check `energy.__TABLES__` again before scraping** and read robots.txt first; a gated source recorded BLOCKED with its wall quoted is a success |
| **G103** | **WATER UTILITIES — as a map layer, in the popups and in the dossier** | 🔴 **OPEN — operator, 2026-08-19 (fifth batch), explicitly DEFERRED** | Operator: *"Add water utilities (both as a map layer and within the popups/dossier), if already available to us (if not, we will have to scrape this data)."* **Measured: we do NOT hold them.** Every water table in `energy` is hydrology or use, not a utility: `nhd_waterbody` (10.4M), `water_cwns_2022` (1.1M), `water_aqueduct` (68,506), `water_use` (3,223), `groundwater_sites` (50), `water_drought_monitor` (5). ⚠ **`water_cwns_2022` is the near-miss to avoid** — the Clean Watersheds Needs Survey is WASTEWATER treatment works, not drinking-water providers, and using it as "water utilities" would be a name match rather than a data match (the CLOUDSCENE_GAP mistake). ⭐ **The right public source is EPA SDWIS** — Safe Drinking Water Information System publishes every Public Water System with its service area, population served, source type (surface vs ground) and violations. **Source type is the part that matters for siting**: a data centre drawing on a groundwater PWS is a different conversation from one on a surface intake, and Indiana is already 99.5% surface for power generation. Pairs with G12 (water at parcel grain, still open) — do them together, since both need the same parcel↔water join |
| **G104** | **⭐ ASSESSED VALUE ON THE PARCEL — popup, dossier and a screener filter** | 🔴 **OPEN — operator, 2026-08-19 (fifth batch), explicitly DEFERRED** | Operator: *"Assessed value for parcels may be already held within our parcel datasets or within USA Structures for the property, and we should explore this fully, including it in the map popup, the dossier, and as a filterable item in the screener."* ⭐ **MEASURED, AND THE ANSWER IS DECISIVE — AND IT IS BIGGER THAN THIS ROW.** `in_parcel_attrs` and its parent both DECLARE the columns: `assessed_value`, `zoning`, `parcel_owner`, `land_use`, `year_built`. **All five are 100% NULL for Indiana** — 0 of 1,143,873 in our clip and **0 of 3,553,381 in the parent**, across all 92 counties. ⛔ **So it is NOT a clip defect and re-clipping will not help.** The parent holds **40,863,827 assessed values and 5,662,067 zoning records across 43 states** — Texas 12.6M, Florida 10.3M, North Carolina 5.6M, even Illinois 1.0M — **and nothing for Indiana.** The national vendor simply does not carry Indiana assessment data. ⭐ **THE CONVERGENCE, which is the real finding: assessed value (G104), owner name (G70), zoning (G71) and the seller-intent funnel (G81) are all blocked on the SAME missing Indiana source, and the DLGF Gateway pull would very likely deliver ALL FOUR at once.** That reframes four separate backlog items as one acquisition and should change how they are prioritised. ⚠ USA Structures is not the fallback the operator hoped — it carries occupancy class, height and footprint, not value. ⚠ And when value does land, remember **unpublished is NULL, never 0**: an unassessed parcel is not a $0 parcel |
| **G97** | **⭐ FEDERAL SURPLUS PROPERTY IS A SELLER-INTENT SIGNAL, NOT A LAND LAYER** | 🔴 **OPEN — operator, 2026-08-19 (fourth batch)** | Operator: *"Use federal surplus property as a SI signal — this will move where it is currently being displayed throughout the application."* ⭐ **The operator is right and the estate already half-agrees with him.** `L-frpp` sits in the map console under **Land & size** as a context layer, but `SIGNAL_PLAIN` ALREADY carries `A2_gov_surplus` — *"a public body has declared the property surplus to its needs"* — classed as a **state**, not an event. So the vocabulary treats it as a signal and the UI treats it as scenery. ⭐ **It may be the single cleanest signal we hold**: every other one in D1–D27 infers willingness from DISTRESS, while a surplus declaration is an owner *publicly stating* it does not want the land — no inference at all. Work: attach FRPP to parcels (it is federal property with coordinates, so a spatial join to `in_sites.parcel_geog`, D85 excluded), admit it through the same sign-off path as every other signal, and MOVE the control from Land & size into Owner motivation. ⚠ The layer must not simply be duplicated into two sections — that is the two-copies-drift defect |
| **G98** | **WITHDRAWN QUEUE PROJECTS AS A SIGNAL, IF THEY CAN BE ADDRESS- OR COORDINATE-INDEXED** | 🔴 **OPEN — operator, 2026-08-19 (fourth batch)** | Operator: *"Use withdrawn queue projects as an SI signal, so long as they can be address or coordinate-indexed."* ⭐ **This is G53 with the blocking condition stated as a condition, which is exactly the right framing** — G53 has sat open because the address lives in late-stage filings and nobody tested whether it is recoverable at scale. The operator has now made that test the gate: if it cannot be indexed, it does not ship as a parcel signal. ⚠ **A coordinate may be the cheaper route than an address.** Queue rows are keyed to a POI or a bus, and PJM publishes its own GIS queue-point layer — we hold it as `in_pjm_gis_queues` (6,923 rows) with the publisher's coordinates. A withdrawn project at a published point is placeable TODAY without any filing parse; what it will not give is the parcel, only the vicinity. **Do the coordinate route first and measure how close it gets before touching the filings.** Pool size: G35 measured 49 of 87 counties with ≥50% of everything ever queued withdrawn. Merge with G53 |
| **G99** | **DO WE HOLD INTERCONNECTION BRANCH DATA? AND EXPLAIN THE TAX-CREDIT GEOGRAPHIES** | 🔴 **OPEN — operator, 2026-08-19 (fourth batch)** | Operator: *"We need to check if we have interconnection branch data in BQ, and, if we don't, how accessible is this information? A description of the environmental checkboxes — how do qualified census tract zones differ from opportunity zones."* ⭐ **Two items, and the first is partly answered already: WE DO HOLD BRANCH TOPOLOGY.** `in_bus_capacity_tier0` carries `primary_limiting_constraint_branch_id`, `contingency_name`, `contingency_id`, `shift_factor_pct`, `constraint_base_flow_mw` and `constraint_contingency_flow_mw`, and G62 established that *every `transmission_facility` string names both endpoint buses*, which is what made the hop-1 placement tier possible. So the branch GRAPH is derivable from what we hold. ⛔ **What we do NOT hold is a published branch inventory with ratings and impedances** — that is CEII for MISO (four sweeps, G7m) and not public for PJM either. **Check before scraping (G25): enumerate `energy.__TABLES__` for branch/line-rating tables first.** ⭐ The second half is a **G21 job on the six tax-credit checkboxes**: a qualified census tract is an HUD designation about income and rent burden used for housing credits, an opportunity zone is a Treasury designation about capital-gains deferral, and an energy community is the IRA designation carrying the 10% ITC adder — three different agencies, three different benefits, and only one is what most energy projects are chasing |
| **G100** | **ARE WE MISSING AN INPUT FOR HOW MUCH GAS IS FREE ON A GIVEN DAY?** | 🔴 **OPEN — operator, 2026-08-19 (fourth batch)** | Operator: *"See if we are missing any data inputs for how much gas is free in a given day."* **What we hold:** `in_gas_capacity_panhandle_eastern` (1,979) and `in_gas_capacity_trunkline` (1,231) are operationally-available capacity from Energy Transfer's ipost, plus `in_gas_state_capacity` and `in_gas_pipelines` (215, HIFLD). ⚠ **Two known weaknesses already recorded:** the MW figure is an **estimate we derive** and is badged as such, and **the unit is INFERRED** (dekatherms/day) because the capture kept no units column. ⭐ **The likely gap is coverage, not concept.** Interstate pipelines post operationally-available capacity per point per gas day under FERC Order 636 — every pipeline does, on its own EBB — and we hold TWO operators. Indiana is also crossed by Texas Gas, ANR, Rockies Express, Midwestern and others. ⛔ **Enumerate `energy.__TABLES__` for gas before scraping anything** (G25 — it has paid for itself seven times). Then the question the operator is really asking becomes answerable: not "is there a pipeline near this parcel" but **"how much unsold capacity passed the nearest point yesterday"**, which is the difference between a pipe and a fuel supply |
| **G101** | **PAYLOADS ARE NOT CACHE-BUSTED, SO A REBUILD CAN SHIP INVISIBLY** | 🔴 **OPEN — found 2026-08-19, small and silent** | **Found 2026-08-19 while verifying G91, and it wasted real time before it was understood.** `stamp_assets.py` appends a content hash to `app.js`, `common.js` and `style.css` — but `fetchGz()` requests `data/*.json.gz` and `data/*.geojson.gz` with **no version parameter at all**. After rebuilding `si_v2.json.gz` the page kept rendering the OLD note strings; the file on disk and a cache-bypassing fetch were both correct. ⭐ **The failure mode is nasty because it is SILENT and it looks like a build failure**: you rebuild, verify in a browser, see the old value and conclude the build did not work. It also means a returning user can sit on stale figures indefinitely while every provenance line on the page swears they are fresh. **Fix:** stamp the payload URLs the same way, or have `fetchGz` append the build id from `state_summary.json`. ⚠ Note `fetchJson()` ALREADY appends `?v=Date.now()` — so the code knows the problem exists and only solved it for one of the two loaders |
| **G93** | **THE FIVE TOP-RIGHT TABS EARN NOTHING — REPLACE THEM** | 🔴 **OPEN — operator, 2026-08-19 (third batch)** | Operator: *"We have no use for the Future Capacity, Market, Acquisitions, Inventory, or Shortlist tabs in the top right of the map — these can be removed and (if able) replaced by something useful, insightful, or actionable to the user."* Measured in `index.html`: `#btn-pipeline`, `#btn-market`, `#btn-acq`, `#btn-inventory`, `#btn-shortlist` — five buttons opening modal panels over the map. ⭐ **Four of the five duplicate a whole PAGE** (Future capacity → `grid.html`, Market → `market.html`, Inventory → `data.html`), which is why they earn nothing: they are a second, worse route to something the nav already reaches, and the governing principle vetoes a surface that cannot say what it changes. ⚠ **Shortlist is the exception — it holds state nothing else holds** (`localStorage in_shortlist`, starred from the evidence panel) and the CSV/Excel export references it, so removing the button must not orphan the feature; fold it into the rail beside workspaces. ⭐ **What to put there instead should be decided from what the map cannot currently answer at a glance** — the honest candidates are a live count of what passes the filters, the binding constraint on the visible buses, and anything from **G94/G96**. ⛔ Do not replace five weak buttons with five new ones |
| **G94** | **SERVICE TERRITORIES: BOLD THE BOUNDARIES AND NAME WHO IS WHERE** | ✅ **DONE 2026-08-19** — they were never actually colour-coded. | ⭐ **The paint tested `utility_type == "investor_owned"` / `"cooperative"`. The payload says `INVESTOR OWNED` (17), `COOPERATIVE` (40), `MUNICIPAL` (14), `NOT AVAILABLE` (74).** All 145 fell through to grey — a guessed value vocabulary, invisible because grey is a plausible colour. Now three layers: fill by type, bolded boundary, utility NAME. Coloured by TYPE not utility (145 colours is noise; the type decides whether the IURC is involved). **NOT AVAILABLE gets its own legend row** because it is half the layer. Operator: *"The service territories layer should contain more than just the overlay — I want to see the service territory boundaries be bolded and be able to decipher exactly which territory is where (they are not currently named or color coded differently anywhere)."* ⭐ **Measured, and the operator is exactly right: `L-terr` draws ONE layer, `terr-fill`, and that is all.** No outline layer, no label layer, and one fill colour for all **145 territories** — so the layer shows *that* a parcel is served and never *by whom*, which is the only question it exists to answer. Everything needed is already in `territories.geojson.gz`: `utility`, `utility_type`, `holding_company`, `regulated`, `control_area`, `customers`, `summer_peak_mw`. **Three layers, not one** — a `line` layer for bolded boundaries, a `symbol` layer for the utility name, and the fill categorically coloured. ⚠ **Colour by `utility_type` (IOU / municipal / REMC / traction), not by utility** — 145 categorical colours is noise, and the type is what changes who you negotiate with and whether the IURC is involved. ⚠ A parcel straddling two territories must keep reporting as straddling (D-11); a boundary drawn boldly makes that case more visible, not less important. Pairs with **G52** (the key must gain the type ramp) |
| **G95** | **ENVIRONMENTAL CONTROLS SAY "EXCLUDE" WHEN THE USER WANTS TO SEE THE LAYER** | ✅ **DONE 2026-08-19** — two verbs were wearing one word. | Filters now sit under "Keep only sites clear of…", separate from the layers that draw geometry. And a **"show me which parcels have one"** selector highlights affected parcels. ⛔ **Not the hazard polygons, and the control says so**: `in_flood` is 803.8 MB and `in_wetlands` 1.3 GB. ⚠ `undefined` is not `false` — an unmeasured parcel keeps the ordinary colour and is not called clear; the running count shows the denominator. Operator: *"For environmental constraints, the layers still say \"exclude\" XXX layer, which doesn't help us if we want to see the layer itself."* ⭐ **Two different verbs are wearing one word, and the confusion is structural rather than cosmetic.** `f-noflood`, `f-nowet` and `f-noprot` are **parcel FILTERS** — they remove parcels from the result set — while `L-padus`, `L-nonatt` and the G72 gates are **LAYERS** that draw geometry. The section presents them in one stack under one subhead, so "exclude flood zones" reads as if it should also draw the flood zones, and it cannot: `in_flood` is **803.8 MB** and `in_wetlands` **1,319.6 MB**, far too large to ship as polygons. ⭐ **So the fix is honest labelling plus one real capability**: relabel the filters as *"only sites clear of …"*, and give flood and wetland a way to be SEEN — either highlight the flagged parcels themselves (the flags are already on every parcel: `sfha_flood`, `wetland_on_parcel`) or ship a simplified or county-level polygon and label it simplified. ⛔ Do not ship a 2 GB payload, and do not leave a control implying a layer exists when it does not |
| **G96** | **RANK ON THE USER'S OWN FILTERS, NOT A 1-5 SCALER, AND SHOW EVERY MATCH** | 🔴 **OPEN — operator, 2026-08-19 (third batch)** | Operator: *"The ranked results should be based on the filters that the user inputs, not a 1-5 scaler; this should ideally be similar to the site screener, and beyond the short list of \"best\" sites, it should also highlight all of the applicable sites to the filters/layers applied to the map (including options for the user to input their own parameters). See how the Illinois tool does this against their HC maps."* ⭐ **This is the deepest item in the batch — it changes what the score MEANS.** Today `scoreSite()` combines six parts under six weights that G68 turned into free numeric inputs, and the composite is an OPINION we hold about a parcel. The operator is asking for the ranking to express **the buyer's own stated requirement** instead. ⚠ **Do not simply delete the weights** — one property of the current scorer is load bearing and must survive: *a part we cannot measure is excluded from the DENOMINATOR, never scored zero*, so a data gap never masquerades as a bad site. ⭐ **The second half is the cheaper and more valuable one**: the map already computes `jsMatches()` for every loaded parcel and draws them all identically, so *highlighting every match* rather than only the shortlist is a paint change over state we already hold. ⚠ Requires a decision the operator has not made yet: when a filter is a hard gate (flood) versus a preference (acreage), because a ranking cannot treat them alike. **Reference the Illinois tool before designing it, as instructed** — and note `REFERENCE_TOOL_GAP.md` already records that their tool separates min-generation from min-load thresholds, which is the same "rank on what I asked for" idea |
| **G77** | **⭐ BUSES SHOULD SHOW HEADROOM BY COLOUR** | ✅ **DONE 2026-08-19** — banded on measured breaks. | ⭐ Buses were sized by headroom and painted one flat amber — comparing circle AREAS, hopeless when 652 of 1,772 sit at zero and the median is 17 MW. Bands are **decisions, not percentiles** (the 25 MW floor, the 300 MW target): 654 / 326 / 500 / 181 / 111. ⛔ **Zero has its own colour, not the dark end of a ramp** — 99.7% of zeros are already over 100% loaded, and a gradient would render "full" and "nearly empty" as neighbours. The legend spells the bands out from the same table the paint uses. Operator: *"Buses should show headroom by coloring."* Today `grid-bus` is **sized** by capacity and coloured amber only where an owner signal exists, so the reader has to compare circle areas to compare headroom — and area is the worst channel for a quantity. ⭐ **Colour it by `headroom_mw`, keep size for something else or drop size entirely.** ⚠ **Band it, do not use a continuous ramp**: a zero here is a statement about the STUDY CASE, not the bus — 99.7% of zero-headroom rows are already ≥100% loaded before any request (G40) — so "0 because already overloaded" must be a *visibly distinct* colour from "small but real", never the dark end of one gradient. `headroom_state` is already on the table for exactly this. Pairs with **G52** (the key must gain a colour ramp entry) and **G21** (the legend must say what the bands MEAN, in MW) |
| **G78** | **⭐ ADDRESS / COORDINATE LOOKUP ON THE MAP, SAVED AND SCORED IN THE SCREENER** | 🔴 **OPEN — operator, 2026-08-19 (second batch)** | Operator: *"There should be an address or coordinate lookup on the map console (that can be saved and used in the screener) — all saved inputted locations should be able to be seen and scored in the screener."* ⭐ **G74 already built most of this and did not expose it.** `SiteStore` holds the user's own sites across pages, `ingestRecords()` places and scores them through the same pipeline as a held parcel, and the screener already shows a banner counting them. What is missing is (a) a single-point entry box beside the file picker, and (b) the saved sites rendered as ROWS IN THE SCREENER TABLE rather than only counted in a banner. ⛔ **A coordinate is free; an ADDRESS is not.** Geocoding an address to a street centreline is refused by project rule — a centreline is not a parcel. Two honest routes: match the address against `in_marion_parcel_crosswalk` and the parcel corpus (exact, where we hold it), or accept the address as a LABEL and require the user to click the point. **Decide which before building the box**, because a lookup that silently centrelines is worse than no lookup. Supersedes the reference-tool 'add a site by click' line |
| **G79** | **THE DOSSIER MUST COVER MANUALLY INPUTTED SITES** | 🔴 **OPEN — operator, 2026-08-19 (second batch)** | Operator: *"The dossier should contain manually inputted sites, not just the sites we currently have in view."* `renderPowerPlan()` takes a held parcel and reads its own polygon — Figure 2 draws the parcel to scale and the serving utility resolves from the FOOTPRINT (up to 64 ring vertices), neither of which an uploaded point has. ⭐ **So this is not a wiring job, it is a DEGRADED-MODE design**: which figures survive on a point, and what each missing one says instead. Everything keyed to location still works — tariff at that utility, county posture, distances, tax-credit zone, bus. Everything keyed to the parcel boundary does not — acreage, buildable area, the diagram, the straddle test. ⚠ **The missing ones must render as themselves** (G51), never as a zero or a blank. Depends on **G73**, which is rewriting the dossier anyway — do them together or the structure will be rewritten twice |
| **G80** | **⭐ AN EXTENSIVE RE-SWEEP OF BQ FOR LAYERS WE HOLD AND DO NOT USE** | 🔴 **OPEN — operator, 2026-08-19 (second batch)** | Operator: *"We need to go an extensive check of BQ and discover more layers/tables/views to use (e.g., we missed tribal land and military bases previously)."* ⭐ **And the miss was worse than "unused".** G72 found `in_tribal_land` held **14 rows, none of them in Indiana** — clipped by a geoid key join instead of spatially — so the table was both unwired AND wrong, and nothing caught it because nothing rendered it. **That is the real lesson: an unwired table is an UNAUDITED table.** ⛔ **Do not do this by reading names.** The sweep must enumerate `energy.__TABLES__` (G25) and, for every candidate, MEASURE the Indiana row count spatially before proposing it — the clip method recorded in `_registry` is not evidence that the clip worked. `TABLE_PURPOSE_INDEX.md` is the starting worklist (**231 objects reach no control**) but it only covers what is already clipped into `indiana_app`; this item is about the parent dataset as well. Fold the results into **G72** |
| **G81** | **⭐ IS 24,277 FLAGGED PARCELS TRUTHFUL? WHY IS IT NOT MORE?** | 🔴 **OPEN — operator, 2026-08-19 (second batch)** | Operator: *"We need to determine if ~24,000 seller intent properties is actually truthful — why don't we have more?"* **Measured: 24,277, and the payload and warehouse agree** (the checkpoint asserts it every run). So the number is not a rendering error. ⭐ **The question is the FUNNEL, and it should be published as one**: `in_si_signals` holds **1,818,158 raw signal rows** and only 24,277 parcels carry a flag. Three candidate causes, and they need separating before anything is "fixed": **(1) grain** — D11/D27/D19 are OWNER-grain and cannot reach a parcel at all today (**G82**); **(2) keying** — a signal matched by location is weaker and may be being dropped; **(3) coverage** — `mat_parcel_attrs.parcel_owner` is **NULL on all 3,553,381 parcels** except Marion's 340,765, so every owner-name join outside Marion fails by construction. ⭐ **The statewide DLGF Gateway owner pull is the single highest-value acquisition left and it unblocks five signals** — that is very likely the honest answer to "why not more". Deliverable: a stage-by-stage funnel with the loss at each stage NAMED, on the Owner signals page. ⛔ Do not tune a threshold to make the number bigger |
| **G82** | **SIGNALS MUST REACH A PARCEL FOR D11, D27 AND D19** | 🔴 **OPEN — operator, 2026-08-19 (second batch)** | Operator: *"We should be able to reach a parcel with D11, D27, and D19, and this should be further explored (e.g., if we have an address, we can match it to the parcel through geocoding or a join key)."* ⭐ **MEASURED, AND THE OPERATOR IS RIGHT — THE ADDRESS IS ALREADY THERE.** Both `in_si_d11_admitted` (983 rows) and `in_si_d27_admitted` (156) carry **`address_line`, `city`, `addr_state`, `zip`** and a `keying` column. So these are filed owner-grain not because we lack an address but because nobody joined on it. ⚠ `in_si_d25_admitted` (127) does **not** — it carries docket / filing / pdf_url only, so it stays owner-grain until an address is extracted from the filing. ⛔ **Match on the address we HOLD; do not geocode to a centreline.** Marion has a real crosswalk (`in_marion_parcel_crosswalk`, 347,049 rows); elsewhere, normalise and join to the parcel address corpus and **record `keying` honestly** so a weak match is visible as one. Feeds **G81** |
| **G83** | **⭐ EXPLAIN EVERY SIGNAL, AND DISCLOSE THE SEVERITY FILTER WE APPLIED** | 🔴 **OPEN — operator, 2026-08-19 (second batch)** | Operator: *"We need to further explain each signal, with all of the data that each signal brings to the table. As such, we filter by severity on many of the signals, and we should be transparent about which filters were applied, why, and what the severity actually shows (e.g., what does environmental violation mean at a site, and why is that a severe enough signal that it made it to the application?)."* ⭐ **This is the most important honesty item on the list.** A severity threshold is a HIDDEN EDITORIAL DECISION: it changes which parcels appear and the reader cannot see it, which is the same defect shape as the `.slice(0, 14)` that showed 14 of 432 qualifying tariff rows without saying so (G58). Per signal, publish: the source, the vocabulary it uses, **the threshold we applied and how many rows it excluded**, what the surviving severity physically means at a site, and the inference to seller intent. Pairs with **G84**, which asks the harder version of the same question |
| **G84** | **⭐ WHY DOES AN ENVIRONMENTAL VIOLATION IMPLY INTENT TO SELL?** | 🔴 **OPEN — operator, 2026-08-19 (second batch)** | Operator: *"For IDEM enforcement, why does this matter, and how does this signal intent? For instance, an environmental violation usually doesn't result in a sale, so why do we believe our environmental violations will? Same goes for EPA ECHO."* ⭐ **Take this as a live challenge to the signal, not a request for better wording.** It is the largest signal family we carry — `in_si_d22_echo_indiana` **58,003 rows** and `in_si_d22_idem_dated` **22,565** — and the operator has already ruled once that maintenance activity does **not** prove a willing seller. The honest answers are narrower than "a violation": an enforcement ORDER with a compliance cost, a CLOSURE or permit termination, a facility that stopped reporting. **Deliverable: either a defensible mechanism per severity band, or the signal is demoted to context and says so.** ⛔ Do not defend it by volume — 58,003 rows is exactly the "volume standing in for value" failure the governing principle names. Depends on **G83** for the vocabulary |
| **G85** | **⭐ BUS DATA MUST BE CONSISTENT ACROSS EVERY SURFACE** | ✅ **DONE 2026-08-19** — BOTH halves, rendering and source. | ⭐ Rendering half: the unified bus section (G4). ⭐ **Source half, and it was the bigger one:** `in_screener_candidates` read `vw_pjm_bus_withdrawal_located` (PJM only, 227 buses) for withdrawal and a **MISO 300 MW probe** for injection. So `wd_mw` spanned **13–132 MW** — nothing could ever reach 300, the page's own default — and **the screener held NO MISO load-side data at all**. Repointed at `in_bus_capacity_tier0`. Load figures 207,614 → 530,734 parcels; sites at ≥300 MW **0 → 5,249**. Operator: *"The bus data needs to be consistent throughout (e.g., MISO POIS shows only injection, and the Orennia dataset we use contains both and all of the specs that PJM provides us)."* ⭐ **MEASURED, AND THIS IS A RENDERING DEFECT, NOT A DATA GAP — so it is cheap.** `in_bus_capacity_tier0` already holds **both directions for both ISOs**: MISO **1,731 injection + 1,731 withdrawal**, PJM **1,814 injection + 1,826 withdrawal**, with **zero null capacities**. So the MISO card on `grid.html` is showing one of two directions we hold. ⚠ Note the PJM asymmetry — **1,814 vs 1,826 is a real 12-bus difference**, not a rounding artefact, and it should be stated rather than smoothed. Audit every surface that names a bus (map layer, grid page, screener, dossier) against ONE resolver so they cannot drift. Pairs with **G77** (colour) and **G86** (links) |
| **G86** | **⭐ EVERYTHING SHOULD BE CLICKABLE THROUGH TO ITS LOCATION ON THE MAP** | 🟡 **MECHANISM SHIPPED 2026-08-19; the sweep across other tables remains** | ⭐ `index.html?lat=&lon=&z=&label=&src=` now flies to a coordinate, drops a marker and opens a panel — built because the new bus table needed it, so every bus row links to its location. ⚠ The panel says the coordinate **may be an ESTIMATE** rather than implying a survey (91.9% of the vendor's PJM positions are estimated). **What remains is applying it** to the other tables the operator named — SI signals, grid assets, upgrades. Operator: *"Ideally, everything should be clickable to bring the user to the location that the table uses (e.g., buses in grid and capacity should have a link to the map for where they are located, and this should be expanded throughout for all applicable grid assets, layers, SI signals, etc.)."* ⭐ **The mechanism already exists and is proven** — G39 built `index.html?fips=&parcel=[&open=dossier]`, which drives the county-loading machinery itself rather than waiting for the viewport. Extend the deep link to accept a **bus / substation / asset id or a lat-lon + zoom**, then make every table row that names a located thing a link. ⚠ **Only where a location is real.** 92 of 242 data-centre pins are city centroids and 91.9% of PJM buses are ESTIMATED positions — a link that flies to an estimate without saying so converts a caveat into a coordinate. Link *and* badge, or do not link |
| **G87** | **⭐ FUTURE CAPACITY MUST GO FAR BEYOND THREE FIELDS, WITH A MAP** | 🔴 **OPEN — operator, 2026-08-19 (second batch)** | Operator: *"We should expand upon what we have for future capacity, leaning into kV, in service, cost, etc., and going beyond these three fields ... maybe you should follow what we do on the Illinois map to better outline where the upgrades occur and where they may be located; the utilities often provide location estimates or regions where a project will take place — again, reference against the Illinois tool."* ⭐ **This is G15 with the operator naming the reference implementation, and `REFERENCE_TOOL_GAP.md` already identified the same thing** — their Illinois tool draws planned investments with **location-uncertainty rings**, which is exactly the two-tier locate G15 prescribes (exact join for existing assets, rings for vaguely-described new ones). State of play: `in_grid_plans` holds **618 rows with county on 0, cost on 0 and in-service year on 15**, and **227 IURC documents are identified, public and unparsed**. ⚠ Its `_registry` row still claims 7 rows and fails the G16 re-runnability test. **Merge this into G15 rather than tracking two rows** |
| **G88** | **COUNTY POSTURE: DC DEVELOPMENTS COMPLETED / IN PROGRESS, AND THE UTILITY** | 🔴 **OPEN — operator, 2026-08-19 (second batch)** | Operator: *"For county posture, instead of having queued MW, we should put the number of DC developments that are either completed/in progress (maybe one field for each), and the utility should be able to be determined."* ⭐ **Agreed, and queued MW deserves to go** — it is an interconnection-queue figure standing in for development activity, and G35 measured that **49 of 87 counties have ≥50% of everything ever queued withdrawn**, so it overstates by design. We hold what is needed: `in_data_centers_deduped` (242), `in_dc_county_counts` (built for G48) and `in_territories` (145) for the serving utility. ⚠ **Two counts, never one**, and ⚠ **92 of the 242 pins are city centroids**, so a per-county count must state how many of its own pins are city-precision. Utility must resolve from the FOOTPRINT where one exists, and a straddle reports as a straddle (D-11) |
| **G89** | **DERIVE THE EXPIRY OF A DC BAN OR MORATORIUM WHERE IT IS NOT PUBLISHED** | 🔴 **OPEN — operator, 2026-08-19 (second batch)** | Operator: *"We should probably dive deeper into the expiration dates of DC bans/moratoriums, and if none exists, we should be able to estimate/derive an expiration date based on the data we currently hold (e.g., one year moratorium is 365 days from the effective date)."* ⭐ **MEASURED, and the columns already exist — they are just empty.** `in_dc_actions_resolved` (86 rows) carries `verified_effective_from`, `verified_effective_to` **and** `expiry_condition_verbatim`, populated on **13 / 3 / 6** rows respectively. Of **9 confirmed moratoriums only 2 carry an end date**. So: extract the duration from the instrument text into `expiry_condition_verbatim`, then derive `verified_effective_to` from `effective_from + duration`. ⛔ **A derived date must never style as a published one** — badge it, and ⚠ an open-ended moratorium ("until the ordinance is amended") must stay NULL with its condition quoted, because inventing a date there is worse than admitting there is none. Note one row is already `expired-moratorium`, so the surface must handle a lapsed action |
| **G90** | **⭐ WARN NOTICES: WE ARE USING THE LAYOFFS AND IGNORING THE CLOSURES** | 🟡 **MEASURED 2026-08-19 — the split is cheap; making it REACH a parcel is not, and is blocked on the same acquisition as G104** | ⭐ **Measured: the table is FULLY POPULATED — 1,220 rows, every one carrying Company, City, `LO_CL_Date`, `Affected_Workers` and `NAICS`.** So the closure/layoff split the operator asked for is genuinely cheap: `Notice_Type` gives **CL 564 / LO 415 / "Potential Closure" 22 / N/A 204** plus free-text variants, and normalising it is a declarative mapping. ⛔ **BUT THE SIGNAL BARELY REACHES A PARCEL TODAY — `D19_warn` is on exactly 2 parcels** — and the reason is structural: **the WARN table carries Company and City and NO STREET ADDRESS.** A company name plus a city cannot resolve to a parcel; it would need matching against a parcel OWNER, and Indiana parcel owner is NULL on all 3,553,381 rows. ⭐ **So G90 splits in two, and they have very different costs.** (a) Normalise `Notice_Type` and surface closures distinctly on the Owner signals page — do this, it is small and it improves what the signal MEANS wherever it lands. (b) Make WARN reach parcels — **blocked on the same DLGF Gateway acquisition as G104, G70, G71 and G81**, and should be prioritised with them rather than separately. ⚠ Do not attempt a company-name-to-parcel match before owner data exists; that is the CLOUDSCENE_GAP mistake waiting to happen. Operator: *"For WARN notices, we should have more than just layoff data, and much of this is actually useful in determining SI (e.g., a closure or future closure would be useful)."* ⭐ **MEASURED, AND THE OPERATOR IS EXACTLY RIGHT — THE CLOSURES ARE THE MAJORITY AND WE ARE NOT USING THEM.** `in_si_state_warn_notices` carries a **`Notice_Type`** column: **CL (closure) 564**, **LO (layoff) 415**, `N/A` 204, **"Potential Closure" 22**, plus RH / TR and mixed strings such as "LO and CL" and "PENDING CL". ⭐ **A closure is a far stronger seller-intent signal than a layoff** — a layoff keeps the site operating, a closure vacates it — and a *future* closure is a dated forward event, which is the scarcest thing in the whole signal estate. Also unused on the same table: `LO_CL_Date` (the event date, distinct from `Notice_Date`), `Affected_Workers` (a size proxy), `NAICS` (industrial vs retail — a retail closure is not a siting opportunity) and `notice_pdf_urls`. ⚠ **The vocabulary is dirty** — 204 `N/A` and free-text variants — so normalise it declaratively and report the residue rather than defaulting it to layoff |
| **G91** | **⭐ NO NUMBER CLASSIFICATIONS IN USER-FACING TEXT — D22, A6, P1** | ✅ **DONE 2026-08-19** — and the probe that reported 'none' while a dozen were on screen. | ⭐ The check required an UNDERSCORE SUFFIX, so it caught `D4_tax_delinquency` and sailed past `D22`, `A6`, `D11`, `Lane D` — the form actually complained about. Widened (with a self-test), then fixed in all three routes G91 predicted: **markup** (12 headings and the `<title>`, which still read "SI Feed"), **strings built in JS** (4 prose blocks the markup probe cannot see), and **values in the payload** (`signalsPlain` stripped `D11_` but not `D11 `, so 19 cells printed the code in front of the name it already carried). Verified on the RENDERED page: 0 codes in 27 table cells, 0 in any text node. Operator: *"We really need to ensure that the number classifications (e.g., A6, D22, etc., don't make it to the app's text — this should be a part of the polish)."* This is **G8 tightened and re-scoped**, and G8 is nearly closed on the narrow test: `audit_backlog_truth.py` reports **zero raw D-codes in page markup**. ⚠ **But that probe only greps the static HTML.** Codes reach the reader by three other routes it cannot see: **strings built in JS** (template literals defeated a whole front-end audit once — G21/2026-08-17), **values carried in the payloads themselves** (`signals` is a comma-separated list of D-codes; the screener already maps it through `SIGNAL_PLAIN` but every new surface must too), and **the CSV and Excel exports**. ⭐ **Make it a CHECK, not a sweep** — extend `audit_frontend.py` to fail on a `/\b[A-D]\d{1,2}\b/` match in rendered text and in export headers. A sweep is done once and rots; a check holds. Fold into **G75** |
| **G92** | **MAP POPUPS NEED THE DEPTH THE PAYLOADS ALREADY CARRY** | 🟡 **PARTLY DONE 2026-08-19 — the gates reached the popup; gas headroom did not** | The G72 land-status gates now render in the parcel evidence panel with three-state rows. **Still open: the operator's own example** — gas pipelines showing headroom, and the sweep of other popups against what their payloads already carry. Operator: *"We need more data in the map popups (e.g., the natural gas pipelines should show headroom, where the data is available, and this can be applied elsewhere as well, in many places)."* The gas popup prints **operator and type — two fields** — while we hold `in_gas_capacity_panhandle_eastern` (**1,979 rows**) and `in_gas_capacity_trunkline` (**1,231**), which are operationally-available capacity, plus `in_gas_state_capacity`. ⭐ **Same shape as G39's per-site depth: pure rendering, nothing new to fetch.** ⚠ Three cautions carried from the Market page, which already prices this data honestly: the MW figure is an **estimate we derive** and must stay badged; the unit is **inferred** (dekatherms/day) because the capture kept no units column; and joining a capacity POINT to a pipeline LINE is a spatial guess, so state the join or omit the number. Audit every popup against its own payload — `miscEv()` already truncates several with `.slice(0, 9)` and `.slice(0, 16)`, which is a silent cap of exactly the kind G58 was about |
| **G70** | **⭐ MORE ABOUT THE ACTUAL PARCEL — address, coordinates, building use, owner** | 🔴 **OPEN — operator, 2026-08-19** | Operator: *"we require more information about the actual parcel (e.g., address, coordinates (if available), email address, owner name, building use (USA Structures should publish this), and similar."* ⭐ **`nat_usa_structures` holds 3,377,472 Indiana rows and is one of the 52 GENUINELY UNWIRED tables in `GAP_REGISTER.md`** — it is the largest unwired object we hold and it carries occupancy class, which is exactly the "building use" asked for. `in_sites` already has `occ_cls`, `structure_count`, `structure_sqft` and `build_id`, and `build_id` is the join key into the structures corpus. ⛔ **DO NOT PROMISE OWNER NAME OR EMAIL.** `mat_parcel_attrs.parcel_owner` is **NULL on all 3,553,381 Indiana parcels** (re-measured repeatedly) — the only owner data we hold is Marion County's, via `in_si_d9_absentee_marion` (340,765 parcels, from the county's own crosswalk). Statewide owner is the DLGF Gateway pull, which is the single highest-value acquisition left and unblocks five signals. **Ship what we have, name what we do not**: address and coordinates are held, building use is one clip away, owner is Marion-only, and email is held nowhere at all |
| **G71** | **Zoning from BigQuery, if we hold it** | 🔴 **OPEN — operator, 2026-08-19** | Operator: *"If we have zoning information in BQ, we should use it."* ⚠ **Measure before promising — two different things wear this name.** `in_ordinances_county_codified` (55 counties) and `in_ordinances_dc_v2` are **codified ORDINANCE TEXT**, which answers "does this county's code permit a data centre", not "what is this parcel zoned". Parcel-grain zoning would come from `agis_indy_rezoning` and the county `agis_*` layers, which are metro-only. **The honest deliverable is probably TWO surfaces, not one:** county-level permission from the ordinance corpus (already held and already on Community), and parcel-level zoning wherever a county publishes it, labelled with its coverage. ⛔ Do not blend them — a codified silence is not a zoning designation, and Jefferson County approved a 7.1M sq ft data centre by administrative interpretation under a code that never mentions them |
| **G72** | **⭐ WIRE THE DATASETS WE HOLD AND DO NOT SHOW — the 231** | 🔴 **OPEN — operator, 2026-08-19. THE BIGGEST ITEM ON THIS LIST** | Operator: *"we should add datasets from BQ like military bases, tribal land, and similar datasets that give us more contextual information about the land and environment (I know that we don't currently use all of our BQ tables that we could — this is NOT limited to only the environment section)."* ⭐ **The worklist already exists and is generated: `docs/TABLE_PURPOSE_INDEX.md`.** Of 301 objects only **38 reach a control the user can operate**; **79 are PAGE ONLY** (they reach a filterable page but no control names them) and **152 are READ-ONLY** (rendered, nothing you can ask a question with). That 231 IS this item. The two the operator named are both already clipped and both unshown — `in_land_military_bases` (13) and `in_tribal_land`. ⚠ **G21 binds every one of them:** a layer without its "so what" is volume standing in for value, and G65 just added 11 layers that each need one. **Work it by OBJECTIVE, not alphabetically** — the index classifies all 301 into P1–P6/context/infrastructure, so a session can close "everything that answers *what could stop the permit*" in one pass |
| **G73** | **Rewrite the dossier around OUR data, not the supplied PDF** | ✅ **DONE 2026-08-19** — the borrowed PDF format is gone. | ⭐ **Figure 4 was eight hardcoded milestones all reading "Not started"** — dossier audit D-8 had already named why that is decoration: *it would not change if a study did exist*. Replaced with "What stands between this parcel and power": seven rows measured at THIS parcel, ordered by what can STOP a project above what only costs time. New Figure 5 carries the G72 gates. Verified by RENDERING a real Delaware County parcel, not by reading. Operator: *"The dossier should be geared more to the data that we hold, rather than the document I provided earlier — that was just an idea of what I have seen in the past. Make it revolve around our application, and the overall message that site SHOULD give the user based on what we see, and why it is important; much of the data is fine, but a couple of sections need revision or removal, and other sections should probably be added."* ⚠ **This supersedes the four-page Power Plan structure copied from `PG_LA-Cajun_Power Plan.pdf`.** What we hold that the borrowed format has no room for: the **priced tariff** at this parcel's own utility (22 utilities costed from their books at every service voltage with riders), **25 owner-motivation signals** with event dates and how each reached the parcel, **county posture and the receipts behind it**, exact distances to line/substation/bus/water, and the **buildable-area basis**. What the borrowed format carries that we cannot fill: Figure 4's eight hardcoded milestone rows (D-8, still literal markup). ⛔ Verify by RENDERING, not reading — a parse check once passed `esc is not defined`, which would have made the Dossier button do nothing, and the map does not boot headless so `openDossier()` must be called by hand with a real parcel |
| **G74** | **⭐ ANY Excel in, rich Excel out, persisted across pages** | ✅ **DONE 2026-08-19** — any sheet in, rich workbook out, kept across pages. | ⭐ `vendor/xlsx-lite.js`, ~9 KB, no CDN. Reads .xlsx/.xlsm/.csv/.tsv, writes .xlsx. **Header row detected; column mapping proposed and then ASKED**, because a silent wrong guess on longitude puts every site in the wrong county invisibly. ⚠ **sessionStorage alone gets the persistence WRONG** — it survives a refresh. `SiteStore` reads the Navigation Timing type and drops itself on `reload`. Both halves verified in a browser. Operator: *"It is crucial that we have full functionality for the user to load in sites within the application. ANY Excel sheet should be able to be inputted and saved to the application (locally, always resetting when the page is refreshed, but should stay when changing between the application's pages). The Excel outputs should also be nearly all-encompassing."* **Today: CSV only, and the upload does not survive a page change.** ⭐ **`sessionStorage` is exactly the requested semantics** — it persists across navigation within a tab and clears on refresh/close, which is what was asked for, and it keeps the data in the browser (the site is static; nothing is uploaded anywhere). Needs a **vendored** SheetJS — the CSP/offline posture of this repo is to vendor, as `vendor/maplibre-gl.css` already does. ⚠ **"ANY Excel sheet" means column mapping cannot be hardcoded**: detect the header row, let the user map their columns to lat/lon or address, and **keep the rows that fail** labelled rather than dropping them — §13(2) upload parity was closed on exactly that behaviour. Exports should carry every column the screener holds, not the six the CSV writer emits |
| **G75** | **Polish pass: no stale tables, professional finish** | 🔴 **OPEN — operator, 2026-08-19** | Operator: *"Now would be a great time to polish the application, make sure everything is updated and no stale tables exist, and that the page is highly professional looking."* **Run the instruments FIRST, then decide:** `scripts/audit_backlog_truth.py` (probes each open item), `scripts/audit_wiring_census.py`, `scripts/audit_frontend.py` (a superseded-table check is built into it), `scripts/sync_layer_counts.py --check` and `scripts/audit_map_clicks.py`. ⚠ **"Stale" has a specific meaning here and the checkpoint already guards two of the three shapes** — a payload older than its table, and a payload that lost a key. The third, *a surface reading a superseded generation*, is what `audit_frontend.py` covers. ⭐ The remaining visual half is **G6** (polish) and **G18/G21** (every surface earns its "so what"), and the honest order is: retire what is stale, then make what remains legible — polishing a stale surface is the worse outcome |
| **G76** | **The acceptance run's "public-data-only" check fails on the DISCLOSURES the rules require** | ✅ **DONE 2026-08-19** — both halves, and they were broken in opposite directions. | ⭐ The criterion grepped for the vendor NAME, which G50 REQUIRES us to print — all 9 hits across 4 files were prose disclosures, **zero were leaks**. Now tests for table IDENTIFIERS, which cannot occur in an English sentence; self-tested 4/4 leaks caught, 0/5 disclosures flagged. AND `checkpoint.py` had `check(label, True, ...)` — a literal hardcoded PASS — so it printed green regardless and disagreed with the state block. Now reports PASS/PARTIAL/FAIL, without double-counting the wiring census. `docs/ACCEPTANCE_RUN.json` criterion 1 reads **FAIL — 4 leaks in `app.js`, `data.html`, `grid.html`**. Measured: all of them are prose like *"the load-side figure below comes from our licensed Orennia subscription"* — which **G50 explicitly REQUIRES**: the vendor rows must render with their source named on the face of the surface. The check greps for the vendor's NAME and so fails us for obeying the rule that says to print it. ⛔ **The cost is not the red line, it is that a GENUINE leak would look identical** — the criterion is permanently failing, which is how a real one gets ignored (rule 9: an audit that cries wolf gets ignored). **Fix:** test for vendor VALUES reaching a payload or a rendered figure, not for the vendor's name in prose. ⚠ Also worth fixing alongside: `scripts/checkpoint.py` hardcodes the acceptance line to PASS (`check(label, True, ...)`), so the checkpoint prints green while two criteria fail — the generated BACKLOG state block reports them correctly, and the two disagree |
| **G65** | **⭐ EVERY MAP FEATURE MUST BE ITS OWN TOGGLE** | ✅ **DONE 2026-08-19 — browser-verified.** **19 checkboxes → 30 controls** (26 in `LAYER_MAP` + 2 context + parcels + screener). Five bundles split: gas into pipelines / compressors / storage; generation into power plants / solar / wind; logistics into rail / primary roads / secondary roads; PJM into queue points / candidate buses; and **tax-credit areas into all six geographies**, with critical habitat coloured red and labelled *a CONSTRAINT, not a credit*. Every new layer carries its own click-to-evidence handler — splitting a layer without splitting its handler would have silently removed the popup that explains it. Verified in a browser: `ALL_LAYER_BOXES` 30, `LAYER_MAP` 26, **0 registry entries without a checkbox** (the one thing that throws at boot) and 0 layers the registry points at that are never added | Operator: *"everything that a user would use within their tool should be in the map console as a checkbox that could be turned on or off as they wish — every map feature should be togglable by nature."* **Measured: 19 layer checkboxes cover 33 distinct drawable feature classes, so 14 classes have no control of their own.** Five boxes are bundles hiding 18 classes: `L-fac` draws power plants (2,671), wind turbines (1,652), HIFLD plants (208) and solar (114) as ONE thing; `L-log` merges rail (2,117), secondary roads (861) and primary roads (225); `L-gas` merges pipelines (215), compressors (24) and storage (22); `L-pjm` merges queue points (425) and candidate buses (229); and `L-bonusgeo` hides **SIX different federal tax-credit geographies** behind one word — low-income tract 662, QCT 337, coal closure 221, opportunity zone 156, critical habitat 10, energy community 8. ⭐ **These are not cosmetic groupings — they answer different questions.** A developer chasing the energy-community adder cannot separate it from a critical-habitat polygon that would stop the project, and "solar" and "wind" are different siting facts wearing one label. ⛔ **Build it THROUGH `ALL_LAYER_BOXES`, never as new one-off ids** (G34): that registry is what enforces "off means hidden", and any layer added outside it will keep drawing while its box is clear. ⚠ The payloads already carry the `kind`/`layer` property, so each sub-layer is a MapLibre layer id with a `filter` on it — a rendering split, not a data rebuild. **Pairs with G52** (a legend for what is drawn) and **G21** (each new toggle needs its "so what" written at the same time) |
| **G66** | **Remove the "Jump to a view" preset block** | ✅ **DONE 2026-08-19.** The railcard and the 42-line `PRESETS` object plus its applier are gone; `ALL_LAYER_BOXES` and `syncLayers()` deliberately kept. The delete script asserted the applier was inside the cut range and that both survivors were still present **before** writing — refusing to cut blind, because removing the registry with the presets is exactly G34. Browser-verified: `typeof PRESETS === "undefined"`, `#presets` absent, the words *Jump to a view* nowhere in the page, and the whole of `app.js` still executes through `renderPowerPlan` and `openDossier` | Operator: *"remove the JUMP TO A VIEW section, since we want the user to toggle these things on and off."* It is `index.html:41-42`. ⚠ **This is not just deleting a div — it retires the PRESETS machinery, and half of that machinery must SURVIVE.** `PRESETS`, the preset applier and `PRESET_GAPS` go. **`ALL_LAYER_BOXES` and `syncLayers()` MUST STAY**: the registry is the single source of truth for layer state, and deleting it with the presets would reintroduce the exact G34 defect — `grid` set `L-fac:1`, no other preset mentioned it, and wind and solar stayed drawn after a preset switch because nothing ever set them back to 0. ⛔ **Delete the scripts that read the removed nodes in the SAME edit.** G47 recorded the trap: a `getElementById()` left pointing at a deleted node throws on null and takes **every later line of that script with it**, silently emptying the rest of the page — the same failure shape that once killed the map |
| **G67** | **⭐ THE SCREENER MUST FILTER ON EVERYTHING, IN SECTIONS** | ✅ **DONE 2026-08-19 — browser-verified.** **23 controls → 36**, and a sixth section. ⛔ **CORRECTION to this row's original text: it said the screener had ZERO sections. That was wrong.** It already had five `<details class="acc">` blocks mirroring the map console; my probe counted `<h2>` elements and never looked at `<summary>`. The missing FILTERS were real; the missing sections were not. **13 filters added, every one backed by a field the payload already carried, so nothing needed a re-export:** county (92), land-record type, open-ground acres, structure count, connection-point kV, binding facility (free text), placement confidence, **which owner-motivation signal** (this alone replaced one boolean standing in for the whole D1–D27 taxonomy), event counts in a 3/5/10-year window, how the signal reached the parcel, which tax-credit zone, and a new §6 *What we can show you* holding the mappable-only filter. Enumerated dropdowns are populated **from the data** so an option cannot drift from the vocabulary. ⚠ **Three defects found by RENDERING, not by reading:** `signal_types` is a COUNT and the names live in `signals`, so the filter was splitting the digit `1`; `SIGNAL_PLAIN[v]` is a tuple `[label, explanation, kind]` and was being printed whole into the dropdown; and `landPlain()` keys on `occ_group`, so calling it on `site_kind` labelled a parcel WITH a building *undeveloped land* — the G10 defect reintroduced by using the right helper on the wrong field. ⚠ **STILL OPEN, and NOT faked:** county posture, water stress, transmission-line distance and the serving utility/tariff are not in `screener.json.gz` — they need an exporter change, filed rather than invented | Operator: *"in the site screener, the user should be able to filter by EVERY single feature that this tool offers, and they should be sectioned as the map console is (except, we will likely need more than just the map layers, so we will have to create more dropdowns)."* **Measured: `screener.json.gz` carries 43 fields per site; the page exposes filters on about 15; and it has ZERO sections** — three `<h2>`s, not one of which is a filter group, so every control sits in one flat block. **Unfilterable today although each is ALREADY in the payload:** county (all 92 — there is no county filter at all), `signal_types` (**the whole D1–D27 taxonomy collapses to one has-a-signal tick**), `bonus_kinds` (same shape — six credit zones behind one tick), `structure_count`, `site_kind`, `inj_kv`/`wd_kv`, `inj_binding`/`wd_binding`, `wd_conf` (placement confidence), `events_3y`/`_5y`/`_10y`, `first_event`, `keying`, `sub_name`, `lat`/`lon` (plottable-only), and `outdoor_acres` separately from `parcel_acres`. **And FIVE dimensions the map console filters that the screener has no control for at all:** transmission-line distance, line kV, county posture / opposition, water stress, and the serving utility with its tariff. ⭐ **The section scheme cannot just copy the map's six themes** — the operator is explicit that more are needed, because the screener filters on things that are not map layers: dates, event counts, match confidence, and cost. ⚠ **Preserve every existing `s-*` id** (G5): a renamed id kills a filter SILENTLY, and `audit_frontend.py` reads source, not runtime, so it will not catch it |
| **G68** | **Any number, anywhere — no silent caps** | ✅ **DONE 2026-08-19.** Every `max=` on both pages is gone — measured after: **0 remaining caps**. Density was `max=20` MW/acre on the map console and the screener; signal recency was `max=10` years. ⭐ **The six scoring weights were range sliders 0–5 with a read-only span beside them**, so a weight could be neither typed nor set above 5; they are now number inputs with no ceiling, the input IS the readout, and the `wv-*` mirror spans are retired. `scoreSite()` reads `w-${k}` unchanged. Browser-verified: all six are `type=number`, `max` null, `wv-p1` absent | Operator: *"the user should be able to input ANY number (or be given a dropdown, where a specified number is unrealistic) for anything they would like to see or filter by — this is crucial for user functionality."* **Measured caps that silently bound the answer today:** `f-density` and `s-density` are `max="20"` MW/acre; `s-siyears` is `max="10"`; and the **six scoring weights are range sliders 0–5 with no numeric entry at all**, so a user can neither type a weight nor read one exactly. ⛔ **A cap is a silent filter** — the same defect class as G58's `.slice(0,14)`, which showed 14 of 432 qualifying rows and said nothing. **The rule: a numeric field carries no `max` unless the maximum is a fact about the world** (a percentage, a share of something); where a free number is meaningless — use case, direction, county, shading metric — give a dropdown that enumerates **every** real option rather than a curated few. ⚠ **Removing a cap changes what the filter admits, so measure the fan-out before and after and state it** — G29 step 3 is the precedent: exact distances made ~2.5× more parcels qualify, and that was the correction landing, not a regression |
| **G69** | **⭐ INDEX EVERY TABLE THIS APP USES OR DERIVES, AND WHAT IT IS FOR** | ✅ **v1 SHIPPED 2026-08-19 — `docs/TABLE_PURPOSE_INDEX.md`, 301 objects, 0 unclassified.** ⭐ **The finding: only 38 of 301 reach a control the user can operate** — 79 PAGE ONLY, 152 READ-ONLY, 20 no surface, 12 infrastructure. The wiring census says 286 of 300 “reach a surface” and both are true, because a surface counts a provenance line nobody can click. ⚠ **The 231 in between are now G72**, and this index is its worklist. ⛔ Its first run was WRONG and the correction is recorded in the generator: a cross-product join claimed `in_sites` — the 3.55M-parcel spine — was exposed by `L-terr`, the utility-territory checkbox, reading coverage as 82 instead of 38 | Operator: *"index and understand every single table that is either used or derived within this application, and how it fits into our objectives."* ⭐ **This is the DENOMINATOR for G65 and G67** — "every feature the tool offers" cannot be exposed until it is enumerated, and today it is not. **The three documents that look like they answer this each answer something else:** `docs/TABLE_INVENTORY.md` lists 304 objects with what they CARRY (owner/geo/key/date/status flags) but its "what it is" column is the source string, not a purpose; `docs/FEATURE_INVENTORY.md` maps features→tables in the OTHER direction, so a table reaching no feature is invisible in it; and the **wiring census answers a WEAKER question** — 282 of 300 reach *a surface*, where "a surface" counts a provenance line the user cannot filter, click or toggle. **Deliverable: one row per object** carrying (1) its subject in plain language, (2) the developer question it answers — the objective, (3) the CONTROL that exposes it today, and (4) where none exists, either the control to build or a written reason it should not be one. **Feed the result straight into G65/G67** so both become checkable rather than judged. ⚠ Generate it, do not hand-type it: a hand-typed inventory of 300 rows is correct exactly once (the whole reason the checkpoint's state block is generated) |
| **G57** | **A published rate of `0.0` violates “unpublished is NULL, never 0”** | ✅ **CLOSED 2026-08-19 — AND THE PROBE THAT KEPT RE-OPENING IT IS FIXED** | ⭐ **The row was right and the CHECK was wrong, which is why this kept coming back.** `audit_backlog_truth.py` reported *"2 BILLING-LEG zeros (the actual breach)"* while its own next sentence said *"a STATED zero is not an absent value"* — both cannot be true. Measured: **all 11 published zeros carry `value_status = 'published'`**, and the two it called a breach are I&M Tariff GS demand charges at **subtransmission (code 236)** and **transmission (code 239)** — the exact rows this row already verified against I&M's own book. The other nine are Duke FMCA, Duke EE opt-out, I&M RAR/TAX, NIPSCO FAC/DSMA/FMCA and SIGECO DSMA/SRR: factors that are currently zero. ⭐ **The rule is "unpublished is NULL, never 0", so the breach is a zero standing in for a rate we NEVER GOT** — the probe now tests exactly that (`rate = 0` AND status ≠ published) and returns **0 breaches of 11 zeros**. A permanently-failing check is how a real one gets ignored (rule 9, and the same shape as G76) `in_utility_tariff_riders` holds two I&M Tariff G.S. rows — *Demand charge >10 kW – transmission (code 239)* and *– subtransmission (code 236)* — with **`rate = 0.0` and `value_status = 'published'`**. Either I&M genuinely publishes a zero demand charge at those voltages, or the scrape recorded an absent value as a number, which is the exact failure the project rule exists to prevent (it once produced 95 false “below floor” violations). **Impact today is contained**: G.S. carries a 1,000 kW class ceiling, so a large load is correctly excluded from it and the $0 column is shown only so the exclusion is checkable. **To close:** read those two sheets in the source book and either set them NULL with `value_status='not_held'`, or record the publisher's own zero and say so. Do NOT guess |
| **G58** | **A silent cap on the cost-proxy card was hiding 418 of 432 rows** | ✅ **FIXED 2026-08-18** | The card sorted cheapest-first and then did `.slice(0, 14)` with no statement. Measured at the industrial sector: **432 rows across 35 utilities qualified and 14 rendered.** ⭐ Worse than the volume was the DIRECTION — sorting by cost and keeping the cheapest means the cut selects for whichever rows have the **least data**, so a gap in our extraction presented itself as the best deal in Indiana. Same failure as the “cheapest voltage” bug on the rate engine one card over. **Now one row per UTILITY** (ranking utilities is what the card is for), cheapest qualifying rate named, the rest counted, **all 35 shown**, and the 18 rates excluded for carrying no demand charge are **counted on the page with the reason**. Rule restated: *no silent caps — if a surface bounds coverage, it says what it dropped and why* |
| **G61** | **⭐ PJM capacity parity with Orennia — derivation SOLVED, binding-facility selection OPEN** | ✅ **THE DERIVATION IS SETTLED, AND THE VENDOR'S OWN CUTOFF RULE WAS FOUND 2026-08-19** | ⭐ **Their capacity export carries a `Shift Factor Cutoff Ratio` column we had never read, and it is NOT one number.** On the 297 Indiana tier-0 PJM withdrawal rows it takes exactly two values, perfectly correlated with the overload flag: **0.05 where `Existing Overload Flag = false` (279 rows), 0.20 where it is true (18 rows)** — and those 18 are precisely the 6.1% the vendor reports at ZERO. A facility already over its rating only binds if the bus STRONGLY drives it. ⛔ **We applied a flat 0.05 and, worse, ordered by `facility_overloaded ASC`, which skipped an over-rating facility for the next healthy one — so we reported a positive number for EVERY bus in the state and never once said zero.** Both fixed (`mw_clean` had the same bug and had to move with the ORDER BY, or the row would NAME an overloaded binder and REPORT a healthy one). **Like-for-like on 282 shared buses: >0 went 100.0% → 95.7% against their 96.8%; median ratio 1.056; "they say 0 and we don't" went 18 → 0.** ⭐ **And the arithmetic was never the problem: where we pick the SAME binding facility the ratio is median 1.001, 68 of 91 inside 5%.** This is matching the METHOD (G22), taken from their file rather than from the distance to their answer. **What remains is G62's facility gap, not this row.** **The derivation is theirs and we now reproduce it.** Their figure is `(rating − base flow) / |shift factor|` on the binding facility — verified against their own published row for bus 243209: 2844 MVA, 2550 MW, −0.0745 → **3,946 against their 3,943.59**. We do not ship the rating but can recover it: `rating = impact_mw / ((post − pre)/100)`. **Measured on the 283 shared buses:** `MIN(available_mw)` — what we shipped this morning — agreed **exactly 0** times at a median ratio of **0.136** (seven times low); the derived form agrees within 20% on **84** at a median ratio of **1.010**. Centred instead of biased, which is the difference between a number a developer can site around and one that silently rules out good buses. PJM withdrawal median 24.0 → **157.4 MW**. ⚠ **The open half is COVERAGE, not ranking.** Of their 298 binding facilities: **146 are absent from our harvest entirely**, 134 are present and pass our filters (real ranking work), 18 are excluded by our `|dfax| ≥ 0.05` cutoff. We monitor **89.5 facilities per bus** and they report **9.4** — so it is a DIFFERENT monitored set, not a subset. Operator's read: the gap is the un-scraped scenarios, which the running ladder tests. ⛔ A 5,000 MW probe changed **nothing** on matched rows (4,673 of 4,673 identical), so if the ladder closes this it will be by surfacing facilities a 100 MW request never binds |
| **G62** | **⭐ PJM bus placement — our own method, exact where the gazetteer allows** | 🔴 **CEILING CONFIRMED 2026-08-19 — measured from a SECOND, independent direction, and it cannot be lifted from data we hold** | ⭐ **G62 concluded from PJM bus placement that "the ceiling is the gazetteer, not the matcher". G15 hit the same wall from completely different text — IURC TDSIC workpapers — which is strong evidence the diagnosis is right.** The stations that fail are not obscure: **CHICAGO AVE, SOUTH VALPARAISO, MICHIGAN CITY, MILLER, ROCK RUN, STILLWELL, EAST WINAMAC, BURNS DITCH, TRAIL CREEK, MARKTOWN, KOSCIUSKO** — all real northwest-Indiana NIPSCO stations. ⛔ **AND THE GAZETTEER CANNOT BE EXTENDED FROM WHAT WE HOLD, now checked directly rather than assumed:** `in_substations` (3,858) is already the COMPLETE Indiana cut of `energy.mat_grid_substations` (3,858 IN rows — they match exactly, so nothing was lost in clipping), and **neither parent source carries the names either** — `osm_power_substations` has 2,873 Indiana rows and `nat_substations_hifld` 2,077, and NOT ONE of the twelve appears in either by exact match. ⭐ **So this is an ACQUISITION, not a matcher bug, and it blocks two backlog items at once.** What the matcher CAN still win is suffix handling — HIFLD holds "SCHAHFER STATION" where the workpaper says "SCHAHFER" — and that ladder is now built and shipped in `build_grid_plans_located.py`, worth 11 of 119 matches. Candidate acquisitions: the utilities' own system maps, IURC filings that name a station and a location together, or EIA-860 plant coordinates as a partial cross-reference Operator: *“we can generally place them using the same (or similar) estimation method that Orennia uses (since we use the same datasets).”* Right, and measured against their coordinates as a **yardstick only** — the method and the data are ours. **Bus label → substation name**, stripping the AEP area prefix (`05EUGENE` → `EUGENE`) and the trailing bus index (`05DELAWR1` → `DELAWR`), reproduces their placement at a **median 0.03 mi**. Topology from the branch graph — every `transmission_facility` string names both endpoint buses — adds a hop-1 tier at ~**5.3 mi**; hop-2 is 6.8 mi and **hop-3 is junk at 33 mi and is not used**. Coverage on the Indiana set: **82 → 111 of 283**. ⛔ **The ceiling is the gazetteer, not the matcher.** `in_substations` does not contain MODOC, FOWLER, STUDEBAKER, BOUNDARY or ADAMS, while DELAWARE and SOUTH BEND are present and match cleanly. **Extending the gazetteer is the unlock.** ⚠ Denominator: our harvest is owner=AEP, spanning Ohio/WV/VA/KY/Michigan, while `in_substations` is Indiana-only — so **283, not 1,826**, is the number that matters |
| **G63** | **The bus rebuild, and the THREE separate paths that each had to be repointed** | ✅ **DONE 2026-08-18** | `in_bus_capacity_tier0` rebuilt: **2,117 rows → 7,102 / 3,557 buses**, both directions for both ISOs. PJM 2027 RTEP (1,475) → **case 23, 1,826, ours**; MISO DPP-2021 injection-only (603) → **DPP-2025 both ways, 1,731, licensed**. ⭐ **The win is a DIRECTION:** MISO publishes no load-side headroom anywhere, so across the two thirds of Indiana inside MISO the app could only ever answer the generator question. **1,079 buses now carry non-zero load headroom.** ⛔ **Three surfaces read the buses by three different paths and each had to be found separately:** `gridsiting.json.gz` (dossier, screener) never read tier0 at all; `grid.geojson.gz` (the MAP) still had DPP-2021 after the other two were fixed — **the operator caught that one**; and grid.html reads its own queries. **One binding rule across both ISOs**, verified against the vendor: the tightest facility NOT already over its rating. Applying it moved MISO withdrawal from a median of 0.0 to 42.2 MW and PJM from 0.0 to 42.0 — two independently derived medians landing within 0.2 MW is the check that it is the same rule. The **worst/median/best triple is gone** from every surface: three rival numbers let a reader pick the flattering one |
| **G64** | **`export_grid_sentiment.py` silently empties the Community court-activity table** | ✅ **FIXED 2026-08-18 — and it now self-heals** | It rewrites `county_context.json` and drops the IOCS block that `export_signoff_payloads.py` merges on afterwards. **This has now bitten twice**, and both times the symptom was a payload that is present but missing one key — which looks fine until someone opens that tab. The checkpoint's payload-key guard caught it within minutes. ⭐ **Remembering an ordering rule is not a control.** The exporter now CHECKS for the IOCS block after it writes, re-runs the enricher itself if it is missing, and asserts. Verified: it detected the drop and restored 92/92 counties without intervention |
| **G60** | **⭐ DOSSIER AUDIT — 10 findings; 9 CLOSED, 1 half, 1 open** | ✅ **FIXED AND DEPLOY-VERIFIED 2026-08-18** — `docs/DOSSIER_AUDIT_2026-08-18.md` | **D-1: the dossier now carries a real rate.** It loads `tariffs.json.gz`, resolves the parcel's utility and prices that utility's own book — Vanderburgh → SIGECO **8.03¢** HLF, Marion → AES **10.51¢** HL, Morgan → South Central Indiana REMC **6.61¢** LI, LaPorte → NIPSCO **11.10¢** 631 — all measured on REAL parcels against the DEPLOYED build. The costing engine moved to `common.js` so the Market page and the dossier share ONE copy, and `scripts/utility_names.py` enumerates the territory→tariff map because the two vocabularies match **zero** times out of 145. ⭐ **D-11, added by the operator mid-pass, was the most consequential:** resolve the utility from the parcel FOOTPRINT, never a point. `p.lat/p.lon` sits 2–332 m from the centroid — survivable while the dossier only NAMED a utility, not now that the name selects a tariff. `territoryForParcel()` tests up to 64 ring vertices and reports a **straddle** instead of picking a side, and on **2 of 4 counties the point resolved NOTHING** while the footprint resolved Hendricks County REMC and SIGECO. A bbox pre-filter took it from 435ms to 194ms worst case. The Figure 2 no-centroid claim is now precise and names the one remaining point use (bus distance — the buses ARE points). **Also closed:** D-3 the hardcoded $210k example is replaced by this utility's own spread; D-4 the source list names what the document actually reads; D-5 G51's third state; D-6 restated on 1,826 buses; D-7 polygon holes; D-8 the checklist admits it is not derived; D-9 the 6-character prefix match is gone. 🟡 **D-2 half closed** — the withdrawal figure now DISCLOSES that its study is superseded; rebuilding tier0 on case 23 is blocked on the **G26 headroom method** and belongs to the bus work. 🔴 **D-10 open** — acceptance still records the tariff as impossible. ⚠ **Two bugs were introduced during the fix and caught by RENDERING the document rather than reading it:** `esc is not defined` would have made the Dossier button do nothing and **a parse check passed it**; and one spread printed as both $2M and $2.3M. |
| **G59** | **The PJM case-23 re-harvest produced BYTE-IDENTICAL data — the “refresh” claim is wrong** | ✅ **DONE 2026-08-19 — 1,118,058 duplicate rows retired** (`in_pjm_qs_tc2phii_wd` 462,654 + `_inj` 655,404) with their `_registry` rows. Verified identical **and RE-VERIFIED immediately before the DROP**, because a check that ran minutes ago is a claim about the past and this is the one action that cannot be undone: 0 rows present in either superseded table and absent from its replacement, on every value-bearing column. Nothing in the repo read them. ⚠ **Still open from this row: add the powerflow filename to the harvest schema**, or the next PJM refresh is invisible again without diffing values | Both directions landed at **1,826 of 1,826 buses** (`in_pjm_qs_c23sens_wd` 462,654 rows, `_inj` 655,404) and both registered. ⛔ **But they are duplicates of the tables they were supposed to supersede.** Verified a genuine fresh pull (`_pulled_at` 14:14–19:10 on 08-18 against 19:25 on 08-17) with **identical key sets** (0 only-in-new, 0 only-in-old) and **0 of 410,947 withdrawal keys and 0 of 553,276 injection keys changed** in `available_mw`, `dfax` or `post_loading_pct`. **So PJM did NOT refresh the powerflow file inside case 23**, and the “1,094 changed rows” probe that justified a multi-hour re-harvest was measuring something else — most likely the VENDOR's two exports, whose differing powerflow filenames are a fact about their files, not about QueueScope. Operator's read, and it is the right one: harvesting into a new table name was the safe call, so what we have is a duplicate PAIR rather than a study and its successor. **To close:** retire one pair (~1.1M duplicate rows), correct the “superseded study” wording in `HANDOFF_2026-08-18.md` §2 and `RESUME_HARVESTS.md`, and **add the powerflow filename to the harvest schema** so the next refresh is visible without diffing values |
| **G55** | **Tariff coverage: books for 22, URDB for the other 50** | 🟡 **RESTATED PRECISELY 2026-08-19 — still procurement, and the number is 22 of 73** | **Re-measured rather than re-litigated:** `in_utility_tariff_riders` holds **73 utilities**, of which **22 carry a priced component from their own book** (20 with a demand leg, 22 with an energy leg) across 668 components — rider 180, eligibility 159, energy 132, demand 102, base_charge 63, ratchet 22, fuel_base 10. ⛔ **Nothing engineering-side is blocking the other 51.** Their books are not published anywhere we can reach; URDB is already wired as a **labelled floor** for the 49 that have one, and **2 stubs have neither a book nor a URDB row** — genuinely nothing held. ⭐ **This row should not be worked again until a book arrives.** The actionable form of it is a procurement list: name the 15 costable municipals, name the 2 with nothing, and send one request each. Filing that list is the next step, not more code ⭐ **The framing in this row was wrong and the measurement corrected it.** It is not “19 municipals”: `in_utility_tariff_riders` holds **73 utilities**, of which only **20 carry both a demand and an energy leg**. **FIFTY-ONE are single-row stubs** recording that nothing was captured — and **49 of those 51 have rates in `in_urdb_rates`** (969 rows / 70 utilities), which the app was not reading. A siter looking up Winamac found nothing while the warehouse held its rates. **DONE:** six municipals now have per-utility adapters (Auburn, Southeastern REMC, Anderson, Logansport, Lebanon, plus I&M's slash) and **the costing audit is CLEAN across all 73 utilities bar one** (G57). **URDB is now wired as a labelled FALLBACK** for every utility whose book gives us nothing to cost: the Market selector went **22 → 72 utilities**, 50 of them marked *“no tariff book (URDB only)”*. ⛔ It is carried in its own payload field and rendered in its own panel — **never merged into `schedules`** — because URDB is FLATTENED (no riders, no fixed charges, no seasonal blocks, no service-class split) and the rider stack alone is worth 1.5–2¢/kWh where we hold it. Every URDB figure renders with a **≥** prefix as a floor, and a rate with no demand charge captured is called unusable rather than cheap. **Proven additive: the fingerprint shows 0 utilities moved.** ⚠ **STILL OPEN:** the 15 costable municipals beyond the six have no adapter yet; **2 stubs have no URDB either** (genuinely nothing held); and a URDB floor is not a bill — closing this properly still means acquiring the remaining books. Richmond TS (~7.6¢/kWh at 90% LF) is the best published firm municipal path and `docs/TARIFF_HARVEST_MUNIS_COOPS.md` still reaches no surface. ⚠ **Operational dependency: if the IURC loader re-runs, `load_tariff_books_munis_coops.py` MUST re-run after it** | | ⚠ **MEASURED 2026-08-19 and the 'unaudited municipals' half of this row is STALE:** **21 of the 22 utilities that carry a priced demand or energy component already have an adapter** — only Paulding-Putnam does not. What genuinely remains is the **50 utilities on a URDB floor**, and those need BOOKS THAT ARE NOT PUBLISHED (utility offices, paper ordinances, a phone call to Tell City). That half is an acquisition, not engineering, and no amount of scraping reaches it. 
| **G56** | **Rewrite the tariff parser as PER-UTILITY adapters, not one generic matcher** | ✅ **DONE — and as of 2026-08-18 (second session) PROVEN rather than asserted** | `scripts/tariff_adapters.py` holds one declarative adapter per IOU; the arithmetic stays shared in `export_tariffs.py`. Each may declare `applies_to_norm`, `class_separators`, `split_classes`/`merge_classes`, `explicit_classes`, `multi_class_phrases`, `parent`, `large_load`/`not_large_load`, `closed_classes`, `by_contract`, `llf_schedules`, `conditional_applies_to`, and a prose `notes` that ships in the payload so the publisher's conventions reach the page instead of living in a commit message. ⭐ **The operator's follow-up was the right challenge — declaring independence is not proving it.** `scripts/tariff_fingerprint.py` hashes, per utility, every field that can change a bill (billing leg, rate, months, class binding, block ladder, conditional/reactive status) and names exactly which publishers moved. **Demonstrated, not assumed: perturbing ONLY Duke's `closed_classes` moved exactly 1 of 73 utilities, and reverting restored the baseline byte-for-byte.** Deliberately blind to prose, so rewording a `notes` string does not read as a costing change. `data/tariff_fingerprints.json` is the baseline; `--update` accepts a new one. ⚠ Run `export_tariffs.py` first or you fingerprint a stale build. ⚠ **The 19 municipals still have NO adapter** and fall back to the generic path (G55) |
| **⚠ IN FLIGHT** | **What is running or half-built RIGHT NOW — read before starting anything** | 🟡 **live 2026-08-19 ~13:00** | ⭐ **A 26-RUNG PJM LADDER IS RUNNING and one command resumes, continues AND repairs it — safe even while one is going, because it polls for the ABSENCE of a QueueScope process:** `powershell -ExecutionPolicy Bypass -File scripts
un_pjm_ladder.ps1`. **COMPLETE at 1,826/1,826 buses:** 5,000 · 10 · 15 · 100 MW, BOTH directions (8 rungs). **RUNNING: withdrawal 25 MW.** Queued: 25 inj, then 50 · 200 · 300 · 500 · 1000 · 1500 · 2000 · 3000 both ways. ⛔ **NEVER start a second QueueScope process. NEVER delete `data/` — archive markers, never delete. Owner is 1568, not 739** (739 loads 0 rows and exits successfully). ⚠ **A `SHORT` line in the log is NOT data loss** — check `audit_pjm_short_reads.py`, never the log; stopping a healthy harvest over one is what created the batch-30 gap. ⭐ **THE LADDER'S VALUE IS NOW PROVEN AND BOUNDED, measured 2026-08-19:** injection 5,000 vs 100 MW adds **443 new constraint KEYS** and **0 new facilities** (1,300 either way); withdrawal adds nothing at all. So it DOES surface constraints — and `in_bus_capacity_tier0` now sources the 5,000 MW rung because of it, changing **986 of 1,814 injection buses, 466 of them tighter**. ⛔ But because the FACILITY set never grows, **the ladder cannot close the vendor-parity gap** (117 of 282 vendor binding facilities are absent from our harvest at any request size). 
un_pjm_ladder.ps1`**. **Complete at 1,826 of 1,826 buses:** 5,000 MW both directions, 10 MW both directions, and 100 MW (held as `in_pjm_qs_c23sens_*`, auto-skipped). **Running:** 15 MW withdrawal at 28 of 74 batches. **Queued:** 15 inj, then 25/50/200/300/500/1000/1500/2000/3000 both ways. ⛔ Owner **1568**, not 739 — 739 loads 0 rows and exits successfully. ⛔ Never a second QueueScope process; ⛔ never delete `data/` (archive markers). ⭐ **WHAT THE LADDER HAS PROVEN, measured:** withdrawal 5,000 vs 100 MW added **0** keys and **0** facilities; injection 5,000 vs 10 MW added **443 keys and 0 facilities**. A bigger request IS a strict superset of constraint keys, but the monitored FACILITY set is a property of the study case, not the request size — so **the ladder cannot close the 146-missing-binder gap** and PJM parity needs a different attack. ⚠ Both tables carry ~102k duplicate rows (655,790 rows / 553,719 keys), structural not a defect — **always dedupe on (bus, facility, contingency) before comparing**. ⚠ A `SHORT` line in the harvest log is NOT data loss — check `scripts/audit_pjm_short_reads.py`, never the log; stopping a healthy harvest over one is what created the batch-30 gap that had to be repaired today. ⚠ Killing the supervisor kills its python child seconds later even though an immediate check says otherwise. **(2)** G70–G76 are the operator's 2026-08-19 list; **G72 is the biggest** — `docs/TABLE_PURPOSE_INDEX.md` shows only **38 of 301** objects reach a control a user can operate. **(3)** G15/G53/G55 remain, and G55's books are not published anywhere. 
| **G54** | **Cost each tariff to an annual spend and an effective ¢/kWh** | ✅ **RIGHT FOR THE 5 IOUs — 2026-08-18, second session** | Validated against each utility's EIA-861 **industrial** realized average (operator's call — industrial, not C&I, because the customer is large load). ⭐ **THE BIG ONE, and it invalidated every rider figure on the page: NO RIDER WAS EVER COUNTED.** `service_class()` returns the raw `applies_to` as the class KEY, so an attached rider carried `volt='hl (all voltages), pl'` / `'rate hlf'` / `'rate 632'` — strings that can never equal a service class — and the renderer's per-class filter `c.volt === volt` dropped **every rider at every service voltage**. The Riders column read **$0 on 17 of the 18 IOU schedules** while `n_riders_attached` correctly reported 8–13 riders attached. That is exactly why it survived a whole session: **the riders were selected, their money was never counted**, and I&M's eight riders were declared fixed on the strength of that count. Root cause was **ORDERING, not matching** — the class-normalisation loop runs over a schedule's own components BEFORE the riders are appended, so riders never passed through it. **EIGHT more defects found and fixed after it:** (1) a **comma between digits** — Duke's `transmission (138,000/230,000/345,000 V)` — read as a class-list separator, which merged its two ALTERNATIVE transmission rates and **summed** them (+92%); (2) `volt_base(name)` inferring a service class from a **charge name**, so NIPSCO's *“Transmission charge”* took the transmission family, collided with an unrelated row, and its **$0.014689/kWh — $32.8M/yr — was silently dropped**; (3) NIPSCO's **optional services billed as firm** — maintenance service at **$0.62/kW-DAY**, unavailable June–September and capped at 60 days a year, charged 365 days: **+$106.22M/yr on Rate 632 alone**; (4) `MAX_RE` not knowing the words **“ceiling” or “exceeds”**, so I&M's **Tariff G.S. — a 1,000 kW schedule — was priced at 300 MW, 300× over its own limit** (measured: adding those two words reclassifies **exactly 1 of the 31** eligibility rows in the estate); (5) the renderer keeping a **second, drifted copy** of that same vocabulary; (6) **inherited schedule legs counted as riders**, which is why I&M IP-LL showed **ENERGY $0** on all four classes with $153.89M sitting in its Riders column; (7) Duke's **connection charge** spanning all transmission tiers becoming a **phantom third class** holding nothing but $855.37/month — demand $0, energy $0, and a **0.77¢ headline** that led the schedule's range; (8) Duke's **grandfathered** `secondary (closed class)` priced as an available option. ⭐ **The leg guard was refined rather than kept blunt:** it could not tell a **missing** leg from an **absent** one, and was refusing AES **PH** for a $0 demand column that is simply the truth — Process Heating publishes two energy blocks, a customer charge and **no demand charge anywhere in the book**. The exporter now states `has_demand_leg` / `has_energy_leg` per schedule, so the guard still fires on NIPSCO **633** (which does publish $24.72/kW-month) and comes off schedules that never had one. **Result at 300 MW / 85% LF: every priced row at all five IOUs now lands inside a reasoned band except three, and all three are explained** — AES **PH** +56% (energy-only, special-purpose), NIPSCO **631** +82% (a genuinely expensive new large-load tariff against a legacy-steel average), I&M **IP-LL at secondary** +38% (300 MW at <4 kV is not a real option). Duke **HLF went +92% → −4%/+5%**; SIGECO **LP −36% → −5%**; I&M **IP −34% → −14%**. Four schedules are now correctly withheld as **ineligible** (NIPSCO 624/632/633 on 25 MW ceilings, I&M GS on 1,000 kW) and two as **inapplicable** (Duke LLF/LLF-B are LOW-load-factor services; AES CSC is negotiated, not published) |
| **G53** | **A WITHDRAWN interconnection request is a seller-intent signal** | 🔴 **OPEN — operator, 2026-08-18** | Operator: *"the owner was interested in placing an energy development there regardless, and we could then step in with either a DC or BESS development in its place… filterable by date of withdrawn application."* ⭐ **This is a genuinely new signal class and it is better than most of what D1-D27 gives us.** Every existing seller-intent signal infers willingness from DISTRESS — unpaid taxes, a foreclosure, a condemnation order. This one infers it from **revealed preference**: a landowner who signed an interconnection agreement already consented to host energy infrastructure, already accepted the land use, and now has a site with a **studied grid position and no project on it**. That is a warm lead, not a distressed one. **The pool is large** — G35 measured **49 of 87 counties with ≥50% of everything ever queued withdrawn**. ⚠ **TWO DEPENDENCIES, both operator-stated, and the item is worthless without them:** **(1) LOCATION.** Queue rows are keyed to a POI or a bus, not a parcel. The operator's lead: *"the last one or two [filings] before interconnection provide an address"* — so the address lives in the LATE-STAGE filings, not the queue summary. **Test that on a handful of known withdrawn projects before building anything**, because if the address is not recoverable at scale the signal cannot reach a parcel and becomes county-grain context like D17. **(2) SIZE MATCH.** A withdrawn 5 MW solar project does not imply land for a 300 MW campus; the queue MW is the proxy for how much land and interconnection was contemplated, so match it against the user's MW. **Also required:** a **withdrawal-date filter** — recency governs, since a 2015 withdrawal says little about today's owner, and this is an `event` signal not a `state` one under the G9 vocabulary. ⚠ **Watch the double-count with G48**: queued MW is already shown as county shading, and a withdrawal is simultaneously a *negative* for that county's queue health and a *positive* for that parcel's availability. Do not let one number carry both meanings |
| **G52** | **The map needs a legend for what is currently displayed** | ✅ **DONE 2026-08-19.** A collapsible key, bottom-left, that never covers the evidence panel. ⭐ **DERIVED, never written down twice**: the label comes from the checkbox's own `<label>` and the colour is read back off the map with `getPaintProperty()`. Nothing needs touching when a layer is added — put the box in `ALL_LAYER_BOXES` and it appears. A layer painted by an EXPRESSION (lines banded by kV, DC pins tiered by precision) says **“banded”** rather than showing one of its colours and implying the rest; a lazy layer not yet on the map says “loading” rather than inventing one. Verified: 5 rows by default, 7 after ticking two split layers | Operator: *"place a legend or key in the corner of the map for what the map is currently displaying (e.g. the environment section should say what the coloring distinction is between the different layers, and so on for the rest of the map presets)."* **Today the only legend is `#metric-legend`, which explains the COUNTY SHADING and nothing else** — every drawn layer's colour is undocumented on screen. A reader looking at the Environmental preset sees protected land, nonattainment areas, tax-credit areas, watersheds and water stress rendered in different colours **with no key at all**, and has to guess which is which. ⭐ **It should be driven by `ALL_LAYER_BOXES`, not hand-written**, so it lists exactly the layers that are currently ON and cannot drift from what is drawn — G34 made that registry the single source of truth for layer state, and a hand-maintained legend would be a second copy that WILL drift. **Each entry needs the swatch, the plain name (G8: no internal codenames), and for a banded layer the band meaning** — voltage class colouring and the transmission-line palette are the clearest cases where a colour carries information the reader currently cannot decode. ⚠ **Corner placement over a map is a real constraint**: it must collapse, it must not cover the evidence panel, and it must stay legible at mobile width. **Consider making it the natural home for the preset's one-line framing too**, so the reader is told what question the current view answers |
| **G51** | **“Cannot assess” conflates two different states — split them** | 🟡 **SWEPT WHERE COVERAGE JUSTIFIES IT 2026-08-19 — and the sweep must stay evidence-driven** | ⭐ Measured: **179 call sites, 14 with a third state.** The obvious move is to sweep the other 165 and it would be **WRONG** — this row already ruled that the narrow sweep was deliberate, because inventing "none" where we never looked is the unpublished-rate-as-zero defect. A third state was added only where a COVERAGE ARGUMENT exists: the three WRI Aqueduct labels, largest waterbody (the NHD clip is 100% complete), and the four G72 gates. ⛔ **Everything else keeps "not measured here" on purpose.** Operator: *"instead of saying 'cannot access', we can say something that is more truthful to the user (e.g. 'none here')…"* ⭐ **Agreed, but it is a SPLIT, not a rename**, and the distinction is load-bearing: **(1) we measured and found nothing** — *none within 25 miles* — which is a real finding and useful; **(2) we could not measure it** — no source, no coverage, or the join did not resolve. ⛔ **Collapsing them is a documented defect in both directions.** Printing *none* for something never measured invents a negative finding the reader cannot detect — the same shape as treating an unpublished rate as zero, which produced **95 false ‘below floor’ violations**. Printing *cannot assess* where we DID look and found nothing understates what we know, which is what prompted this. **Shipped:** `row(k, v, absent)` in `app.js` now takes a third argument; the default stays the honest **“not measured here”** (the G8 wording), so **a caller that says nothing is never taken to be asserting emptiness** — the safe direction. Two literal *cannot assess* renderings in the score table moved to the same wording. All three states verified in a browser. ⚠ **REMAINING, and it is the real work:** sweep the callers and pass `absent` wherever the field was genuinely measured — water, queue, grid distance and bonus geographies are the obvious candidates, since each has a known search radius or a complete county set. **Only a caller knows which state it is in**, which is why this cannot be fixed centrally. ⚠ One string deliberately untouched: *“No county-tagged receipts held — cannot assess, not 'quiet'”* already names the gap AND warns against the wrong reading |
| **G50** | **Surfaces say “cannot assess” for data we now HOLD — dossier, popups, market, buses** | ✅ **DONE — verified by probe 2026-08-19.** `scripts/audit_backlog_truth.py` measures the dossier printing *“not resolved at this point”* **0 times**, and the MISO vendor source IS named in prose on the surfaces (5 mentions of the licensed subscription in `app.js`, plus `grid.html`). Both halves this row listed as open are closed. ⚠ The row's own wording was stale for a day before anyone measured it — which is why that audit now exists — the **tariff** half is on the Market page: 419 components across 18 rate schedules and 128 rider attachments, from five IOUs plus the municipals, each schedule carrying the riders that bill on it. Costing them to an annual figure is split out as **G54**. **STILL OPEN:** (a) the **MISO bus** half — `in_bus_headroom_miso_vendor` holds 17,146 rows / 1,731 buses with coordinates on every row and reaches no surface yet; (b) the **PJM bus** half, waiting on the case-23 re-harvest; (c) the **dossier's joined rows** — `app.js` renders *“not resolved at this point”* for the serving utility, generation provider and transmission owner, which is where the operator's complaint started and which the tariff data can now partly answer | Operator: *"We genuinely contain much of the information in the dossier (and the popup) that is listed as 'cannot access', and we should have all of this updated… including the market updates (tariffs and a complete table breakdown for all of the tariff schedules/riders) and the updated bus information from MISO (Orennia) and PJM (ours)."* ⭐ **This is the defect class this project keeps hitting** — `SI_COVERAGE.md` asserted D4 was absent while 17,617 rows sat in the warehouse; a payload shipped 24,275 after the warehouse moved to 24,277. **A surface claiming we cannot see something we hold is worse than a gap: it is a false negative the reader cannot detect.** ⚠ **Measured, so the search is scoped:** the PARCEL payload is not the culprit — across 102,214 parcels in 12 county files **no property is null on every parcel**, and the >60% nulls (`structure_sqft`, `build_id`, `lat`/`lon`, `occ_cls`) are legitimately sparse because a parcel with no building has no building fields. The culprits are the **joined** sections: `app.js:1484/1490/1495` render *“not resolved at this point”* for `T.utility`, the generation provider and the transmission owner, and the generic row renderer at `app.js:1227` turns any null into *cannot assess*. **Three bodies of data landed TODAY that these surfaces predate:** (1) **187 tariff component rows** in `in_utility_tariff_riders` (AES 62, NIPSCO 78, SIGECO 44) plus the I&M book transcribed and verified, including **NIPSCO Rate 631**, the purpose-built ≥10 MW large-load schedule a data centre would actually take service under; (2) **`in_bus_headroom_miso_vendor`**, 17,146 rows / 1,731 buses, **every row carrying coordinates**; (3) the PJM case-23 re-harvest, in flight. **Do it in that order** — tariffs are complete and static, MISO is complete, PJM lands when the harvest does. ⛔ **Two rules bind the fix:** the vendor MISO rows must render with their source named ON THE SURFACE (`provenance_class='vendor_licensed_proxy'`, licence lapses late 2027), and a genuine gap must still read *cannot assess* — **the goal is to stop lying about what we hold, not to fill every cell** |
| **G49** | **Popups still say “What this means.”** | ✅ **DONE 2026-08-18** — operator: *"the site popups still say 'what this means' throughout, and this should be fixed."* Same family as G44, which swept *So what:* and *Why this matters:* but **not this third phrasing** — a reminder that a label sweep is only as complete as its list of labels. **3 removed** from the grid, queue and water popups (`app.js`), all three already inside a `.sowhat` block so the machine-readable marker was untouched. No capitalisation fix was needed here: unlike the G44 blocks, each of these opens with a template branch whose text already starts capitalised — **checked rather than assumed, because that is exactly what bit G44.** ⚠ **Deliberately KEPT:** `<th>What it means</th>` in the dossier Figure-1 table, the milestone table and the Market reliability table. **A column header naming what a column contains is not a rubric label** — removing it would leave an unlabelled column, which is worse |
| **G48** | **County posture: an existing data centre is a POSITIVE signal, and should read green** | ✅ **DONE 2026-08-19.** **23 counties host a data centre: 8 promoted to green, 14 to amber, 1 left alone because a restriction outranks it.** ⭐ The operator's binding precedence holds **by construction, not by a special case** — the data centre is appended as one more action and the existing worst-tone-wins fold decides. Threshold measured, not chosen: `opposition_intensity == 0` on 61 of 92 counties, the modal value. ⛔ The county comes from a SPATIAL join (`in_dc_county_counts`, 244 of 244 located DCs placed) — a name match here is the CLOUDSCENE_GAP defect. ⚠ That join lives in a BUILD script because the checkpoint caught the first version doing it inside the exporter: *no EXPORT reads energy directly* | ⭐ **The threshold question is answered, so the next attempt does not have to guess.** “Nothing else at play” needed a defensible definition; measured across the 92 counties, `opposition_intensity` is **0 on 61 of them (66%)**, then 1 on 7, 2 on 4, 3 on 4, 4 on 6, 6 on 7, and a single outlier at 25. **So `opposition_intensity == 0` is the natural cut** — it is the modal value, it means literally no recorded objection activity, and a reader can verify it rather than take a percentile on trust. **The data is located too:** existing data centres are `in_data_centers_all` (244 rows, exported by `scripts/export_facilities.py`), and the shading comes from `action_summary.tone` built in `scripts/export_grid_sentiment.py` from `in_dc_actions_resolved` — **which reads county ACTIONS only, so an operating data centre never reaches the shading at all.** That is why a county with a live DC and no recorded approval currently shades GREY. **Remaining work:** promote to green in `export_grid_sentiment.py` (not client-side) when a county has an existing DC or an approval, has no ban/moratorium, and has `opposition_intensity == 0`; keep the operator's binding precedence that **a restriction outranks a development**; and state the rule in the legend so a green is reproducible | Operator: *"a county that HAS a data center is a positive signal (so long as they don't have a ban/moratorium in place), and should be potentially signaled as green on the map if there is nothing else at play happening within the county (such as opposition, etc.)."* **What the shading does today** (`app.js`, `countyPaint` → `action_tone`): `blocking` red = ban or live moratorium, `watch` amber = proposed / denied / uncodified, `open` green = **has APPROVED one**, grey = nothing recorded. ⚠ **Green today means an APPROVAL EXISTS IN THE RECEIPTS, which is not the same thing as a data centre existing.** We hold 244 existing data centres as their own map layer (`L-dc`), and that layer does not feed the shading at all — so a county with an operating data centre and no recorded approval currently shades GREY, i.e. “cannot assess”, which understates it. **The change:** let an existing DC promote a county to green when nothing else is at play. ⛔ **Preserve the precedence rule, which is operator-set and still binding: A RESTRICTION OUTRANKS A DEVELOPMENT.** 10 counties have approved a data centre and **6 are still amber or red**; Lake County holds an approval AND a moratorium. An operating data centre must NOT turn a banned county green. ⚠ **“Nothing else at play” needs a measured threshold, not a vibe** — opposition intensity is an unbounded relative count (G21), so “low opposition” has to be defined against something, and the honest options are a percentile of the observed distribution or an explicit band. **Decide it once and state it in the legend**, because a green that a reader cannot reproduce is worse than grey. **Order of precedence to implement:** ban/moratorium → red; proposed / denied / uncodified → amber; **existing DC or approval, with opposition below the threshold → green**; existing DC or approval WITH elevated opposition → amber; nothing held → grey, still labelled *cannot assess, not calm* |
| **G47** | **Remove the Corrections panel from Community — and apply the principle everywhere** | ✅ **DONE 2026-08-18** — panel removed from `community.html`, **and the data deliberately left in place**: `O.corrections` is still in the payload and still built upstream, only the reader-facing surface is gone, with a comment in the file recording where it lives and why it belongs behind the *How this was built* pattern rather than in the bin. ⚠ **The removal had a trap worth recording:** two script lines targeted the deleted nodes — `getElementById("t-cdafix").insertAdjacentHTML(...)` and `getElementById("cda-corrections").classList.add("hidden")`. Deleting the HTML alone would have thrown on null and **taken every later line of that script with it**, silently emptying the rest of the page — the same failure shape that once killed the map. Both renderers removed with the surface, and the page **verified in a browser afterwards**: verified-postures 53 rows, county posture 93, receipts 301, IOCS 93, zero empty tables. ⚠ **Sibling sweep still open** — the audience test generalises and other build-log panels on reader-facing pages have not been swept | Operator: *"We can remove the corrections section on the community and local rules page, since the user simply doesn't need to know this information (as similar throughout — if it isn't relevant to the user, we shouldn't publish it)."* **The specific target:** `community.html` line 55, `<div class="card" id="cda-corrections">` — *“⚠ Corrections — leads that were NOT Indiana”*, fed by `O.corrections` and already self-hiding when empty (`app.js`-side: `if (!FIX.length) … classList.add('hidden')`). It is a record of OUR filtering having worked. A developer does not open the Community page to learn which leads we correctly discarded. ⭐ **The principle is the bigger half of this item and generalises the G33 audience test:** a surface that exists to prove WE were careful serves us, not the reader. Keep the material — it is exactly what catches a bad claim — but move it behind the *How this was built and checked* accordion pattern that `si.html` now uses, or onto the **Data & sources** page, which is already marked internal-by-design and exempt from G21. ⛔ **Delete the SURFACE, never the DATA:** `SI_COVERAGE.md` once asserted a signal was absent while 17,617 rows of it sat in the warehouse, and provenance material like this is what catches that class of error. **Sweep for siblings while in there** — the same audience test applies to any remaining build-log panel on a reader-facing page |
| **G46** | **Define a bus placement methodology we actually OWN** | 🔴 **OPEN — operator, 2026-08-18** | Operator: *"these are NOT our coordinates, and we will have to determine and define a placement methodology (at some point in time, maybe not today, since our Orennia subscription may lapse in the next year)."* ⛔ **Corrects my framing:** I had called MISO placement solved. It is **borrowed**. Our 9,608 placed MISO buses come from `in_miso_poi_identity` — **MISO's own published POI data** — and the 0.0 mi median agreement with the vendor across 570 buses proves **shared provenance, not competence**: their file shows 571 MISO buses sourced `ISO`, so we are both copying the same upstream. **A dependency, not a capability** — if MISO retires that layer we are where PJM is, and the failure would be SILENT because the table would stop refreshing rather than error. ⭐ **PJM proves nobody has a coordinate feed:** 0 of 298 PJM buses carry `Location Source='ISO'` and the vendor estimates **91.9%** of them, so our 227 substation-matched buses are the same class of artefact as their 274 estimates — the problem is estimation QUALITY, not acquisition. **Build in this order, because it is ordered by trustworthiness:** (1) **Queue Generator Coincidence** — a JOIN not a guess, a queue project interconnects at a named bus so its coordinates place it, and we already hold the PJM queue (`in_queue_*`); (2) **Breaker Branch Coincidence** — buses on a common breaker are one physical station, our `transmission_facility` carries from/to bus ids so the graph is walkable, and **each placement seeds the next**; (3) **string similarity LAST and gated** by the area-prefix geographic prior, since unconstrained name matching lands in the wrong state (median 100-180 mi, measured); (4) **a published per-bus confidence, refusing to place below a threshold** — unplaced is honest, misplaced is not, and a wrong coordinate silently corrupts every distance, county rollup and screener filter while a missing one announces itself. ⚠ **Do it while the subscription is live**: the vendor's 2,029 placed buses are the only labelled truth set we will ever have to score steps 1-3 against |
| **G45** | **Re-derive the bus tables across the FULL MW ladder, not just 100 MW** | 🔴 **OPEN — operator, 2026-08-18** | Operator: *"you may have to update the buses based on the new Orennia Excel sheet I provided you with ALL of the available MW ranges applied."* **What is measured so far:** their 2026-06-23 cost export carries proxy sizes **100 / 200 / 500** for PJM and **50 / 100 / 200 / 500 / 5000** for MISO; the operator reports QueueScope itself offers **10, 15, 25, 50, 100, 200, 300, 500, 1000, 1500, 2000, 3000 and 5000** in both directions, so what the export contains is a SUBSET of what was available, not the whole ladder. ⚠ **Do not record the export's subset as ‘their ladder’** — that was my inference and the operator corrected it. **Why it matters:** per-constraint headroom is request-INVARIANT (G7b, proven on 38,381 keys), so the ladder does NOT change a bus's headroom — but it does change **which constraints QueueScope returns**, and constraint-set divergence is the open blocker on PJM parity (we bind on a different facility from the vendor on 94-100% of buses). It also drives **upgrade counts and costs**, which are per-proxy-size and which we hold for MISO (`in_miso_dpp2025_ph1_project_costs`, 202 projects, $29.52B) but not for PJM. ⛔ **Cost before starting:** one direction at one MW took ~4.5 hours. The full 13-rung ladder in both directions is ~117 harvest-hours and **cannot be run as a single job** — the standing rule is one QueueScope process at a time, and it was already broken once today by a chained job whose wait failed open. **Sequence it, checkpoint each rung, and pick the rungs that answer a question** — start with the sizes the vendor actually published (100/200/500) so parity is testable, before harvesting rungs nobody has a yardstick for |
| **G44** | **Drop the visible “So what:” / “Why this matters:” LABELS — keep the statements** | ✅ **DONE 2026-08-18** — **33 labels removed across index / market / community / app.js, not one word of substance lost.** The marker moved from the TEXT to the `.sowhat` CLASS first, as the item required: **19 → 32 `.sowhat` blocks**, so coverage stays measurable. Verified no repo script greps the literal phrase before changing it. ⚠ **Two things the plan did not anticipate, both caught by looking at the output:** (1) every sentence had been written to FOLLOW its label, so stripping it left 27 blocks starting lower-case (*“this is the evidence behind…”*) — the opposite of the polish intended; all 27 capitalised, plus 5 more in the `app.js` county-metric legends where the label sat mid-paragraph after a `<br>`. (2) `app.js` is boot-critical and this repo has a recorded case of a syntax error killing the entire map, so it was **parse-verified in a real browser BEFORE pushing** — `METRIC_LEGEND`, `ALL_LAYER_BOXES` and `PRESETS` all defined, G34 intact at registry 17 / `PRESET_GAPS` 0, legend rendering 713 chars with no label, **0 lower-case starts and 0 labels remaining** | Operator: *"We don't actually have to state 'why this matters' in any part of the application, so long as the 'why this matters' statement is shown to the user… this basically just creates a more professional/polished look, and shouldn't materially change any of the information we are displaying."* **The insight the labels were scaffolding.** They were how G21 got written and how it got audited; a finished product does not narrate its own rubric at the reader. Good editorial writing states the consequence and trusts it to land. ⚠ **Purely presentational — NOT ONE WORD of substance may be lost.** The sentence stays; only the label in front of it goes. If removing the label makes a sentence read as a floating assertion, the sentence needed rewriting anyway, and rewriting it is in scope. ⛔ **THE TRAP, and it is a real one: our audits are TEXT-BASED.** `audit_frontend.py` reads source, and the G21 coverage check I ran on 2026-08-18 found surfaces by grepping for the literal strings *So what* and *Why this matters*. **Delete the labels without a replacement marker and every G21 coverage check silently reports zero coverage** — the same shape as the `[:12]` clip and the preset that named 10 of 17 layers: a check that passes because it can no longer see anything. ⭐ **So the marker must move from the TEXT to the CLASS.** `.sowhat` already exists in `style.css` and is already used on the newer surfaces; make it the single machine-readable marker, convert the older `<p class="hint"><b>Why this matters:</b>…` blocks to it, and update the coverage check to count `.sowhat` elements rather than grep for a phrase. Do the marker change FIRST, then strip the labels, so coverage is never unmeasurable in between. **Two inventories to sweep, both audited at runtime not from source:** the six Market cards and eight Community cards carry the label today, and `index.html`'s six themed layer accordions each open with **So what:** |
| **G43** | **Map layers are not clipped at the Indiana border** | ✅ **DONE 2026-08-19.** `scripts/clip_payloads_to_indiana.py` clips at the publisher's own state polygon (never a bbox — the southern border is a river). **27,933 → 25,891 features; 579 clipped, 2,042 dropped as wholly outside.** ⚠ The drops are a PRODUCT decision and were measured before writing: **82% of PJM candidate buses and 29% of transmission lines are wholly outside Indiana** because our AEP harvest spans five states. ⛔ No held-parcel distance can move — those come from BigQuery against FULL geometry; this is the drawing layer only, and every cut feature is stamped `_clipped`. Corroboration the boundary is right: water dropped 36 of 110, and `WATER_GEOMETRY.md` independently establishes only 39 of 76 subbasins touch Indiana. ⭐ **Now SELF-HEALING** — `export_grid_sentiment.py` silently un-clipped it once (9,020 → 10,659), so the exporter re-runs the clip itself and asserts | Operator: *"many of the layers are not properly clipped at the Indiana border, and this is necessary for aesthetic purposes."* Layers bleed past the state line, which reads as sloppy and, worse, **implies coverage we do not have and have not verified** — a line drawn into Ohio suggests we know something about Ohio. ⚠ **This is presentation, but it is not only presentation.** The standing rule is *Indiana only, clipped at the border*, and an unclipped layer quietly breaks it. **Likely culprits are the national sources we clip by attribute rather than by geometry** — transmission lines, gas pipelines, protected land (PADUS), bonus geographies, watersheds and HUC8 boundaries, and the nonattainment areas, several of which are genuinely multi-state polygons where an attribute filter keeps the whole shape. ⭐ **The fix should be a geometry clip at EXPORT time, not a filter at build time**: `ST_INTERSECTION(geom, indiana_boundary)` against `energy.state_boundaries` WHERE `STUSPS='IN'`, so one method covers every layer and a new layer inherits it. ⛔ **Do NOT clip by county-name or state-name attribute** — that is what produced the bleed, and it also drops any feature whose attribute is missing rather than clipping it. ⚠ **Two things must survive the clip:** a feature whose usefulness is its out-of-state end (an interstate transmission line or pipeline heading to a neighbouring hub) should be clipped for DRAWING but keep its full-length attribute, and a nearest-distance calculation must still measure to the real asset, **not to the point where we cut it** — clipping the geometry and then measuring to the clipped end would silently overstate distance to every cross-border asset, which is the G29 defect reintroduced from the other direction |
| **G42** | **News on Community: links, and the rows they were attached to** | ✅ **DONE 2026-08-17** — operator: *"the news section… should have links to the new sources, and doesn't supply them."* ⭐ **The links were never missing — the ROWS were.** All 283 news items carry a working URL and the render already emitted an `<a>`; but the browser listed the first 300 receipts **in payload order**, and the payload leads with 516 IURC dockets, so the default view was **300 dockets and ZERO news**. Nothing was broken, and nothing looked broken. ⚠ **Then the fix did not work either, for a second and separate reason:** news rows are stamped `2026-08-12 22:19:00+00`, and that `+00` offset is not valid ISO — `Date.parse` returns NaN, so all 283 scored 0 and sorted to the very bottom. Measured across the corpus: **642 plain `YYYY-MM-DD`, 283 with `+00` (exactly the news count), 27 longer, 3 empty.** Parsing the date part only handles all four. **Shipped:** newest-first across every kind (**216 news now visible by default, first at row 2**), the cap declared out loud — *"Showing 300 of 955 — 655 not shown"* plus a live per-kind breakdown of what is on screen — counts in the kind picker so a category can never be invisible again, `county_action` (50 rows) given the filter option it never had, multi-county tags rendered as *Allen, Wayne +2* instead of `|Allen|Wayne`, and untagged items labelled **no county tagged** rather than mislabelled *state*. ⚠ **Follow-up, not fixed:** 12 news items carry no county and at least some are plainly out of state (a Conroe, Texas item and a Chicago one sit in today's top rows) — the app is Indiana-only by rule, so that needs an operator call on whether national data-centre news is deliberate context. **LLM summaries are explicitly deferred** — operator: *"that can come at a later time… this WON'T be done for quite some time."* |
| **G41** | **A shipped payload can silently fall behind the warehouse, and the checkpoint does not see it** | ✅ **CLOSED 2026-08-17 — the checkpoint sees it now.** **TWO checks added, because there are TWO failure modes.** (1) *“no shipped payload is older than the table it reads”* — 8 payloads mapped to the tables behind them, compared against `__TABLES__.last_modified_time`; a table named here that does not exist is REPORTED, not skipped, since a silent no-op is the very defect being guarded. (2) *“no payload has lost a key a surface depends on”* — because a **wholesale rewrite drops a key while leaving the file NEWER than its table**, so freshness alone would pass it; this asserts `state_summary.si_by_signal` and `county_context.by_fips.*.iocs` and names the surface that goes blank without each. ⭐ **Both were proven to FAIL, not just to pass** — a payload was backdated 3 days and the key deleted, and the checkpoint reported *“si_v2.json.gz is 71.1h behind in_si_signal_coverage”* and *“state_summary.json lost `si_by_signal` -> SI Feed signal inventory renders EMPTY”*, then both were restored and the checkpoint returned to its single standing failure. A check that has only ever passed is a claim about the instrument. **All four of the day's instances would have been caught:** stale `si_v2` and the 337-feature `overlays` gap by (1), the `si_by_signal` and `iocs` losses by (2). ⚠ Still uncovered by design: a payload that is fresh, keeps its keys, and is simply WRONG. **PRE-DEMO SWEEP CLEAN ✅ PRE-DEMO SWEEP CLEAN 2026-08-17** — with a company-wide showing the next day, every page on PRODUCTION was swept for the failure mode this item describes: **~81 tables across 8 pages, ZERO rendering empty, zero unfilled `…` placeholders, zero blank named slots.** (`index.html` shows `#ledger` as `…` in the headless sandbox ONLY — `renderLedger()` is called at `app.js:69` inside `map.on("load")`, so it fills in any browser where MapLibre boots; the map not booting here is the known environmental failure.) ⚠ Note for the audit: `audit_frontend.py` reports `evidence-head` and `usecase-note` as dead ids, but both carry static content in the DOM — they are **false positives**, not broken surfaces. The sweep is a manual instrument and does not replace the check below | Found three times on 2026-08-17, each time by looking rather than by a check. (1) `si_by_signal` was dropped from `state_summary.json` by a whole-file rewrite, so the SI Feed's inventory table rendered EMPTY under a *“totals conserve to 0”* provenance line. (2) `overlays.geojson.gz` predated 337 QCT tracts, so the tax-credit layer shipped **1,057 of 1,394** bonus features — 24% short. (3) `si_v2.json.gz` still said **24,275** after the warehouse moved to 24,277, because `export_sites_exact` rebuilds the county files while the SI Feed has its own exporter. ⭐ **The mechanism is always the same: several scripts write the SAME payload, and at least one rewrites it WHOLESALE rather than merging.** `export_grid_sentiment.py:168` does `json.dump({"by_fips": ctx, …})`, which is why the court-activity table on Community had been rendering empty — `export_signoff_payloads.py` writes `iocs` into that same file and whichever ran last won. ⚠ **`audit_frontend.py` cannot catch any of these**: the element id IS referenced by a script, so it is not a dead id — the DATA is absent, and the audit reads source, not runtime. ⛔ **The checkpoint's payload-agreement check covers ONLY the 92 county sites files.** **Fix:** a check that every generated file under `data/` is newer than the tables it reads, or fails loudly — which would have caught all three, and would have caught the court table years earlier |
| **G40** | **PJM bus parity — match the vendor table grain for grain** | 🟡 **OPEN — OWNED BY THE PARALLEL SESSION, do not build it here** | Filed 2026-08-17 so the request lives in the contract rather than only in a chat. ⛔ **Another session holds the detailed derivation plan; this row is a POINTER, not a second copy** — two copies of one plan will drift (§2.15c). **Inputs verified independently here, not inherited:** `in_pjm_qs_tc2phii_wd` **462,654 rows** and `in_pjm_qs_tc2phii_inj` **655,404**, both **1,826/1,826 AEP buses**, **0 null `available_mw`**, both `case_label = "2028 TC2 Phase II Case (Summer Peak)"` — the vendor's exact PJM case — registry rows final and matching the live counts, and no harvest process still running. ⭐ **The two findings worth keeping even if nothing else survives:** (1) **A zero is a statement about the STUDY CASE, not the bus** — average pre-request loading is **83.0%** on withdrawal, and **118,689 of 119,066 zero-headroom rows (99.7%) are ALREADY ≥100% loaded before any request**. (2) **The vendor settles the treatment: flag-and-report, never exclude-and-hide** — their own Existing Overload Flag is TRUE on 8,232 of 19,846 Indiana rows (41.5%) and they still publish a capacity for those rows. ⚠ **THE OPEN PUZZLE, and it runs the opposite way to the old MISO gap:** our permissive variants put **88–98% of buses above zero against the vendor's 39.3%, median 24 MW against their 0** — we are MORE generous, so the temptation is to tune the rule until the number lands. ⛔ **Do not.** G22 is binding: match the METHOD, never the output; fitting to an answer key is not parity. The diagnostic is to compare our **binding-constraint NAME per bus** against their *Primary Limiting Constraint* for the same bus — that column reveals which constraint class their tier 0 keeps and we drop. **Limits that must ship as COLUMNS on the face of the output, not as footnotes:** tier 0 only (their tiers 1–4 need the CEII upgrade study, and nearly all their capacity lives there, so tier 0 is a DIFFERENT question rather than a weaker one); **AEP/I&M only**, which is northeast Indiana, not the state; PJM's own thermal-only and not-current-conditions caveats; and `desired_mw = 100` is an INPUT, with headroom request-invariant (G7b) so what the request size decides is FIT. ⛔ Never substitute one direction for the other — measured agreement on 200 AEP buses is **zero** (G7e). **Retire the case-4 tables** (`in_pjm_queuescope_aep`, `in_pjm_bus_withdrawal`, `in_pjm_bus_injection`); case 23's 1,826 buses vs case 4's 1,475 makes G23 moot by construction. **Type traps:** `bus_number` is STRING in the harvest and INTEGER in `in_pjm_bus_locations_candidate`; `bus_kv` STRING vs FLOAT; owner ids are renumbered per case (AEP = **1568** in case 23, 739 in case 4, and the wrong id loads 0 rows while **exiting successfully**) |
| **G39** | **Screener: per-site depth, a jump to the map, a dossier link, and the sites ON the map** | ✅ **DONE 2026-08-18 — all four parts, browser-verified** | **(1) DEPTH.** The table showed six columns while the payload already carried far more — measured across **51,493 sites**: the injection side on **50,333 (97.7%)**, the owner-signal block on **24,275 (47.1%)**, tax-credit zones on 22,819, flood on 11,824, protected land on 3,405. ⭐ So it was a **RENDERING gap, not an acquisition one** — nothing new is fetched. Each row now expands into four sections (getting power, sending power, the land, owner motivation and what could stop you) and, per the governing principle, **every field states what it CHANGES**, not what it is. **(2) DEEP LINK.** `index.html?fips=&parcel=[&open=dossier]`. The console loads parcels per county on demand and only above zoom 10, so the link cannot fly somewhere and hope — `ensureCountyLoaded()` fetches that county itself, then the map fits the parcel's **polygon bounds**, not a point. Verified end to end: screener row → link → 5,350 parcels → target found → dossier renders **Duke at 8.23¢**. ⛔ **Data and presentation now fail separately** — the first version lost the parcels when a map step threw and reported *“that link did not resolve”*, blaming the link for a rendering failure. **(3) DOSSIER.** The dropdown **links** to the Power Plan rather than reimplementing it; a second copy would drift the way the eligibility vocabulary did. ⭐ **(4) THE SCREENER ON THE MAP** (operator addition). New `L-screener` layer, lazy-loaded on first toggle (3.7 MB), amber where the owner shows a public reason to sell, sized by what the parcel can host — **20,040 plotted, 6,264 amber, 14,404 at ≥100 MW**. ⚠ **Coverage is partial and the control says so: 20,040 of 51,493 carry a lat/lon (38.9%)**, because *“the screener's sites”* and *“the screener's sites we can plot”* are different claims. Clicking a point opens the REAL parcel panel, not a copy. Every preset states the new layer so `PRESET_GAPS` stays empty (G34). ⚠ The front-end audit went 2→3 findings on this change and is back to 2 — it bound `const d = await fetchGz(screener)` and matched every unrelated `d.value` in a 2,500-line file. A false positive, but it was right that a one-letter payload name is bad here |
| **G38** | **Do the code-violation admits actually prove INTENT?** | ✅ **RULED AND BUILT 2026-08-17** ⭐ **Ruling 1 — `tier2_unresolved` STAYS.** Operator: *"I think that it measures seller intent to some extent, yes."* No change was needed — it was already admitted and contributing; the 715 parcels (103 of which carry no other signal) remain. ⭐ **Ruling 2 — catastrophic damage ADMITTED, deliberately ahead of its own evidence.** Operator: *"genuinely worth it, considering that we will maintain periodic rescrapes… we may see more of these signals, and do not want to miss them."* New signal **`D16_catastrophic_damage`**, status-gated to `CASE_STATUS IN ('Destroyed','Major')` on `CASE_TYPE = 'Enforcement/Damage Assessment/NA/NA'`, admitted at **TIER 1** — a destroyed building needs no pattern of repeat citations to be believed. `Affected` (202 rows) and `Minor` (51) are deliberately NOT admitted. ⚠ **My pre-build estimate was WRONG and the operator's instinct was better:** I measured *"3 of 24 addresses reachable"* by joining against the already-filtered widened table — a filtered instrument. The real build places **22 of 24** through the full Indy address authority, of which **2 pass the non-residential gate** and reach the flags. **Headline moved 24,275 → 24,277**, payload and warehouse agree, and coverage went 24 → 25 signals. ⚠ Every held event dates 2012–2014, so today the signal is nearly all latent — which is precisely the operator's rationale: the rule exists so the next event is caught when the rescrape lands, not added afterwards. **Ruling 3 — voided liquor licences (3 rows) noted and skipped.** Plain label added to `SIGNAL_PLAIN` so no codename reaches the UI (G8) | Operator, 2026-08-17: *"I really ONLY care about the observations that show actual seller intent… maintenance does not PROVE that the seller would be willing to sell."* **Measured, and the existing guard mostly holds:** of the parent corpus's 910,483 rows, **781,716 are excluded and only 128,767 admitted**; the excluded set includes **467,636 weeds/grass/trash rows**, so lawn care never reached the flags. `tier1_condemnation_track` (14,397 parcels — Unsafe Buildings, Vacant Board Order, Demolition) and `tier2_chronic` (12,302 parcels, **≥ 3 structural cases at one address**) both survive the intent test. ⚠ **The weak slice is `tier2_unresolved`: 715 parcels (3% of D12) admitted on a SINGLE unresolved repair case** — maintenance-shaped by the operator's own standard. **⭐ Answering *"are there violations that signal intent beyond neglect?"* — essentially no, and for a structural reason: code enforcement records the CITY ACTING AGAINST an owner, so by construction it captures neglect, never volition.** Nothing in the 29-value `CASE_TYPE` vocabulary expresses an intent to dispose. Two narrow, high-precision exceptions exist and both mirror a signal class the project already admits: (a) **`Enforcement/Damage Assessment/NA/NA` with `CASE_STATUS` in (`Destroyed` 4, `Major` 20) — 24 rows**, catastrophic structural loss, exactly the operator's own "total and complete structural fire" example and the same logic as D16; (b) **`Enforcement/Liquor License/NA/NA`, 3 rows, all `Void`** — a ceased licensed business, the D8 exit-intent shape. ⛔ **Do NOT admit `Enforcement/Legal/NA/NA` (20,557; 19,612 already Closed) or the 196k Zoning rows** — volume standing in for value, and neither expresses volition. **Decision needed:** drop `tier2_unresolved`, add the 24 Damage-Assessment rows, or both. Any change touches `in_si_sites_flags_v2` and therefore the shipped **24,275** headline — so it needs the standing sequence: rebuild, **re-export sites, THEN checkpoint** |
| **G1** | **Screener tab** (`screener.html`) | ✅ **DONE** `d1e2c95` | Ranked screener, grid as **CAPACITY not proximity**. ⛔ **Do NOT reference the national buildout's screener** — it holds P1 only and is materially incomplete |
| **G2** | **Insights summary tab** (`insights.html`) | ✅ **DONE** `d1e2c95` | What the data says, for someone who has never used the tool, including what it *cannot* tell you |
| **G3** | **Dossier → 3–5 page Power Plan** | ✅ **DONE** `92475c5` | Four pages on the supplied model, prints to PDF. Figure 1 stakeholders (point-in-polygon), Figure 2 parcel diagram to scale, Figure 3 path-to-power, Figure 4 checklist, Figure 5 evidence, appendix |
| **G4** | **Grid-siting UI** | ✅ **DONE 2026-08-19** — `gridsiting.json.gz` finally reaches a page. | ⭐ **A "Buses — both directions, both ISOs" section on `grid.html`**, reading the payload that had existed since this row opened and that only one page had ever touched (for a single provenance string). 3,912 buses, direction selector, free MW/kV entry, and every row links to the map. **879 zero-headroom buses are counted in public** with what a zero means, and the 1,246 unplaced PJM / 2,212 uncoordinated MISO buses are named on the card. `data/gridsiting.json.gz`: 871 buses, 328 MTEP, 116 cost studies, 145 territories. Wired into the Power Plan; **still not a map/dashboard section** |
| **G5** | **Rail → plain-named accordions** | ✅ **DONE** | 8 sections, 1 open by default, all named in plain language. **Every one of ~65 element ids preserved** — a renamed id kills a filter silently rather than erroring. Persistent `Filters in effect` bar above the map, verified live with filters set in COLLAPSED sections |
| **G6** | **Polish / professionalism pass** | 🟡 **HALF DONE 2026-08-19 — the headings; the structure remains** | Renaming the codenamed headings IS the first half: "Structure fires severe enough to move an owner to sell" earns its place in a way "D16 candidate" cannot. ⚠ **si.html still has 27 tables and 25 headings on one page with no grouping** — measured, and all 27 populate, so the clunk is volume and structure, not emptiness. **Do the restructure with G83**, which rewrites the same page; doing them separately means writing it twice. Reduce clunk; every table earns its place or comes out |
| **G7** | **Both directions, BOTH RTOs** | ✅ **DONE 2026-08-19** — closed with G4, same section. | The PJM and MISO ladder data now renders. See G4. PJM injection + MISO ladder landed; MISO withdrawal **BLOCKED, verbatim wording recorded**. **Not yet wired to any page** |
| **G8** | **Plain language everywhere** | 🟡 PARTLY DONE | Shared glossary in `common.js`; nav renamed; screener/insights/Power Plan clean. **Existing pages not yet swept** |
| **G9** | **Date-gate EVENT signals only** | 🟢 **SCREENER + MAP CONSOLE DONE** | Three-way gate (dated / standing / **undated**) on both. ⭐ **The map console was already keeping and disclosing undated records** — the missing half was the operator's *choice*, now `f-keepundated` (default on) in section 4, with "undated records excluded" surfaced in the Filters-in-effect bar so a collapsed section can never hide an exclusion. **Verified in-browser:** keep-ON passes undated and counts it; keep-OFF drops it; a *dated* stale record is dropped either way. ⚠ On the map payload only **0.6% of SI parcels carry an event date** (935 of 165,494 over 7 counties), so a naive cut would delete 99.4% of them for a reason about OUR coverage. **Remaining: `si.html`** — folded into G33 |
| **G10** | **"Vacant" means two things** | ✅ **DONE** | One word, one meaning, swept app-wide. `LAND_PLAIN`/`landPlain()` in `common.js` is the single vocabulary; the map's `f-vac` reads *"Undeveloped — no building on it"*; screener, `si.html` labels and the D5 split table all say **undeveloped land**. ⭐ **Deliberately preserved:** ACS *housing-unit* vacancy (census term, genuinely about buildings) and the landbank's *"mostly vacant lots"*, now marked **"(their words, not our vocabulary)"** — a direct quote is evidence, and rewriting a source's words would be worse than the ambiguity |
| **G11** | **Sentiment statuses not reaching the map** | ✅ **DONE 2026-08-19** — and SIX counties were wrongly passing a filter. | ⭐ **`countyPosture()` is now the single resolver** and FIVE surfaces were moved onto it — the restriction filter, `scoreP5`, the dossier takeaway, dossier Figure 3 and dossier Figure 4. ⛔ **`local_bans` is 0 on all 92 counties** while the warehouse holds two bans, so the old test could never fire: **Cass scored 100 — the most permissive band — while holding a BAN**, and passed the exclude-restrictions filter. Verified across all 92: **14 restricted, 14 excluded, 0 disagreements, 0 scored above the restricted band**; it was 8 before. Rule: take the WORSE of the two vocabularies — each sees restrictions the other misses (Cass only on the verified side, Marion only on the legacy side). Measure the vocabulary diff between `county_context.json` (map) and `receipts.json.gz` + `in_dc_actions_resolved` (Community) **before** changing anything |
| **G11** | **Verified county actions never reached the map** | ~~superseded~~ **Older duplicate — the live G11 row is directly above** |⛔ Superseded by the 2026-08-19 G11 row above, which unified FIVE surfaces onto one resolver. Kept for history. 33 counties carry a verified action and the map called **13 of them "quiet"** — Cass has a **ban**, Floyd/Huntington/Whitley **moratoriums**, all with `has_local_restriction=false`. And **10 counties have APPROVED a data centre** with no representation at all. Now a categorical shading + legend |
| **G29** | **Parcel→asset distance is measured wrong on the map** | ✅ **CLOSED 2026-08-19 — the bus was the last asset on the approximation.** `enrichDistances` measured parcel-FIRST-VERTEX to a binned bus point, which always overstates and can never return 0. ⭐ **Nothing needed computing:** `in_screener_candidates` has carried exact per-direction ST_DISTANCE (`wd_mi`/`inj_mi`, against the real polygon) for all 532,868 candidates since it was built — the screener was always right and only the map console approximated. A JOIN, not a build. Both directions ship (`x_bus_wd_*`, `x_bus_inj_*`), **never fused**; withdrawal is the default because a data centre asks the load question (G63). `_dpoi_exact` rides on the parcel so the evidence panel names which measurement it is showing, and the client-side path survives ONLY as the fallback for uploaded rows that never had one | ✅ `in_asset_distance_parcel` (532,868, fan-out 1.000) ships exact `ST_DISTANCE`. ⭐ **41,986 parcels have a transmission line physically ON them**, plus **79,168 with water on them** — the map reported ~0.55 mi for every one. Verified twice: **ST_INTERSECTS agrees 400/400**, and the point method returns **median 0.088 mi, max 0.772 mi where the truth is 0.0**. `app.js` now PREFERS the shipped exact value and falls back to `repPt()` only where none exists (uploads), carrying `_dist_exact` so the panel says which measurement it shows. **Remaining: buses on the map console** (the screener's were always exact) |
| **G30** | **GitHub Pages deploy failures** | ✅ **CLOSED 2026-08-17 — NOT OUR DEFECT, and the previous diagnosis was WRONG** | ⭐ **Read the actual error before acting on this.** Run #128 (`9361a16`): `build` **SUCCEEDED** in 39s and uploaded a **363 MB `github-pages` artifact**, then `deploy` failed with **HTTP 503 — "No server is currently available… is githubstatus.com reporting a Pages outage? Please re-run the deployment at a later time."** A **transient GitHub-side Pages outage.** Exactly **4 failures ever — #125, #126, #127, #128 — matching the operator's 4 emails — and #129 SUCCEEDED with no change from us.** ⛔ **The inherited "cause is size" diagnosis is DISPROVEN:** the 363 MB artifact uploaded fine, no file exceeds 100 MB (largest 15.6 MB), and the published site is under the 1 GB Pages limit. Acting on it would have meant moving `data/sites/` to PMTiles/CDN/LFS — **large, risky, map-breaking work to fix a GitHub outage.** ⚠ Separately, the operator reports **a water agent failing to PUSH oversized files** — that is a *git push* rejection, not a Pages deploy, and the operator is handling it. **Two different email sources; do not conflate them** |
| **G30b** | Repo pack is 2.91 GiB (hygiene, not a failure) | 🔴 **OPERATOR RULING 2026-08-19: KEEP ALL 13 DECIMAL PLACES. The 45% saving is OFF THE TABLE.** | ⛔ **Operator, 2026-08-19: *"we would like to use all 13 decimal places, if able, to ensure full precision and accuracy."* So the truncation this row proposed — 13 dp → 6 dp, 334 MB → ~185 MB — IS REJECTED and must not be revisited.** Recording it here so no future session re-proposes it as an easy win: it is not available. ⚠ **What the row still gets right: the repo `size-pack` is 2.91 GiB and shrinking the FILES would not shrink it anyway** — a new copy adds to the pack; only history rewriting shrinks it. And it **does not affect Pages**, only clone time. ⭐ **So the honest remaining options are all about the PACK, not the precision:** (a) accept it — it costs clone time and nothing else; (b) `git lfs` for `data/sites/`, which also addresses the `SEC_E_MESSAGE_ALTERED` push failure on 224 MB of already-gzipped files; (c) shallow clones for anyone who does not need history. **(b) is the only one that fixes a live problem.** Lowering priority to match: this is now a repo-hygiene item, not a data item Real but *not* what broke anything: `size-pack` **2.91 GiB** from **12 commits × ~334 MB of already-gzipped** payload, which git cannot delta-compress, so each rebuild stores a full fresh copy. Slows clones; does not affect Pages. ⚠ **Shrinking the files does NOT shrink the pack** — a new copy *adds* to it; only history rewriting shrinks it. Measured win available if wanted: parcel coordinates carry **13 decimal places** (nanometre precision on a survey boundary); truncating to **6** (~0.11 m) is **45% smaller — 334 MB → ~185 MB** and invisible on screen. Do it when `data/sites/` is next regenerated, not as a standalone rewrite |
| **G31** | **NHD watershed backfill** | ✅ **DONE** | `in_nhd_flowline_geom` **160,128 → 163,976**, `in_nhd_waterbody_geom` **6,415 → 7,430**, both at **100% Indiana-cut completeness**, 0 null geog, 0 duplicates. ⭐ **The brief's premise was wrong and the agent caught it: only 39 of 76 HUC8 rows touch Indiana** (two independent tests agree), so coverage is **39 of 39**, not 59 of 76 — chasing the "gap" would have loaded Wisconsin and Michigan streams into an Indiana table. ⚠ It also found `energy.nhd_*.src_state` is unreliable: 932 flowlines + 90 waterbodies tagged `IN` sit outside Indiana, up to 480 km away. Distance answers are unaffected; **row COUNTS of "Indiana rivers" overcount**. Rebuilt `in_water_distance_parcel` against the complete geometry: **79,168 water-on-parcel, median 0.38 mi — identical**, ~20 rows different of 532,868 |
| **G37** | **PJM case-23 harvests (the benchmark's exact PJM study)** | ✅ **COMPLETE — 100% BOTH DIRECTIONS** | Case 23 = *"2028 TC2 Phase II Case (Summer Peak)"* = the vendor's exact PJM case. **`in_pjm_qs_tc2phii_wd` 462,654 rows and `in_pjm_qs_tc2phii_inj` 655,404 rows — 1,826 of 1,826 AEP buses each, 0 null `available_mw`, registry rows FINAL and matching.** 1.12M constraint rows total. Survived a mid-run network drop and resumed cleanly from on-disk checkpoints — the checkpoint design held. ⛔ **Owner id is `1568` for case 23, NOT 739** (renumbered per case; 739 loads 0 rows and exits *successfully*). ⛔ Never two QueueScope instances; ⛔ never delete `data/`. **NEXT: rebuild `in_bus_capacity_tier0` on case 23 (it still holds stale case-4 PJM), attach the G7m cost join, retire the case-4 tables** |
| **G7m** | **DPP-2025: cost dimension LANDED, headroom is CEII-walled** | 🟢 **RESOLVED — see the G7m section** | ✅ `in_miso_dpp2025_ph1_project_costs` (202 projects, $29.52B; **21 of 27 Indiana projects, $1.7B**) via MISO's open find→CDN route. ❌ Bus headroom is **CEII** (study's own words + FERC ER24-2046), walled even from inside the viewer's session. Path forward: model it ourselves from BPM-015 §3.1.1, validate against the 2021 GeoTIFF |
| **G7n** | **DPP-2023: cheap cost check only, NOT a headroom hunt** | 🔴 **OPTION, next session** | 2023 headroom is CEII the same way (low value). 2023 **cost** SIS likely public via the same route and its **Phase 2 already exists** (2025's does not) → fuller cost picture. One `--discover` call |
| **G35** | **Queue pressure on the parcel panel** | ✅ **DONE** | Both RTOs already held (`in_queue` 361 PJM + 583 MISO Indiana, all with MW). Evidence panel now shows active projects / active MW / **withdrawn %** per county, stating both readings (queued generation is future supply AND competition for study slots). ⭐ Attrition is the sharp signal: **49 of 87 counties have ≥50% of everything ever queued withdrawn** |
| **G34** | **Layers stay drawn when their toggle is OFF** | ✅ **DONE 2026-08-17 — reproduced and fixed** | *"Wind + solar projects stay in view even when their overlay isn't displaying — they should disappear when not prompted to be on the map."* **Partly diagnosed, NOT yet reproduced** (the map does not boot in the sandbox browser, so this needs a real-browser repro). ⭐ **Structural finding:** `syncLayers()` enforces visibility by iterating **`LAYER_MAP` only**, and the change-listener is bound over `Object.keys(LAYER_MAP)` — but **`L-ghgrp` and `L-frpp` live in a SEPARATE `CONTEXT_LAYERS` map** (`ctx-ghgrp`, `ctx-frpp`), so those two are outside the single mechanism that enforces "off means hidden". **Two registries for one concept is the §2.15c defect this project keeps hitting.** ⚠ Second contributor: the four presets (`land`/`grid`/`env`/`sentiment`) each set only *some* layers, so switching preset leaves the unlisted ones in their previous state — `env` never mentions `L-fac`, which is the wind/solar layer. **FIXED exactly that way.** ⭐ **Reproduced at last** — not at runtime (the map still does not boot in the sandbox browser, confirmed again: `map` exists but `getStyle()` throws and the style never loads) but **statically and decisively, then verified end-to-end in a real browser without needing the map to render.** ⭐ **The proof: `grid` set `"L-fac": 1` and NO other preset mentioned `L-fac` at all.** Switching Grid → Environmental therefore left wind and solar drawn, because nothing ever set it back to 0 — precisely the operator's report. `git log -S "L-fac" -- app.js` returns a single commit (`da749bd`, the one that introduced the layer), confirming it had never been touched since. **7 of the 17 layer boxes were unstated by land/env/sentiment.** Fix shipped: `ALL_LAYER_BOXES` unifies `LAYER_MAP` + `CONTEXT_LAYERS` + `L-parcels`; `syncLayers()` now enforces the context layers through the same path; the preset applier **iterates the registry, not the preset**, so an unstated box turns OFF instead of persisting; all four presets state all 17; and `PRESET_GAPS` console-errors at boot if a newly added layer is ever left unstated. **Browser-verified:** registry 17, `PRESET_GAPS` empty, Grid → `L-fac` true, Env → `L-fac` false, and all six previously-unstated layers clear on a preset switch. ⚠ Deliberately minimal: every value a preset already stated is UNCHANGED, only the unstated became an explicit 0 — so `env` still does not switch the water layers on, which is a reasonable follow-up but was not part of the reported defect |
| **G33** | **Owner-signals page needs a COMPLETE overhaul** | ✅ **DONE 2026-08-17 — rebuilt around the question, browser-verified** ⭐ **24 flat sections → 9 top-level surfaces + ONE accordion holding 14 build-log cards.** ⭐ **The find: `top_evidence` — 120 ranked parcels — was ALREADY in the payload and rendered NOWHERE.** The page shipped the answer to its own question and showed none of it, while the owner-portfolio table sat at position 21 of 24. Both are now the lead: *"Which owners might sell you land — and how strongly we believe it"*, ranked by how many DIFFERENT kinds of distress stack on one parcel, with a plain-language *why* column, a `.sowhat`, and event-vs-state recency handled per signal (a foreclosure goes stale, contamination does not — old-but-durable is marked, not dropped). ⚠ **Three defects found while rebuilding, all pre-existing:** (1) **`t-sig` was rendering EMPTY** — `si_by_signal` had been dropped from `state_summary.json` by an earlier whole-file payload rewrite, and the page printed *"totals conserve to 0"*; restored via `build_t3_t4.py`, **17 signals, 1,818,158, conserving exactly to the original commit's figure**. (2) **Raw codenames were reaching the UI on four surfaces** (`t-sig`, `t-cov`, `t-own`, `t-adm`, plus one in prose) — a G8 violation; six codes had no plain label at all, so `SIGNAL_PLAIN` in `common.js` gained `D6/D17/D25/D8/D3/D5_vacancy`, which fixes every surface at once rather than one page. (3) **The inventory heading implied completeness it does not have** — it lists 17 signals from ONE upstream table while 24 appear in coverage and 34 variants exist across 29 slots (D1–D27 + A1/A2, **none missing**); retitled and reframed with LIVE counts. ⛔ Every id moved, none renamed (G5); verified 0 ids lost, 0 empty tables, 0 unfilled placeholders, **0 codenames anywhere including inside the collapsed accordion** | *"That entire page needs a complete overhaul… the tables are all over the place and there is seemingly no structure whatsoever — many of the tables are irrelevant and don't provide material insights or actionable results."* **Measured: 26 tables, 24 top-level sections, 1 sub-heading, ZERO accordions** — a flat wall with no hierarchy. ⭐ **Root cause is not layout, it is AUDIENCE: `si.html` is a BUILD LOG, not a product surface.** At least 7 of its 24 sections document how the corpus was assembled and audited ("Lane D — columns pulled and never wired", "Marion placement checked by two instruments", "Admitted by operator sign-off", "The flag was a vacancy flag — what changed"). That was the right artefact while building; it is not what a developer opens. Rebuild around **"which owners might sell me land, and why do I believe it"**, and move provenance behind a single collapsed *How this was built* section — or onto the Data page, which is already exempt from G21 as internal-by-design |
| **G32** | **Map layers belong INSIDE the themed dropdowns** | ✅ **DONE** | The flat *"Map layers"* block is dissolved; all **18 layer controls + county shading** now sit in the theme that owns their subject — **Land & size** (parcels, rail/roads, federal surplus), **Power & grid** (substations, lines + kV filter, connection points, PJM queue, territories, gas, generation, large emitters), **Environmental** (protected land, nonattainment, tax-credit areas, watersheds, water stress), **Community** (data centres, county shading). Each block gained a G21 *"So what"*. ⭐ **Verified in a real browser: all 21 ids present, zero duplicates, every control in its intended section** — the DOM nodes were MOVED, never renamed, per G5's silent-kill rule. Also corrected two stale claims found in the moved text: watersheds said *"76 subbasins"* (only **39** touch Indiana) and *"rivers and lakes are not drawn… we are chasing the geometry"*, which the NHD backfill closed the same day |
| ~~G32 old~~ | | 🔴 **OPEN — operator, 2026-08-17** | *"For the map layers, those should be embedded [in] the 1-6 dropdowns in the dashboard, so everything stays consistent with their function."* Today the themed accordions (Owner motivation / Power & grid / Land & size / Environmental / Community / Market) and the map's LAYER toggles are two separate organising systems over the same subjects — a user turns on a grid filter in one place and a grid layer in another. **Each layer moves into the theme it belongs to**, so one dropdown owns one subject end-to-end: filter, layer and its "so what" together. ⚠ **Every one of ~65 element ids must survive the move** — G5 established that a renamed id kills a filter *silently* rather than erroring. Pairs with **G21**: a layer arriving in a themed section still needs its why-it-matters line, and a legend is not a "so what" |
| **G12** | **💧 WATER** | 🟢 **ON THE MAP** | `in_water_county` (92/92) + `in_water_stress_basin` (34). Statewide 7,176.7 Mgal/d, thermoelectric 3,822.0. **Parcel-grain still open** (NHD proximity, ECHO discharge). Two silent traps hit and fixed; groundwater/NWIS genuinely NOT held |
| ~~G12 old~~ | | | A first-order cooling constraint, so a missing **screening dimension**, not a missing layer. Clip the Indiana slice into `indiana_app` (**clip, do not duplicate**), register, export, surface in the screener and map |
| **G13** | **Voltage: filter, colour, AUDIT** | ✅ **DONE** | Audit first, as required. **335 lines carried `kv = -999999`** (HIFLD's not-available marker loaded as a number) and **65 were RECOVERED** from `volt_class` rather than binned. All 1,114 OSM lines had NULL `volt_class` while carrying clean kv. Lines now coloured by audited class with **unknown as its own dashed colour**, plus a voltage filter. ⚠ Disclosed: **53% of substations have no published voltage**, so a kV filter silently discards half of them |
| **G14** | **Re-scrape missing signal dates** | ✅ **MEASURED 2026-08-19 — ITS NAMED SUBJECT IS ALREADY 100% DATED. The row was pointing at the wrong signal.** | ⭐ **Measured at PARCEL grain, which is what the app actually renders: `D4_tax_delinquency` carries 15,106 events and 15,106 of them are dated — 100.0%.** The row's premise ("90% of tax-delinquency records are undated, and it is our largest signal") is stale on both counts: it is fully dated, and at 15,106 events it is not the largest — `D12_code_violation` (59,050) and `D5_unsafe_building` (49,338) are bigger, and both are also 100% dated. ⭐ **The undated mass is somewhere else entirely, and the pipeline already records WHY:** `date_basis` carries *"publisher carries no event date"* on **7,101** rows, which matches almost exactly the one real gap — **`D5_abandoned_building`, 7,167 events with 47 dated (0.7%)**. ⛔ **And that one should NOT be chased**, because G9 already ruled it: an abandoned building is a **state**, not an event, and states do not expire. Date-gating it would silently delete real current inventory. ⭐ **The ONE genuinely actionable remainder is `D1_tax_sale`: 11,333 events, 7,286 dated (64.3%) — while its SOURCE `in_si_refresh_sri_taxsale_in` carries `auctionDate` on 82,595 of 83,547 rows (98.9%).** That gap is a propagation loss in our own join, it is an EVENT signal so recency genuinely matters, and it is the only piece of this row still worth work. Others for the record: `D22_facility_inactive` 15.3% (a state again), `D22_environmental_violation` 86.6%, `D2_foreclosure` 97.3%. ⚠ Also spotted while measuring and worth carrying to **G70/G81**: that same tax-sale source holds **`ownerName1`/`ownerName2`** and coordinates on 29,955 of 83,547 rows — owner names for Indiana parcels, from a source we already hold. **90% of tax-delinquency records (8,523 of 9,459) are undated** and it is our largest signal. Establish first whether the source publishes a date we failed to capture |
| **G15** | **Future capacity — IURC + ISO/RTO, plottable** | 🟡 **PROGRESSED 2026-08-19 — and the row's premise was wrong. It does not need re-extracting; it needed MAPPING.** | ⭐ **`in_grid_plans` was never missing the text — it was written into the wrong column.** `project_name` held the ENTIRE pipe-delimited workpaper row: `1 | TS000091 | Burns Ditch 13810-37 | Breaker | 138kV | 22.4 | 87% | 19.40`, while `location_text`, `substation_names` and `county` sat NULL beside it. So this was a **propagation loss inside our own pipeline**, the same shape G14 turned out to have, and **no scrape and no PDF parse were required**. `scripts/build_grid_plans_located.py` splits the row into its real fields and matches each asset's station against `in_substations` (exact → suffix-insensitive → **unambiguous** prefix; `location_method` records which one fired, and a name matching 2+ stations is REFUSED rather than assigned — that is the CLOUDSCENE_GAP mistake). ⭐ **County went 0 → 119 of 618**; 321 rows parse to an asset name. ⛔ **`cost_usd_m` stays NULL ON PURPOSE**: the workpaper's numeric columns arrive unlabelled (`22.4 | 87% | 19.40`) and guessing which is dollars would print a coin flip on a page. Recovering cost means going back to the workpaper HEADER row — that, and only that, is the re-extraction this row describes. **199 rows name a station the gazetteer does not hold — see G62, it is one shared blocker.** 6 utilities held, but location/cost/in-service extracted at **0%**. Re-extract, then locate in two tiers: exact join for existing assets, **uncertainty rings** for vaguely-described new ones |
| **G16** | **Agent registry rows must be re-runnable** | ✅ **RULE SET** | URL, endpoint type, loader command, slice params, publisher vintage, exclusions. In `SESSION_START.md` and sent to the agent |
| **G17** | **Component tariff cost model** | ✅ **CLOSED 2026-08-19 — BUILT BY THE RATE ENGINE (G55/G56). The row predates it.** | ⭐ **Both of this row's named asks were shipped and the row simply never caught up.** **(1) Service voltage as a site attribute:** `applies_to` carries the voltage dimension across the book — secondary (26), primary (17), transmission (11), primary direct (6), `69 kV` (6), `transmission (69 kV+)` (5) — and every schedule object in `tariffs.json.gz` carries a **`volt_classes`** field, so the engine prices each schedule AT EACH SERVICE VOLTAGE. The dossier already tells a reader the spread is worth real money at their own parcel's utility rather than quoting the worked $210k/yr example this row was built on (that hardcoded figure was itself a defect, fixed as dossier audit D-3). **(2) Fuel adjustment as a DELTA:** both halves are held — the base embedded in base rates (`DUKE-FUELBASE` $0.034378/kWh, `IM-FUELBASE` $0.012981/kWh) AND the current factor (`DK-FAC-HLF` $0.004319, `DK-FAC-LLF` $0.004422) — which is precisely what a delta needs. ⭐ Utilities with **no** tracker are recorded as such (`LOGA-NO-TRACKER`, `MISH-NO-PCA`) rather than left blank, which is the absence-is-recorded discipline working Reclassifies §13(8) from *impossible* to *acquirable*. Service voltage is a **site attribute** worth $210k/yr on 35 MW; fuel adjustment is a **delta** worth $3.6M of $16.0M |
| **G18** | **Charts must be labelled and explained** | ~~superseded~~ **This row is STALE — see the G18 DONE row below** |⛔ **Do not work this.** It describes `svgLine()` drawing a bare polyline and contradicting its own caption — `chartLine()` replaced it with real axes, units and a partial-period guard, and fixed the `slice()`. Kept only so the history is legible; the duplicate G-number is the drift this project keeps hitting. `svgLine()` draws a bare polyline — no axes, scale, range or units — **and plots `slice(-120)` while its caption claims 228 months.** The chart contradicts its own caption |
| **G19** | **De-duplicate substations** | ✅ **DONE** | 848 duplicates collapsed. Also found **503 taps + 27 dead ends** mixed in with real substations, and two unreconciled type vocabularies → `asset_class` added. Real substations: **1,741**, not 3,858 |
| **G20** | **100% parity with the vendor extract** | 🟡 **MEASURED 2026-08-19 — and most of the row is moot; what remains is smaller and different from what it says** | ⭐ **The row prescribes DERIVING taps and dead-ends from line topology. We already carry them as their own class** — `in_substations` types 2,394 SUBSTATION, 497 TAP, 29 DEAD END, plus industrial (76), distribution (31), traction (21), generation (17) and 5 RISER. That half is done. ⚠ **What the measurement found instead: 738 of 3,858 rows carry NO substation_type AND — checked — no `max_kv` and no coordinates either.** They are name-only shells. They inflate the substation count without being usable for placement, distance or voltage screening, and they are a quiet contributor to the G62 gazetteer ceiling: the usable gazetteer is **2,255 distinct names over 3,111 substations**, not 3,858. Either resolve them or mark them so a reader knows the count they see is not the count they can use. **Pike County 765 kV remains `status='Planned'` and belongs to G15**, not to a substation backfill Root-caused: **dedup did NOT eat them** — 76% of the 42 are taps/dead-ends (taxonomy, not loss). Derive taps from line topology as their own class; chase the 6 real gaps; **Pike County 765 kV is `Planned`** → belongs to G15 |
| **G21** | **EVERY surface must answer "so what?"** | 🟡 **PROGRESSED 2026-08-19 — the map layers that had no line now have one** | ⭐ Section 3 (Power & grid) carried **14 layers and ONE importance line**, and that line was about voltage colour — so buses, the queue, candidate buses and territories had nothing. Four blocks added, in the operator's own framing (*they need not say "so what"; they must outline why the dataset matters to an energy developer*): substations-vs-lines-vs-buses (only one is about capacity), the queue as competition for the same study slots, territories as who you negotiate with, the six tax-credit geographies as five benefits and one blocker. Section 3: 2→5 blocks, section 5: 3→4, section 2: 1→2. **Remaining: the G72 layers as they land.** ⚠ **PROJECT-WIDE — map layers, tables, charts AND the screener**, not the two example cards. Five requirements per surface + a full ❌ inventory in the G21 section. A surface that cannot answer it comes out |
| **G18** | **Charts labelled and explained** | ✅ **DONE** | `chartLine()` replaces the bare polyline: real axes, units, zero-based, full range, plain reading, and a partial-final-period guard. Fixed the `slice(-120)`-vs-caption contradiction |
| **G22** | **Headroom = the ACTUAL, and the vintage** | 🟢 **FIXED; the vintage gap is now CLOSED AS NOT-ACHIEVABLE** | median→actual→**unclamped** both fixed. Root cause of the zeros is our **DPP-2021 vs their DPP-2025** case. ⭐ **G7m settles it: DPP-2025 constraint rows are CEII-gated and have no public route** (four independent sweeps). Our MISO figures are **MISO's own current public position** — state that on the face of the number rather than implying we are behind on work we could have done. ⛔ Do NOT keep "get the mitigated case" open as if it were engineering time |
| **G23** | **49 missing PJM withdrawal buses** | 🟢 **SUPERSEDED by the case-23 harvest** | The old case-4 harvest covered 1,475 of AEP's 1,524. ⭐ **Case 23 (2028 TC2 Phase II) offers 1,826 AEP buses — 302 MORE than case 4 — so the newer case both replaces the stale one and closes the 49-bus gap by construction.** Once the case-23 harvests complete, rebuild from them and retire the case-4 tables rather than patching them |
| **G26** | **Headroom under-reports: pre-existing overloads + an UNMITIGATED case** | 🟡 **METHOD ANSWERED; vintage half closed by G7m** | 26.3% of Indiana facility rows are already overloaded **before any request**. ⭐ **The benchmark's own file settles the method question: `Existing Overload Flag` is TRUE on 8,232 of their 19,846 Indiana rows (41.5%) — they FLAG pre-existing overloads and still report a capacity, they do not drop them.** So our choice was never "exclude or not"; it is flag-and-report. ⛔ Do NOT drop overloads (that gives 100%). The mitigated-case half is **not obtainable** — see G7m |
| **G27** | **401 columns under-clipped across 24 tables** | 🟡 **PARTLY DONE — and the audit CRIED WOLF on the gas half** | ✅ `percent_loading_before` + `headroom_state` re-clipped — the app can now say WHY a bus reads zero. ⛔ **MEASURED 2026-08-17, three of the four headline claims are wrong:** (a) **gas processing plants** — parent `energy.gas_processing_plants` has **0 Indiana rows**; there is nothing to clip, the clip is empty because the data does not exist; (b) **gas compressor stations** — our clip already has **24 of 24 Indiana rows WITH `geog`**; nothing was lost, the clip and parent are *different sources* the audit matched by name; (c) **`echo_cwa_facilities`** — the parent itself has **no latitude column at all** (17 of 17 columns held), so this is a **platform-side defect, not an under-clip**. ✅ **THE ONE REAL GAP IS NOW CLOSED (2026-08-17).** Cause was `[:12]` truncating the parent schema at column 12 while `classification` sits at 16 — not a mapping error. Rebuilt in place, **62 rows / 56 classified**, the 6 blanks all `Maintenance` areas where no band applies; five epoch-ms date columns decoded to `DATE`; severity band given a `.sowhat` on the map popup. ⚠ **The same `[:N]` idiom is unaudited in `build_gas_facilities.py`, `build_gas_market.py` and `export_full_wiring.py`.** The other three claims stay closed as audit false-positives |
| **G28** | **Extensive acreage audit** | 🟢 **THE OPEN HALF IS NOW WRITTEN — `docs/BUILDABLE_AREA_BASIS.md`, 2026-08-19** | ⭐ **G28's remaining half was a DOCUMENT, not a number, and it now exists.** `docs/BUILDABLE_AREA_BASIS.md` states the basis exactly: **data-centre = the whole parcel; battery = the parcel minus existing building footprints** (`exact_outdoor_acres`, measured against real footprints, never a centroid). **Nothing else is deducted.** Flood, wetland, protected land and transmission right-of-way are **flagged beside the figure and never subtracted from it** — because we hold each as a per-parcel BOOLEAN, not as an area, and docking a parcel by a guessed percentage would produce a measured-looking number wrong by an unknown amount in an unknown direction. That is the unpublished-rate-as-zero defect (95 false violations) in a different costume. ⚠ **What it would cost to net the rest is now priced rather than hand-waved:** `in_flood` is **803.8 MB** and `in_wetlands` **1,319.6 MB**, so a pairwise intersection over 532,868 parcels is a **cost-flag job under the $25–50 rule**. ⛔ **And transmission RoW should be done LAST despite affecting the most parcels (41,986)** — a right-of-way is a legal instrument, not a geometric one, and we hold no easement width, so buffering by a 150 ft rule of thumb turns an assumption into a precise-looking number. Setbacks are blocked behind **G71**. Recommended first move: flood only, one costed query Measured: the two acreage measures **agree** (median 23.75 = 23.75), so inflation is not drift — the defect was the **presentation**. ✅ `mwReality()` in `common.js` now bands every derived MW and states it is gross land. Measured tail: median **94.4 MW**, p99 **710 MW**, but **2,204 parcels ≥1,000 MW, 368 ≥2,000, max 25,428 MW**. ⛔ Not capped — capping hides real land from a phased campus; it is **banded and labelled** instead, and the wording moved from *"fits X MW"* to *"land supports up to X MW"* on the tooltip, verdict line and takeaways. **Still open: a documented buildable-area basis** (net of setbacks, easements, RoW, internal water, slope, existing structures) |
| **G24** | **Asset URLs must be cache-busted** | ✅ **DONE** `92475c5` | `scripts/stamp_assets.py` content-hashes `app.js`/`common.js`/`style.css`. A stale asset cost a full debugging cycle; GitHub Pages caches just as hard as a browser |

### G7 — the finding that makes this urgent

**We hold the WRONG DIRECTION across most of Indiana.**

| RTO | injection (generator) | withdrawal (**what a data centre needs**) |
|---|---|---|
| **MISO** | ✅ 642 located Indiana buses | ❌ **NOT HELD** |
| **PJM** | ❌ **NOT HELD** | ✅ 1,475 buses, only **229 located** |

Measured 2026-08-17: `energy.pjm_queuescope_results` is **WITHDRAWAL-only at `desired_mw=100`
across all 24 PJM transmission owners** — there is no injection hiding in it to clip. And most of
Indiana is MISO (Duke Energy Indiana 881k customers, IPL 519k, NIPSCO 486k); PJM here is
essentially I&M (608k). **So for the majority of the state we hold only the generator direction.**

⛔ **Injection headroom is NOT a weaker version of withdrawal headroom — it is a different
question.** A 5,000 MW injection figure says nothing about whether a 300 MW load can be served.
Never average, compare or substitute across the two. `direction` rides on every bus row in
`export_grid_siting.py` for exactly this reason. Agent is permitted to return **BLOCKED with a
verbatim wall** on the MISO-withdrawal half — MISO's `poi_mf` API is a *generator* tool and a
public load-side equivalent may simply not exist.

**Measured consequence, 2026-08-17:** of 532,868 screener candidates, **only 207,614 (39%) have any
load-side bus within 25 miles**, while 515,932 (97%) have an injection bus. For three fifths of the
candidate set, *the direction a data centre actually needs is unavailable.*

#### ⭐ G7h — THE PJM STUDY THE BENCHMARK USES IS AVAILABLE. WE ASKED FOR THE WRONG CASE.

Operator, 2026-08-17: *"If we have RTEP 2027, why are we not able to get RTEP 2028?"* **We can. It is
case 23.**

`ingest/load_pjm_queuescope_bq.py --list` enumerates **17 study cases**. We had harvested **case 4,
"2027 RTEP Base Case (Summer Peak)"**. The benchmark's PJM rows carry
`Final_2024 Series RTEP 2028 SUM_BD_02052026_TC2_PHII_Final` — which is

| case | label |
|---:|---|
| **23** | **2028 TC2 Phase II Case (Summer Peak)** ← theirs |
| 24 / 25 | 2028 TC2 Phase II (Light Load / Winter Peak) |
| 7 / 8 / 9 | 2028 RTEP Base Case (Summer / Light / Winter) |
| 4 | 2027 RTEP Base Case (Summer Peak) ← **what we had** |

⭐ **PROVEN, not assumed.** A 2-batch smoke test on case 23 returned **11,191 constraint rows over 50
buses**, and the row's own `case_label` reads *"2028 TC2 Phase II Case (Summer Peak)"*. **AEP has
1,826 buses in this case against 1,524 in case 4** — so the newer case is also 302 buses wider.

⛔ **THE TRAP THAT MAKES THIS FAIL SILENTLY: OWNER IDS ARE RENUMBERED PER CASE.**
AEP is **739** in case 4 and **1568** in case 23. Passing 739 to case 23 returns
`unknown owner id(s): ['739']`, loads **0 rows**, and exits **successfully**. A harvest that reports
success and writes nothing is the exact failure shape this project keeps hitting — **always
`--list --case <n>` first and take the owner id from THAT case.**

Two defects fixed in our own wrapper while doing this (`scripts/pull_pjm_injection.py` — `ingest/`
is not editable, and the wrapper pattern exists precisely for that):
1. **The checkpoint directory was pinned to the injection run.** Running a different case would have
   reused those checkpoints and skipped batches it had never done. Now keyed per (case, mode).
2. **The `_registry` method string hardcoded "case 4, mode INJECTION"** regardless of the actual
   arguments — a G16 violation, since the row could not re-run the work it described. Now built from
   the real arguments and carries a `RE-SCRAPE COMMAND`.

#### ⛔ G7d IS DEAD AS WRITTEN — CARTOVISTA IS BLOCKED, RE-TESTED 2026-08-17

**The backlog promoted G7d to "the single highest-value unbuilt item" on the strength of a registry
row reading `access: public`. That is the CATALOGUE row. The PROBE rows beside it record the
measurement, and rule 11 says re-test rather than inherit — so I did:**

| route | result |
|---|---|
| `ferc.cartovista.com/api/settings/miso/ferc` | **200** |
| `cloud.cartovista.com/.../Layer/{id}/mvt/8/12/5.pbf` | **200** — locations only, x/y, **no attributes** |
| `cloud.cartovista.com/.../Layer/{id}/geojson` | **403 — the wall reproduces** |

The registry's own probe rows already record `geojson 403, DataRows 403, dataQueryExecute 403,
joined-aggregate 403`, measured **with the full join contract in hand**. **The 691,523-row transfer
study and the 19,223-bus universe are not obtainable by any known route.** ⛔ Do not re-promote G7d
on the strength of the catalogue row again.

#### ⭐ G7k — WE ARE NOT MISSING COLUMNS. THE DPP-2025 SCHEMA IS PUBLIC AND WE HOLD EVERY MEASURE.

Operator asked four times whether we had scraped *"some, not all"* of the columns. **Answered
definitively 2026-08-17 with the publisher's own schema**, obtained from a route no prior session
had tried: `DataColumns` on each table's **own `systemIdentifier`** rather than the shared layer id.
It returns **200, public**.

| `MISO_TSA_2025-11-11` — their DPP-2025 study, 691,523 rows | our equivalent, held |
|---|---|
| `FCITC` | `mw_available` |
| `DFAX` | `percent_dfax` |
| `RATING` | `rate_base_mva` / `derived_rating_mva` |
| `CONSTRAINT` | `monitored_facility` |
| `CONTINGENC` | `cont_name` |
| `INI_LOAD` | `base_flow_mw` |
| `PER_LOADED` | `percent_loading_before` |
| `BUS_ID` · `KV` · `LATITUDE` · `LONGITUDE` | ✅ held (`MISO_POIs` carries 9 columns, all held) |
| **`SCENARIO`** · **`YEAR`** | ❌ **not held — the study dimensions** |

⭐ **So the gap was never columns.** We hold an equivalent for every measure their study publishes.
The difference is (a) the **model** — DPP-2021 vs DPP-2025 — and (b) the **scenario/year**
dimensions. Stop auditing our clips for missing MISO columns; audit the *model vintage* instead.

#### ⛔ G7l — THE CARTOVISTA WALL, MAPPED EXHAUSTIVELY. Metadata public, ROWS protected.

Operator, correctly on principle: *"we should be able to get around CartoVista, since they have to
derive that set from somewhere… they only extract the information, they do NOT create the study."*
**The principle holds — MISO authors DPP-2025 and CartoVista only renders it — but every CartoVista
route to the ROWS is shut.** Measured 2026-08-17, including two routes no prior session tried:

| route | result |
|---|---|
| `api/settings/miso/ferc` | **200** — and it yields the real study GUID `83dffd45-…`, `studyTitle: "DPP2025"`, `maxInjection: 5000` (matching the benchmark's 5,000 MW ceiling exactly) |
| ⭐ `api/v2/maps/{poiAnalysisMapId}/details` | **200 — NEW.** Confirms `MISO_POIs_2025-11-11` **19,223**, `MISO_TSA_2025-11-11` **691,523**, `MISO_GIQueue` 3,253 |
| ⭐ `api/v2/DataTable/{systemIdentifier}/DataColumns` | **200 — NEW.** Full schema, both tables (see G7k) |
| `api/v2/DataTable/{systemIdentifier}/DataRows` **GET** | **405 Method Not Allowed** — so POST is the correct verb |
| `api/v2/DataTable/{systemIdentifier}/DataRows` **POST** | **403** |
| `Layer/{id}/geojson` · `dataQueryExecute` · joined aggregate | **403** |
| MVT tiles | 200, **x/y only, no attributes** |
| `ferc.cartovista.com/api/poi-analysis/miso/{study}` | **500** (route exists, wrong shape) |

⚠ **The earlier 403s were partly an artefact of using the shared LAYER id.** Against each table's own
`systemIdentifier` the metadata opens up — which is how the schema above was obtained. **The rows
still 403 under the correct POST verb**, so the conclusion stands, but it now rests on the full set
of routes rather than a partial one.

⭐ **THE LEAD THAT REMAINS, and it follows the operator's own logic:** go to **MISO**, not the
renderer. MISO authors DPP-2025 and FERC Order 2023 obliges publication. Unexplored: MISO's OASIS
document tree, DPP-2025 cycle study reports, and `cdn.misoenergy.org` file listings.
⛔ Do NOT re-probe CartoVista — it is now exhaustively mapped, twice.

#### ⭐⭐ G7m — DPP-2025 FINAL RESOLUTION (2026-08-17). The lead above was chased to its end.

**Full route matrix in `docs/MISO_DPP2025_ROUTE.md`. This is the verdict.** The MISO-authored lead
paid off — partially, and the rest is a *legal* wall, not a technical one.

**✅ WHAT LANDED — the cost/upgrade dimension, obtained the right way.**
- **Route (public, proven, re-runnable):** MISO's own open Elasticsearch document index →
  `POST https://www.misoenergy.org/api/find/Optics_Models_Find_RemoteHostedContentItem/_search`
  (anonymous, HTTP 200) → CDN `https://cdn.misoenergy.org/{FileName}{ObjectId}.{ext}`.
- **The document:** `GI-DPP-2025-ALL_SIS_Ph1_FINAL_v1.0_PUBLIC_…zip`, HTTP 200, dated 2026-04-01.
  Vintage proven three ways (index tag `studycycle="2025 Cycle"`, title, scope 351 requests /
  58,730 MW ERIS).
- **Loaded:** `in_miso_dpp2025_ph1_project_costs` — **202 projects, $29.52B** Phase-1 network-upgrade
  cost, ERIS/NRIS MW, fuel, service type. Registry row same-run. Loaders
  `scripts/pull_miso_dpp2025_study.py` (`--discover/--load/--smoke/--dry-run`).
- **Indiana relevance (measured):** joins by J-number to `in_queue_miso` — **21 of our 27 Indiana
  DPP-2025 projects carry a Phase-1 upgrade cost**, totalling **$1.7B**, avg $81M/project, avg
  **$373/kW** of ERIS granted. e.g. J3999 Vermillion (Sugar Creek 345 kV, gas, 1,540 MW) → $345M.
  ⭐ This is the upgrade/cost dimension the benchmark's second file has and we had NOWHERE.

**❌ WHAT STAYS WALLED — bus headroom, and it is CEII, not a scrape gap.**
- The constraint-level rows (FCITC/DFAX/contingency/rating) are **CEII by the study's own words**:
  *"detailed in Appendix C (CEII) — ERIS Results and Appendix D (CEII) — NRIS Results."* Appendices
  A–H all CEII; only I is public.
- ⭐ **CartoVista tested from INSIDE the viewer's own anonymous session** — `fetch()` run in-page,
  carrying its cookies and same-origin referer — and POIs/TSA/GIQueue each **still returned 403
  `ProtectedData`**. So a logged-in-looking session does not unlock them; the protection is
  server-side per table. The viewer renders them via `dataQueryExecute`, which returns 200 but an
  **AES body keyed server-side** (not the giqueue `Salted__` scheme).
- **FERC docket ER24-2046** settles the frame: tariff GIP §6.1.1 makes the per-POI metrics *"publicly
  posted, without a password or a fee"* **as interactive query responses only — bulk export is not
  owed**, and CEII gating applies to the base-case models (GIP §2.3). So the wall is lawful and
  deliberate, and ⛔ **it is not to be circumvented.** Record BLOCKED and stop.

**THE TWO LEGITIMATE RESIDUAL PATHS to the actual headroom numbers:**
1. ⭐ **Model it ourselves.** MISO publishes the recipe: **BPM-015-r33 §3.1.1** (FCITC analysis, 3%
   DF cutoff, transfer from each bus decremented by existing + queued generation). We hold the
   DPP-2021 constraint inputs *and* the published DPP-2021 capacity GeoTIFF (3.39M pixels) to
   **validate the method against** — if our recreation reproduces MISO's own 2021 surface, the
   method is proven and only vintage separates us from the vendor. **This is the path forward.**
2. **Wait for DPP-2025 Phase 2** (~April 2027 per the July-2026 IPWG schedule).

**⚠ HONEST PARITY CEILING with the 2021 data we hold (measured, tell the operator plainly):**
| dimension | how close to the vendor |
|---|---|
| schema + columns | ~90% reproducible |
| **method** | fully reproducible **and validatable** against the 2021 GeoTIFF |
| **PJM numbers** | **near-exact** — case 23 (2028 TC2 Phase II) *is* their PJM case |
| **MISO numbers** | method-faithful but **vintage-divergent — CANNOT match** (ours 0.16% vs their 39.3% tier-0; that gap is DPP-2021 vs DPP-2025 and no method-tuning closes it) |
| upgrade tiers 1–4 | ❌ not reproducible (the tier ladder is the CEII upgrade study) |
| cost | ✅ 21 Indiana projects with real Phase-1 cost; ❌ not their per-tier ladder |
The strongest *truthful* claim is **not** "we match Orennia" — it is *"our MISO figures are MISO's
own current public position; the vendor holds a CEII-gated 2025 cycle with no open route."*

**⭐ G7n — DPP-2023, recorded 2026-08-17 (operator asked; do NOT reopen the headroom hunt for it).**
- **Headroom:** almost certainly walled the SAME way — CEII is not vintage-specific, and MISO's
  pattern is *old cycle open on giqueue (2021), newer cycles gated on CartoVista (2023, 2025)*. Low
  probability of an open route; not worth a hunt.
- **Cost:** ✅ **worth a cheap check** — a 2023 SIS almost certainly exists on the same find→CDN
  route, and **unlike 2025 its Phase 2 report is already published**, so 2023 could give a *fuller*
  (Phase 1 + 2) cost picture. One API call via `pull_miso_dpp2025_study.py --discover` with the 2023
  cycle filter. Grab-while-here, not a reason to reopen headroom.
- ⚠ Even a perfect DPP-2023 would not match the vendor either — *they* use 2025. It trades one
  non-matching vintage for a closer one.

#### ⭐ G7j — OUR MISO DATA IS NOT STALE. **MISO'S PUBLIC TOOL IS.** Measured live 2026-08-17.

This overturns how G22-B2 and G26 have framed the problem for two sessions. We have been recording
"our case is four cycles stale" as **our** defect. It is not ours.

**`https://giqueue.misoenergy.org/PoiAnalysis/PoiAnalysisConfig.xml`, fetched live, still reads:**

> *"The tool is currently using the models and inputs from the **DPP-2021-Cycle**."*

Our pull is dated **2026-08-06**, eleven days old, and it faithfully carries what MISO publishes
**today**. So:

| claim | verdict |
|---|---|
| "our MISO data is four cycles stale" | ❌ **wrong framing** — it is current for the source |
| "MISO's *public* POI tool is four cycles behind" | ✅ **that is the actual fact**, from the publisher's own disclaimer |
| "DPP-2025 is obtainable if we write a loader" | ❌ it is published only via the CartoVista FERC heatmap, which **403s on every tabular route** (re-tested today, G7d) |

⭐ **What this means for the application, and it is defensible rather than apologetic:** our MISO
headroom *is* MISO's own current public position. The gap to the vendor is not sloppiness on our
part — they hold a DPP-2025 mitigated case that MISO does not serve publicly. **State that on the
face of the number** ("MISO's published POI model, DPP-2021 cycle") rather than implying we are
behind on work we could have done.

⛔ **Stop re-recommending "get the mitigated case" as though it were engineering time.** Until a
route to DPP-2025 exists, the MISO half of parity is **not achievable**, and recording it as
NOT-ACHIEVABLE-AS-SPECIFIED with this measurement is the honest close — the same treatment §13(5)
and §13(8) already get.

#### ⚠ G7i — NEITHER OF OUR BUS DATASETS IS THE FERC ORDER 2023 STUDY (operator was right)

Operator: *"We do have PJM datasets, but they were NOT derived from the FERC 2023 study, which
Orennia leverages."* **Correct, and it extends to MISO as well.** Measured from our own columns:

| | ours | theirs |
|---|---|---|
| **MISO** | `giqueue.../POI/api/poi_mf`, **`DPP-2021-Cycle`** — MISO's *legacy* POI Analysis viewer | `DPP-2025-Cycle_SUM_D_ERIS-mitigated_Final`, MTEP-2025 upgrades as of Jan 2026 |
| **PJM** | QueueScope **"2027 RTEP Base Case (Summer Peak)"** | **"2028 … TC2 Phase II"** → ✅ **now obtainable, case 23 (G7h)** |

So the PJM half of the vintage gap is **closable today**. The MISO half is not, because the DPP-2025
mitigated case lives only behind the CartoVista wall above.

⚠ **The MISO POI API returns AES-ENCRYPTED payloads** — the response body begins `U2FsdGVkX1…`,
CryptoJS's `Salted__` marker, decrypted client-side. **Column completeness therefore cannot be
tested by fetching the endpoint**; it has to be tested through the loader that decrypts it. This is
why a naive key-diff against the live payload returns nothing and must not be read as "we hold
everything."

#### G7d (original) — MISO BUS HEADROOM: A ROUTE EXISTS, AND IT IS ALREADY CATALOGUED

Operator, 2026-08-17: *"Is there any other methodology we can use to obtain MISO bus headroom? That
is a crucial necessity of our tool."* **Yes — three, and the best one is public, catalogued and
never built.** The agent's BLOCKED verdict was correct about the `poi_mf` API specifically; it is
not the whole picture.

**ROUTE 1 — MISO POI Analysis Heatmap (CartoVista/FERC, DPP2025). ⭐ Do this first.**
Already in `energy.registry_sources` as **`CATALOGED-not-built`, access `public`**:
`https://cloud.cartovista.com/miso/ferc/poi-analysis-map`
It provides, per the catalogue entry: **19,223 POI buses with kV and lat/lon**, and a
**691,523-row transfer-study table with FCITC MW per bus × constraint × contingency × scenario ×
year**. FCITC (First Contingency Incremental Transfer Capability) is a **transfer** quantity, i.e.
source→sink, which is the shape that carries direction. **19,223 buses against the 642 we currently
hold located is a 30× coverage increase**, and it is a FERC Order 2023 heatmap, published because
the rule requires publication. This is the single highest-value unbuilt item in the estate for this
application.

**ROUTE 2 — derive withdrawal from the study data we already own.**
`in_miso_poi_300mw` (40,007 rows) carries `DFax`, `BaseFlowMw`, `ContFlowMw`, `RateBaseMva`,
`RateContMva`, `MwImpact`, `MonitoredFacilityName`, `ContName` — a complete DC-power-flow constraint
table. Headroom in a direction is `min over (facility, contingency) of (rating − base flow) / |shift
factor|`, and the shift factor **flips sign** for withdrawal. That is exactly how the vendor extract
is built (its columns include `Flow Direction` and `Shift Factor`).
⚠ **Honest limitation, which must be stated wherever the number appears:** MISO returned only the
constraints that bind for *injection* at each POI. Withdrawal loads a *different* set of facilities,
and those rows are not in the payload. A derived withdrawal figure is therefore an **upper bound
over an incomplete constraint set** — a labelled estimate, never a published figure, and it must
never be styled like one.

**ROUTE 3 — utility load hosting-capacity maps** (the pattern the operator's Illinois dashboard uses,
where ComEd/Ameren publish load and generation hosting capacity):
- `I&M/AEP hosting capacity map (PROD_MI_HC_GRID)` — **`done`, public, ArcGIS**, Indiana. Already
  acquired nationally as `hca_aep_multistate_repull` (IN/MI/OH) and **not clipped into this app.**
- `Duke Energy Indiana Grid Hosting Capacity` — BLOCKED · `der_in_duke_energy_indiana` — blocked ·
  `Duke Indiana / NIPSCO / AES Indiana / CenterPoint IN hosting-capacity maps` — blocked/gated.
  ⚠ **Re-test these.** Standing rule 11: *a wall is an observation, not a property of a host* —
  four robots-403 walls did not reproduce on re-test.
- Precedent that it is publishable: `Duke Energy Carolinas POI Heatmap` is **BUILT+LOADED** and
  provides *"transmission bus WORST-CASE TRANSFER LIMIT (trlim MW) under N-1, **injection AND
  withdrawal***". Duke publishes both directions in the Carolinas — so ask why not in Indiana.

**Also noted, and NOT usable in the tool:** `Transect interconnection data` — BUILT+LOADED but
**NDA-gated** — provides buses, interconnection capacity/cost, substations and lines with power-flow
and N-1 for Indiana (MISO/PJM). Same category as the Orennia extract: **benchmark only, never a
source.**

#### G7c — AGENT INTERIM REPORT, 2026-08-17 (verify before building on it)

| deliverable | state |
|---|---|
| `in_miso_poi_ladder` | **230,286 rows** — MISO injection, **all six rungs** |
| `in_bus_headroom_miso_ladder` | **3,852 rows** — per-bus rollup of the above |
| `in_pjm_queuescope_injection` | **47,700 rows** — PJM injection, all six rungs on 25 AEP buses; **full-footprint harvest still running** (checkpointed and resumable) |
| **MISO withdrawal** | ⛔ **BLOCKED, recorded with verbatim publisher wording** — as anticipated: `poi_mf` is a *generator* tool and no public load-side equivalent surfaced |
| `docs/RTO_DIRECTIONS.md` | written, with the LANDED/BLOCKED matrix |
| `_registry` | rich rows written for all three tables (G16 compliant) |
| boundary | clean — no `energy.*` table touched |

⚠ **These are NOT yet wired to any page**, which is why the checkpoint reads *270 of 273 objects
reaching a surface*. That failure is the check doing its job, not a regression. **Do not wire a
table whose harvest is still in flight** — a half-loaded table on a UI is worse than one nobody can
see. Wire after the agent's final report confirms the AEP footprint is complete.

⚠ And per the standing rule: **verify, do not inherit.** Re-measure row counts and the direction/
`request_mw` columns directly before the screener or dossier depends on them.

#### G7b — ⛔ CORRECTED: THE LADDER PREMISE IS MEASURABLY FALSE. Headroom is request-INVARIANT.

**I recorded the operator's ladder instruction as a physical claim and it does not hold.** The
premise below — *"a bus with 800 MW available to a 100 MW request may have far less available to a
1,000 MW request"* — was disproved in both RTOs by the agent, and separately by me.

| RTO | proof | result |
|---|---|---|
| **PJM** | same 25 AEP buses, case 4, injection, at 100/300/500/1000/2500/5000 MW, deduped 1:1 on (bus, facility, contingency) | **`available_mw` byte-identical at every rung — max delta 0.0** on 7,950 rows each. `impact_mw` scales *exactly* 3/5/10/25/50× |
| **MISO** | `pMaxValue` identity across two independent harvests | **`PMax(X) == min(PMax_true, X)` — a REPORTING CLAMP, not a study input.** 38,381 of 38,381 distinct (POI, facility) keys, **zero violations**. Confirmed again here: `headroom_mw_unclamped` is invariant at 1.27 avg across all six rungs while the clamped value climbs 0.16 → 0.47 → 0.78 → 1.27 |

**Consequences:**
1. **One harvest per direction answers every rung.** The ~22 hours of extra PJM scraping the 6×2×2
   grid implied buys **zero** new information in the headroom column. Do not run it.
2. **The true headroom is the UNCLAMPED value.** ⚠ I then shipped a *second* bug: after fixing
   median→actual I took the **rung-100 clamped** value as the headline, which reported **100 MW for
   `J1724 POI 138` whose real headroom is 815.3 MW** — an 8× understatement on the single Indiana
   POI that has capacity. Fixed; `export_grid_siting.py` now publishes `headroom_mw_unclamped` plus
   a `fits` map per request size.
3. **What the request size actually decides is FIT, and that is the siting signal.** PJM, 25 buses:
   6 fit 100 MW, 1 fits 300 MW, **0 fit 500 MW and above.**
4. `_invariant_columns` vs `_probe_dependent_columns` were already recorded in the publisher's own
   harvest metadata. We had the answer and did not read it.

#### G7e — INJECTION AND WITHDRAWAL AGREE ON ZERO BUSES (measured, not argued)

200 AEP buses measured in **both** directions: **0 equal.** Injection is larger on 129, smaller on
71. There is no offset and no ratio — the disagreement is unpredictable per bus. This is the
empirical backing for the standing rule that one direction must never substitute for the other:
doing so would be wrong on **100% of buses**, in a direction you cannot guess.

#### G7f — MISO Indiana injection is structurally near-zero *in the DPP-2021 case*

641 of 642 POIs read zero at **every** rung including 100 MW; the sole exception is
`J1724 POI 138` at **815.3 MW**. So the 300 MW probe was not hiding headroom — in this model there
is none. ⚠ **This does not overturn G22-B2:** the vintage gap (ours DPP-2021 vs the baseline's
DPP-2025 with MTEP upgrades as of Jan 2026) remains the best explanation for why the vendor sees
39.3% of buses with tier-0 headroom where we see 0.16%. Both statements are true: our model is
correctly reporting near-zero, *and* our model is four cycles stale.

#### G7g — ⛔ CARTOVISTA IS A HOST, NOT A GATE. A trial buys nothing.

Operator asked what a CartoVista free trial would immediately address. **Nothing.** CartoVista is
the mapping *platform* MISO publishes on — `cloud.cartovista.com/miso/ferc/poi-analysis-map` is
MISO's FERC Order 2023 map hosted there, and the registry records `access: public`, because Order
2023 **requires** publication. A CartoVista subscription sells their GIS software; it confers no
privileged access to MISO study data, because there is no access gate to pass.
**The blocker on G7d is engineering time to write the loader, not a credential.** Do not buy a
trial for this.

#### G7b (original text, retained for the record — its premise is disproved above)

**Headroom is a FUNCTION of the request size, and we harvested exactly one rung per direction:**
MISO injection at `pMaxValue=300` only; PJM withdrawal at `desired_mw=100` only. A bus with 800 MW
free to a 100 MW request may have far less free to a 1,000 MW request — and a hyperscale campus is
a 300–1,000 MW ask. **A single-proxy answer misrepresents precisely the size class this application
exists to serve.**

Ladder to acquire: **100 / 300 / 500 / 1000 / 2500 / 5000 MW**, for BOTH directions, BOTH RTOs —
6 × 2 × 2 = up to 24 cells. Deliverable is a **cell-by-cell LANDED / BLOCKED / not-offered matrix**
in `docs/RTO_DIRECTIONS.md`; partial coverage is expected and fine. Request size must be a COLUMN
(`request_mw`), never one table per size, so the existing 300 and 100 rungs fold in rather than
duplicate. Sent to the in-flight agent as a scope extension.

### G22 — 🔴 THE HEADROOM METRIC WAS WRONG, AND THE CALIBRATION GAP IS NOW MEASURED

Operator, 2026-08-17: *"we use worst, median, and best case scenarios, and we should really only be
presenting the 'actuals' as Orennia does"*, then *"we should be using Orennia as the baseline that we
are attempting to recreate, so ensure that our numbers largely resemble theirs."*

**A. The defect, found and fixed same-day.** `export_grid_siting.py` and
`build_screener_candidates.py` both published **`median_mw`** as the headline MISO figure.

⛔ **Headroom is a MINIMUM over binding constraints. A median over the constraint set is not a
weaker version of it — it is a different quantity.** A median mixes the one facility that sets the
answer in with dozens that do not bind at all.

| MISO injection, 642 Indiana POIs | value |
|---|---|
| `median_mw` — **what we published this morning** | **1,194 MW** average |
| **actual** (min over binding constraints) | **0.2 MW** average |
| POIs with actual headroom > 0 at a 300 MW request | **1 of 642** |

Cross-checked against the pre-existing `in_bus_headroom_300`: **both say 1 of 642**, so the ladder is
faithful. Cause: `facilities_at_zero` averages **15.8 of 59.8** monitored facilities — the binding
constraints are already at their limit in the base case. The worst/median/best triple was never a
methodology; it was a **workaround for a degenerate probe** run at `pmax_request=99999` that returned
`worst_mw=0` on ~88% of POIs. Fixed: the headline now comes from `in_bus_headroom_miso_ladder` (the
actual at each request size); worst/median/best survive only as `ctx_*` fields, never as the answer.

**B. Calibration against the baseline — we are now directionally right and still far too pessimistic.**

Their Indiana-heavy extract (constraint areas DEI, IPL, NIPS, SIGE), 2,023 buses, both directions,
upgrade tiers 0–4:

| direction | tier | median MW | buses with >0 |
|---|---|---:|---:|
| Injection | **0** | **0** | 795 of 2,023 (**39.3%**) |
| Injection | 2 | 68 | 1,187 |
| Injection | 4 | 179 | 1,862 (**94%**) |
| Withdrawal | **0** | **0** | 691 of 2,023 (34.2%) |
| Withdrawal | 4 | 71 | 1,330 |

✅ **Their tier-0 median is also 0 MW** — so ~0 is the honest tier-0 answer and the 1,194 MW we were
publishing was indefensible.
❌ **But they show 39.3% of buses with some tier-0 injection headroom and we show 0.16%.**

**B2. ⭐ ROOT CAUSE OF THE ZEROS: OUR STUDY IS FOUR CYCLES STALE. AUDITED 2026-08-17.**

Operator: *"I positively know that Orennia would never have a bus headroom value of 0.2 MW, so this
should be audited."* Audited. (Precision first: **no individual bus reads 0.2 MW** — that is the mean
over 642 POIs of which 641 are exactly 0.0. The objection still lands.)

First hypothesis — that they run a *greenfield* case while we run the queue-stacked case — is
**DISPROVED**. Their `Interconnection Scenario` is `ISO Base Case` and their
`Generator Modeling Assumptions` is *"Active queue generators up to **DPP 2025** as of Phase 1"*.
That is the same MISO DPP family we use. Same question, same model type.

**The difference is VINTAGE:**

| | powerflow case | transmission modelling |
|---|---|---|
| **ours** | **`DPP-2021-Cycle`** | (2021-era) |
| **theirs** | **`DPP-2025-Cycle_SUM_D_ERIS-mitigated_Final`**, study year 2030 Summer Peak | **"MTEP 2025 Upgrades as of Jan 2026"** |

**Why a 2021 vintage pins everything at zero:** a DPP-2021 queue stack models the grid as saturated
by every project queued in 2021 — the large majority of which have since **withdrawn** — while
omitting four years of MTEP transmission upgrades that have actually been built since. The result is
a model of a grid that is full of generation that never got built and missing wires that now exist.
That is not a wrong calculation; it is a correct calculation on a stale case.

**Solution, and it is already on the list: ⭐ G7d.** The catalogued-but-unbuilt
**MISO POI Analysis Heatmap (CartoVista, FERC) is explicitly `DPP2025`** — the *same cycle* the
vendor uses — with 19,223 buses. So G7d is not merely a 30× coverage upgrade, **it is the vintage
fix and the calibration fix at once.** Promote it to the top of the grid work.

⚠ Until it lands, any MISO headroom figure we show must carry its vintage on the face of it. A
2021-cycle zero presented without that label reads as *"this bus is full"* when it means *"our model
of this bus is four years old"*.

**C. The gap is the UPGRADE TIER, and we have no such dimension.** Their capacity climbs
0 → 8 → 68 → 114 → 179 MW as tiers 0→4 are allowed; at tier 4, 94% of buses have headroom. Our
single figure is effectively *tier 0, over only the constraint set MISO chose to publish*. Three
things close it, in order:
1. **Add upgrade tiers** — "what could this bus take if we paid for N upgrades" is the question a
   developer actually asks, and it is where essentially all the capacity is. Cost and lead time per
   tier come from their cost file's shape (`Total Upgrade Cost ($MM)`, `Min/Max Lead Time (Years)`).
2. ⭐ **G7d CartoVista** — 2,023 of their buses vs our 642 is a 3× coverage gap; CartoVista offers
   **19,223**.
3. **Derive withdrawal from DFAX** (G7d route 2) so the load direction exists in MISO at all.

⚠ **Do not "tune" our numbers toward theirs.** The target is to reproduce their *method* — actual at
a stated request size, per direction, per upgrade tier — and let our numbers land where the public
data puts them. Matching an output without matching the method is fitting to an answer key.

### G21 — tables must answer "so what?" (operator, 2026-08-17)

*"Tables like the ones attached are great, but they aren't actionable. We need to explain why it
matters for a DC or BESS developer, and organize the data in an actionable way — how would the user
actually act on this data?"* Examples supplied: the **reliability (SAIDI/SAIFI)** table and the
**gas capacity at Indiana borders** table.

Both are raw dumps. Neither states what a developer should do:

| table | what it shows now | what it must answer |
|---|---|---|
| **Utility reliability** | `SAIDI 2,766 · SAIFI 4.61` with no units, no expansion, no ranking | **SAIDI is minutes without power per year.** 2,766 min = **46 hours/yr** — that utility cannot support a data centre without serious on-site backup, while AES Indiana's 467 min ≈ 7.8 h/yr can. Rank utilities, band them against uptime tiers, and say what the difference costs in backup generation |
| **Gas capacity at borders** | the same `2,000 MMcf/d` repeated once per year, 1,017 rows | **Can I run on-site generation here?** A 300 MW gas plant needs roughly 50 MMcf/d. Show capacity *near the site*, how much is already contracted, and how many MW it could actually support |

### ⛔ G21 IS PROJECT-WIDE. THE TWO TABLES WERE EXAMPLES, NOT THE SCOPE.

Operator, 2026-08-17, twice: *"My SAIDI/SAIFI comment was largely meant as an example that should be
completed throughout the project, not just for that table"* and *"ALL of the tables and data that we
either show on the map or through tables, charts, or the screener should all answer the 'so what'
question, not just the two charts."*

**THE BAR — every rendered surface must carry all five:**

| # | requirement |
|---|---|
| 1 | **A one-line "why this matters to a data-centre / BESS developer"**, in the reader's terms |
| 2 | **Units on the number, and every acronym expanded** (G8). `SAIDI 2,766` → `32.0 hrs off per year` |
| 3 | **An ordering or banding that makes the good and bad ends obvious** — worst-first, or an explicit band |
| 4 | **A translation into the reader's decision unit** where one exists — MMcf/d → MW supportable; minutes → hours of backup; acres → MW at a stated density |
| 5 | **A link back to the sites it affects**, or a statement that it is context only |

**And the negative test: a surface that cannot answer "so what?" comes out.** Volume is not value —
the gas table shipped **1,017 rows that said about 40 distinct things** because the source repeats
each route once per year.

#### The inventory. Every surface, and its state. ❌ = not yet done.

| surface | where | state |
|---|---|---|
| Statewide demand chart | Market | ✅ units, full range, plain reading |
| Generation (emissions-monitored) chart | Market | ✅ + partial-final-period guard |
| Utility reliability | Market | ✅ hours not minutes, worst-first, banded to backup implications |
| Gas border capacity | Market | ✅ latest year per route (1,017→30), translated to MW supportable |
| Gas operationally-available capacity | Market | ✅ **2026-08-17** — rows now translated; see the row below |
| Gas locations / OAC by county | Market | ✅ **2026-08-17** — translated into the decision unit and de-duplicated. ⭐ **It shipped 203 rows that said 28 distinct things** (the bulletin board re-posts each delivery point and our capture kept every posting) — the same volume-is-not-value defect the gas border table had at 1,017 rows for ~40 facts. Collapsed to **28 delivery points**, ordered best-first, each showing the high and low across held postings. New **≈ MW supportable** column: top point is Elkhart–Consumers Energy at 453,287 Dth/d ≈ **2,906 MW**. ⚠ **Two capture gaps found and stated on the card, not hidden:** (1) **no units column was captured** — iPost posts dekatherms/day and the magnitudes match, but the pipeline never says so in our own data, so the unit is labelled INFERRED and the MW column carries an `.est-badge`; (2) **`gas_day` is empty on every row**, so the card's old claim of *"latest held posting"* was not demonstrable and has been removed |
| Tariff cross-check, wholesale floor, eligibility | Market | ❌ — and see G17 |
| Delivered fuel cost · demand response · EQR filers · URDB tariffs | Market | ✅ **2026-08-17** — all four. **Fuel cost** gains a **≈ ¢/kWh fuel-only** column at a 6.5 MMBtu/MWh heat rate (coal $3.239/MMBtu → **2.11¢**), framed as the floor under any retail quote — a bundled rate near it is a modelling error, not a bargain. **Demand response** states both readings: headroom the utility can free without building, and a signal that an interruptible tariff may be offered — which suits a BESS and not an uptime-committed data centre. **EQR filers** relabelled as **counterparties, not sites** — a PPA call-list, context only. **URDB tariffs** says use it to choose a territory, never to price a deal: the rates are flattened, so the energy column is a **lower bound**, and a large-load tariff carries a MW floor you must clear. Zero and unparseable values render as *not measured here*, never as 0 |
| **Every map LAYER** — substations, lines, buses, queue, gas, territories, data centres, logistics, GHGRP, federal surplus, protected land, bonus geographies, nonattainment | Map console | ❌ **a legend is not a "so what"**; each layer needs why-it-matters and what to do about it |
| County shading metrics | Map console | ✅ **2026-08-17** — all five metrics now carry a **So what:** line in the reader's decision unit, saying what to DO and whether the shading can disqualify a site. ⭐ The operator's test case is answered head-on: **opposition intensity has NO unit** — it is an unbounded relative count of recorded objection activity, not a score out of ten and not a probability of refusal, so a 4 in Marion and a 4 in a rural county are not the same quantity. It changes **schedule and outreach budget, never buildability**, and is labelled context-only with a pointer to *what the county has DONE*, which is the one shading that can stop a project. `queue_active_mw` now states both readings (future supply AND competition for study slots, per G35) instead of only the flattering one; `class_union` says choose-where-to-look-never-rank; `ge25mw` is marked an upper bound on LAND that says nothing about grid delivery or buildable area after setbacks (G28) |
| Evidence panel rows | Map console | 🟡 sourced, not yet consequence-framed |
| Screener result columns | Screener | 🟡 grid/owner columns explained; acreage + capacity columns still need the G28 buildable caveat |
| Ordinances · county actions · receipts · disasters | Community | ✅ **2026-08-17** — the whole page swept: **8 surfaces, 8 “So what” blocks**, each stating whether it can disqualify a site or is context only. **County actions** is named as *the only thing on any page that can stop a project outright*, measured in council calendar. **Posture** carries the two rules that get inverted: a restriction outranks a development, and grey is **cannot assess, never calm**. **Ordinances** separates codified (design to it) from uncodified (diary it — it can change at one meeting), and flags that an all-blank district table is a prohibition written as silence. **Receipts** is reframed as the audit trail behind every county colour. **Disasters and storms** translate into insurance premium, structural spec and downtime rather than event counts. **Court activity** says it reads county economics and ⛔ can never identify a building |
| Owner-signal tables | Owner signals | 🟡 plain names shipped; consequence framing not |
| Legislative preview | Community | ✅ **2026-08-17** — framed as the one thing that can move every county at once (pre-emption, new county powers, sales-tax treatment), against the standing caution that **a bill is not law**: use it for what could change inside a development timeline, never as today's rules |
| Table inventory · estate census · wiring | Data & sources | ➖ internal by design — exempt, but say so on the page |

**Do not treat this as done until the inventory has no ❌.** It is a pass over every page, not a
fix to two cards.

### G20 — 100% PARITY with the vendor extract, and WHY the 42 are missing (operator, 2026-08-17)

*"If we do have data that is missing from our tables that they have within their tables, we should
analyze that and determine why that is the case, and determine possible solutions (e.g., I know we
use a deduping methodology, and maybe that removed some real observations)."* Then: *"Instead of
JUST chasing the >=345kV substation, we should strive for 100% parity... ~97.5% parity isn't enough,
and we should attempt to gain 100%, if feasibly possible."*

**Root-caused 2026-08-17. The dedup hypothesis is DISPROVED, and the cause is a taxonomy gap.**

Our clip is faithful: `energy.mat_grid_substations` holds **3,858 Indiana rows on 2,077 distinct
coordinates** — byte-identical in count to `in_substations`. Nothing was dropped by us, and the
duplicates (G19) are inherited from upstream rather than introduced. The 42 are absent from the
**upstream** table too.

| their `Type` | n | what it actually is |
|---|---:|---|
| **Tap** | **25** | a connection point *on a line*, not a substation |
| **Dead End** | **7** | a line-termination structure |
| Substation | 6 | ← **genuine substation gaps** |
| Not Available | 4 | 4 of these are `Osmunknown*` — OSM nodes their model kept |

**76% of the gap is not substations at all.** HIFLD and OSM catalogue *substations*; the vendor's
asset model catalogues *network nodes*. That is a definitional difference, not lost data — and it is
also why "dedup ate them" is the wrong diagnosis.

**Route to 100%, by class:**
1. **Taps and dead ends (32).** Derive them from topology rather than acquire them: a tap is where a
   line meets a line, and we hold `in_transmission_union` (3,737 segments with geometry). Compute
   line-to-line junctions and terminations, and register them as their **own asset class** — never
   merged into the substation table, or we recreate the taxonomy confusion in the other direction.
2. **The 6 typed `Substation`** — Aq3 Mp (Knox), Unknown174219 (Sullivan), Unknown176298 (Martin),
   Mccool Rd (Porter), New Haven (Spencer), and Pike County 765 kV. Chase individually against
   HIFLD/OSM upstream; these are the real gaps.
3. ⭐ **`Pike County 765kV` is `status = Planned`.** It is not a missing existing asset — it is a
   **future** one, and a 765 kV substation is a major siting fact. **It belongs to G15
   (future capacity), not to a substation backfill.** Worth its own look: a planned 765 kV station
   is exactly the kind of thing a hyperscale campus follows.
4. **The 4 `Osmunknown*`** — present in OSM, absent from our merge. Check whether the upstream OSM
   pull filtered them (unknown voltage is a likely culprit) — and recall the standing rule that
   unknown voltage must never be treated as 0.

**Parity is achievable.** After classes 1–3 the residual is 0, and the honest statement becomes
*"we hold 100% of their substations, plus taps and dead ends as a separate class"* rather than a
single blended percentage that hides a taxonomy difference.

### G18 — every chart must be labelled and explained (operator, 2026-08-17)

*"If we are intending to use charts like the ones attached, they should be labeled and explained
fully."* Supplied screenshot: the **Market & cost** page's two charts.

**The charts are `svgLine()` in `common.js`, and it draws a bare polyline — nothing else.** No axes,
no tick labels, no y-scale, no date range, no units on the plot, no gridlines. A reader can see that
something oscillates and cannot tell whether the range is 2 GWh or 20 GWh, or what period is
covered. The titles then compound it with unexplained acronyms — **FERC-714** and **CEMS** — which
is the same G8 failure that sent the operator to look up OAC.

Minimum bar for any chart we ship:
1. **Y axis with real values and its unit**, and an explicit statement of whether it is zero-based
   (`svgLine` currently scales to `max` with no zero, which visually exaggerates variation).
2. **X axis with the actual date range**, not an implied one — `svgLine` silently plots only
   `series.slice(-120)`, so a chart captioned "228 months" is **showing 120 of them**. That is a
   silent truncation on screen, and the caption actively contradicts the picture.
3. **A one-line plain-English reading** — what the reader should take from it.
4. **Acronyms expanded** on first use via the shared `GLOSSARY` (`FERC-714` = the federal hourly
   demand filing; `CEMS` = EPA's Continuous Emissions Monitoring System).
5. Source, row count and build date — ✅ these are already there and are good.

⚠ The `slice(-120)` truncation is the priority: it is not a styling issue, it is the chart showing
a different dataset from the one its own caption claims.

### G17 — TARIFF RATE ENGINE: designed 2026-08-17 against the CPS workbook. BUILDABLE, as a BRACKET

> **Operator, 2026-08-17:** *"leverage our tariff data (whether it be C&I or large load…) modelled out
> against the rate schedules, riders, and wholesale electric costs… whether the user is interconnecting
> at transmission, sub transmission, distribution… the load portfolio, energy factor load factor, and
> demand requirements (these could all be inputs)… Maybe the user has to choose the utility and the
> above attributes, or should click on the class-based schedule as it is listed… I imagine we will
> have to tweak this a couple of times before it can be seen by any user or management."*

#### ⭐ 1. The CPS workbook decoded — this is the spec, and it is a clean one

`CPS_35MW_Rate_Model.xlsx` is a two-tab component build-up. Copy its method exactly:

| step | formula |
|---|---|
| billing demand | `MW × 1000` (flat 24/7 load → monthly 15-min peak = nameplate) |
| annual energy | `kW × 8,760 × load factor` |
| fixed | `service availability $/mo × 12` |
| demand | `kW × (summer $/kW-mo × summer months + non-summer $/kW-mo × non-summer months)` |
| energy | `kWh × energy charge $/kWh` |
| **fuel adjustment** | `(actual unit fuel cost − the fuel base ALREADY EMBEDDED in the rate) × kWh` |
| riders | `amortisation $/kWh × kWh`, then sales tax on the taxed base |
| output | annual $ **and effective $/kWh, computed once per SERVICE VOLTAGE** |

Two ideas worth stealing outright. **The voltage delta is pure and small**: CPS transmission vs
distribution-primary differs only in the demand rate — $0.50/kW-mo → $210,000/yr on 35 MW, **1.3%** —
which is precisely the number you weigh against the capex of a 138 kV interconnection. And **the
answer is bracketed, never asserted**: wholesale floor → modelled tariff → realized class average.

#### 2. WHAT WE HOLD — measured, and better than the earlier note in `in_rate_component_gaps` implies

`energy.urdb_rates`: **38,730 rates across 2,829 utilities nationally**; the Indiana clip
`in_urdb_rates` is **969 rates across 70 utilities**, and it is a **faithful clip — 23 of 23 parent
columns, nothing truncated** (checked, because three separate surfaces lost columns that way today).

| the operator's input | do we hold the data to honour it? |
|---|---|
| **service voltage** | ✅ `voltagecategory` — Indiana: **Transmission 77, Primary 159, Secondary 172** rates (561 unclassified) |
| **demand requirement → tariff class** | ✅ `peakkwcapacitymin` / `peakkwcapacitymax`. **This is how "large load" is identified without guessing**: NIPSCO *Industrial Power Service* carries a **15,000 kW floor**, *High Load Factor Industrial* a **10,000 kW** floor, Richmond *Transmission Service* 10,000 kW |
| **load factor / load portfolio** | ✅ pure inputs → `annual kWh = kW × 8,760 × LF` |
| commercial vs industrial | ✅ `sector` |
| demand + energy price | ✅ `demand_rate_max_usd_kw`, `energy_rate_min_usd_kwh`–`_max_` (a RANGE, so the output is a range) |
| eligibility thresholds | ✅ `in_rate_eligibility` |
| wholesale floor | ✅ `in_rate_wholesale_floor`, `energy.iso_lmp` |
| realized $/kWh per utility × class | ✅ `in_eia861_sales` |
| **service / customer charge** | ❌ not in URDB's schema at all |
| **the rider stack** (FAC, RTO/PJM, DSM/EE, environmental) | ❌ `in_utility_tariff_riders` holds **3 rows, ONE utility, 1 with a rate** |
| **summer / non-summer split** | ❌ URDB is flattened on season |
| a named Indiana large-load tariff | ❌ `energy.dc_e3_largeload_tariffs` = **5 rows, 4 states, ZERO Indiana** |

⭐ **The gap is already quantified.** `in_rate_component_gaps` records: *"tariff-modelled energy+demand
gives **$0.0261–$0.0606/kWh** at 300 MW while the EIA-861 realized industrial average is
**$0.0804/kWh**. That delta IS the riders + fixed charges + ratchets."* The CPS Results tab reaches
the same conclusion from the other direction. **So the missing components are not unknown — they are
bounded**, and that is what makes a bracket honest rather than evasive.

#### 3. THE DESIGN — answering *"I'm not sure quite how to complete this action"*

**Inputs:** utility · load **MW** · **load factor** · **service voltage** (transmission /
sub-transmission / primary / secondary). Derive, never ask: `billing demand = MW×1000`,
`annual kWh = kW×8760×LF`.

**Class is DERIVED, not chosen — this is the answer to *"or should click on the class-based schedule"*.**
Do not make the user classify themselves. Run their demand against `peakkwcapacitymin/max` and
`voltagecategory` and **return the schedules they qualify for**, best-first, each showing its floor.
Show the ones they FAIL, greyed, with the reason — a developer at 8 MW learns that NIPSCO's large-load
rate needs 15 MW, which is itself a siting insight. ⛔ A kW floor is a **minimum to qualify, never a
cap** — that rule is already on the page and must not invert here.

**Output — three numbers and the NAMED gap between them, never one number:**
1. **Floor** — ISO wholesale energy-only at their kWh. No delivery exists at this price; below it, a quote is a modelling error.
2. **Modelled tariff** — `demand × demand_rate × 12 + kWh × energy_rate`, as a **range** (URDB gives min and max energy). Labelled **explicitly incomplete**: no fixed charge, no riders, no seasonal split.
3. **Realized anchor** — `in_eia861_sales` average for that utility and sector × their kWh: what comparable customers actually paid, riders included.

Then **say what the gap is**: *"the distance between 2 and 3 is the unpublished rider stack — for I&M
Industrial, roughly $0.02–$0.05/kWh."* A developer can act on a bracket with a named cause. They
cannot act on a fake point estimate, and a point estimate is what an itemised number would be.

**Voltage comparison** ships as the CPS delta — same load, same LF, one voltage against another — or
renders *not measured here* where no rate carries that `voltagecategory`. Never $0.

#### 4. THE ACQUISITION THAT UPGRADES THIS TO CPS GRADE
The Indiana tariff books — **public PDFs, not a gated source**. `in_rate_component_gaps` already
carries the first URL: **I&M IURC No. 20**,
`https://www.indianamichiganpower.com/lib/docs/ratesandtariffs/Indiana/IMINTB19_01-16-2023.pdf`.
The same is wanted for AES Indiana, Duke Energy Indiana, CenterPoint/Vectren and NIPSCO. Each yields
the customer charge, the seasonal demand split, per-voltage demand rates and the rider stack — every
❌ above. **This is the highest-value acquisition left on the market side**, and it converts the
bracket into the itemised CPS-grade model.

⚠ **Ship v1 behind the operator's own caveat** — *"we will have to tweak this a couple of times before
it can be seen by any user or management"* — so treat the first cut as internal until reviewed.

### G17 (original note) — §13(8) reclassified from "impossible" to ACQUIRABLE

Operator supplied `CPS_35MW_Rate_Model.xlsx` 2026-08-17: *"this is how the tariff costs should be
broken out for the estimates (leveraging the tariff rate schedule and riders to compute a highly
accurate estimate)... the schedules should change for a site depending on the max load,
interconnection type, etc. This should be fully explained in the dossier, and should be seen as
accurate in the Market and Rates section."*

⚠ **This supersedes a standing conclusion.** §13(8) has been recorded twice as *not achievable as
specified* because "no component-level Indiana tariff exists anywhere in the estate." That was a
statement about **what we hold**, and it was true — we hold `in_utility_tariff_riders` (3 rows) and
`in_dc_eei_tariffs` (5). It is **not** a statement about what is obtainable: Indiana tariff sheets
and rider schedules are public IURC filings. **Reclassify §13(8) from structural to acquisition.**

**The model shape to reproduce (from the supplied workbook):**

| stage | components |
|---|---|
| **Inputs** | load MW · load factor · actual unit fuel cost $/kWh · riders (regulatory-asset amortisation, sales tax) · hours/yr |
| **Load derivation** | billing demand kW = MW × 1000 · annual kWh = kW × hrs × load factor |
| **Rate components** (per tariff sheet) | service availability $/mo · energy charge $/kWh · **fuel base embedded in rates** $/kWh · summer vs non-summer month counts |
| **Per SERVICE VOLTAGE** ← the part we do not model at all today | demand $/kW-mo summer *and* non-summer, stated separately for **transmission** vs **distribution primary**, plus an **extra transformation adder** where more than one step-down is needed |
| **Annualised** | fixed = availability × 12 · demand = kW × (summer rate × summer months + non-summer rate × non-summer months) · energy = kWh × energy charge · **fuel adjustment = (actual fuel − fuel base) × kWh** · then riders |
| **Output** | total $/yr and **effective all-in $/kWh** |
| **Brackets** | wholesale-energy floor · industrial-retail ceiling · a non-comparable class shown as reference only |
| **Decomposition** | share of cost that is fixed / demand-shaped / volume-shaped |
| **Verify list** | the assumptions a user must confirm with the utility, printed *with* the number |

**Two things this makes non-negotiable:**
1. **Service voltage changes the answer, and it is a SITE attribute.** In the worked example,
   transmission service beats distribution primary by **$210,000/yr on 35 MW** — the same load, the
   same utility, a different interconnection point. So the rate schedule is a *function of* max load
   and interconnection type, exactly as the operator says, and a single blended $/kWh for a county
   is not a weaker version of this — it is a different and misleading quantity.
2. **The fuel adjustment is a delta, not a rate.** `(actual fuel − fuel base embedded in rates)`.
   Ignoring the embedded base double-counts fuel; in the worked example fuel adjustment is
   **$3.6M of $16.0M**, so getting it wrong moves the headline by more than 20%.

**Work:** acquire per Indiana utility (AES Indiana, Duke Energy Indiana, NIPSCO, I&M, CenterPoint,
IMPA, Hoosier, Wabash Valley) the large-load rate schedules with their **load-size eligibility
thresholds**, demand/energy components by **service voltage**, the embedded fuel base, and the rider
stack. Then build the calculator, surface it in **Market & cost**, and print the full breakdown
**plus the verify-list** in the dossier. ⛔ The existing flattened-URDB cross-check is an *ordering
hint* and must not be dressed up as this.

### G15 — FUTURE CAPACITY: the extraction dropped location, cost and dates (operator, 2026-08-17)

*"Future capacity comes from the grid plans (from the IURC) and the updates from the ISOs/RTOs in
Indiana; we should have these points plottable to some extent, and the only utility that we have
data for currently is AES... for the new ones they often give a vague location (e.g. north of the
business district in XXX) and should be plottable in a relative sense (see the Illinois tool)."*

**Partial correction, measured 2026-08-17.** We hold **six** utilities, not one:
NIPSCO 376 · AES Indiana (IPL) 102 · Duke Energy Indiana 57 · CenterPoint/SIGECO 35 ·
Indiana Michigan Power 31 · unattributed IURC IRP page 17 = **618 rows, 391 of them projects.**

**But the operator is right that almost nothing arrives, and the reason is worse than a missing
utility — the location extraction failed across ALL of them:**

| field on the 391 project rows | populated |
|---|---:|
| `county`, `city`, `location_text`, `line_endpoints`, `cost_usd_m` | **0 (0%)** |
| `substation_names` | 7 (2%) |
| `in_service_year` | 15 (4%) |
| `voltage_kv` | 326 (83%) |

`location_status` = **`neither` on 384 of 391**; only 7 are `joinable`. The schema has exactly the
right columns and the parser filled voltage while dropping location, cost and in-service date.
Downstream, `export_grid_sentiment.py` groups on `UPPER(IFNULL(county,'(UNLOCATED)'))`, so **all 618
rows collapse into a single `(UNLOCATED)` bucket** — which is why the app looks like it holds
nothing. ⚠ Also: `_registry` records `n_rows = 7` for `in_grid_plans` while the table holds 618.

**The work, in order:**
1. **Re-extract from the IURC TDSIC workpapers** (`document_url` is populated — the source docs are
   already identified). Recover `location_text`, `city`, `county`, `substation_names`,
   `line_endpoints`, `cost_usd_m`, `in_service_year`. This is the whole ballgame; everything below
   depends on it.
2. **Locate in two tiers, and never blend them:**
   - **existing assets** → join by substation/facility ID to `in_substations` (exact point);
   - **new assets** → a *vague* description ("north of the business district in Kokomo"). Plot as a
     **location-uncertainty ring**, sized by how well the location is actually known, exactly as the
     operator's Illinois dashboard does. ✅ **This does NOT breach the no-centroids rule:** a
     centroid is banned because it *impersonates* a precise point; a ring explicitly states "the
     asset is somewhere in here". Carry `location_grain` (site / substation / city / county) on
     every row and colour or size the ring by it.
3. **Fold in the ISO/RTO side, which we already hold and barely surface:** MISO MTEP
   `in_txexp_miso_mtep_appendix_a_status` (328) and PJM RTEP `in_pjm_rtep_upgrades` (15,443),
   `vw_pjm_rtep_upgrades_located` (932), `in_rtep_bus_summary` (79).
4. **Chase the utilities still absent** — Hoosier Energy, IMPA, Wabash Valley — and confirm the
   Duke/NIPSCO pulls are complete rather than a first page.
5. Fix the `(UNLOCATED)` roll-up once locations exist.

### G16 — RULE: an agent's registry row must be enough to RE-RUN the work (operator, 2026-08-17)

*"Make sure the agents update the registries so everything is trackable and documented (e.g., URL,
endpoint type, loader used)."*

**Binding on every agent brief from now on.** A `_registry` row that merely exists is not
compliant. Each row must carry:

| must record | example |
|---|---|
| exact source **URL / endpoint**, parameterised | `giqueue.misoenergy.org/POI/api/poi_mf?pMaxValue=300` — not the site's home page |
| endpoint **type** | `arcgis_feature_layer` · `json_api` · `html_page` · `xlsx_download` · `bq_clip` |
| **loader** used, re-runnable verbatim | the estate convention: a literal `RE-SCRAPE COMMAND: <command>` string |
| **parameters defining the slice** | `request_mw`, direction, RTO, state filter |
| **observed vintage** from the publisher payload | not the pull timestamp |
| row count and **what was excluded, and why** | |

Test: *could a stranger refresh this table from the registry row alone?* If not, the row is
incomplete. Sent to the in-flight agent as a standing requirement, including a back-fill of rows it
had already written.

### G13 — voltage: filter by it, colour by it, and AUDIT it (operator, 2026-08-17)

*"The substations/buses/transmission lines should also be filterable by kV and the transmission
lines should depict different colors based on their voltage — some mislabeled and should be
audited/fixed."*

Three separate pieces of work, and **the third is the one that matters most**:

1. **Filter by voltage** on substations, connection points (buses) and transmission lines. The
   screener already filters substations by kV; the map console does not filter any of the three.
2. **Colour transmission lines by voltage class**, the way the operator's own Illinois dashboard
   does. Today all lines render one colour, so a 765 kV backbone and a 69 kV lateral look identical
   — and they are not remotely the same siting fact.
3. ⚠ **Audit the voltage labels first.** Operator reports some are mislabeled. `in_transmission_union`
   is a HIFLD+OSM merge carrying `voltage_raw`, `kv` and `volt_class`, and the two publishers
   disagree about units and encoding (volts vs kilovolts, ranges, nulls). **Colouring by a field
   that is wrong just renders the error in high contrast** — and a mislabeled voltage propagates
   into the substation kV filter the screener already ships. Measure the `kv` distribution against
   `voltage_raw` per source, quantify the disagreement, then fix, then colour.

⛔ Do not treat an unknown voltage as 0 — `in_substations` already has blank-is-unknown handling and
"never treated as 0" is the standing rule. An unknown-voltage line needs its own colour, not the
bottom of the scale.

### G11 — sentiment statuses do not fully reach the map console (operator, 2026-08-17)

*"I do not believe that all of the sentiment statuses actually reach the map for the sentiment
section of the map console — it reaches the Community and Regulatory page, but doesn't make its way
to the map fully."*

**NOT YET VERIFIED — measure before fixing.** The suspicion is specific and plausible: the map's
`sentiment` preset and the `opposition_intensity` county shading read `county_context.json`, while
the Community page reads `receipts.json.gz` **and** `in_dc_actions_resolved`. Those are different
sources with different gating, so the two surfaces can legitimately disagree — which is exactly the
shape of the bug that showed 4 ordinances while 107 county actions existed. Compare the distinct
status/posture vocabulary reaching each surface and report the diff before changing anything.

### G23 — 49 PJM withdrawal buses are silently missing (operator, 2026-08-17)

`in_pjm_queuescope_aep` (303,671 rows) and its rollup `in_pjm_bus_withdrawal` (1,475 buses) are an
**incomplete harvest**. QueueScope offers **1,524** AEP (owner 739) POI buses in WITHDRAWAL mode —
the same count it offers in INJECTION mode — so **49 buses (3.2%) are missing** and are absent from
every withdrawal answer the application gives, with nothing on screen to say so.

⚠ This was first written up as a *direction difference* ("the bus lists differ by direction"). They
do not differ. Measuring is what caught it.

**Fix, in order:**
1. **Identify the missing buses.** Compare `DISTINCT bus_number` in `in_pjm_queuescope_aep` against
   the authoritative 1,524-bus list. **This becomes free** once the injection harvest completes —
   `in_pjm_queuescope_injection` then *is* that list, so the 49 fall out of a subtraction with no
   extra scraping.
2. **Re-run the withdrawal harvest.** Use the wrapper pattern in `scripts/pull_pjm_injection.py`,
   which imports the upstream loader and rebinds `DATASET`/`TABLE`/`CKPT` without editing
   `ingest/`. Point `CKPT` at the ORIGINAL `energy-platform/data/_ckpt_pjm_queuescope/` or accept a
   full 2–5 h re-harvest. ⛔ **RUN SEQUENTIALLY — never two QueueScope instances at once.**
3. Rebuild `in_pjm_bus_withdrawal` via `scripts/build_pjm_withdrawal.py` and refresh its registry row.

**Blocked as of 2026-08-17:** `pull_pjm_injection.py` is mid-harvest. Starting the withdrawal run
now would be exactly the collision the constraint forbids.

### G12 — 💧 WATER: county grain LANDED; the parcel-grain half is still open

**Built 2026-08-17.** `in_water_county` (92 of 92 counties) and `in_water_stress_basin` (34 basins).
Statewide withdrawal **7,176.7 Mgal/d**, of which **thermoelectric cooling is 3,822.0** — power
generation is already the largest water user in the state, which is the context a new large load
arrives into.

#### The water estate we hold — enumerate this before adding anything (G25)

| table | rows | state |
|---|---:|---|
| `nwi_wetlands` | 40,575,843 | already used for the wetland gate |
| `nhd_flowline` | 39,542,980 (**2,415,369 Indiana**) | 🔴 **not used** — rivers/streams |
| `nhd_waterbody` | 10,431,981 (**186,667 Indiana**) | 🔴 **not used** — lakes/reservoirs |
| `nfhl_flood_zones` | 5,554,986 | already used for the flood gate |
| `water_cwns_2022` + `vw_cwns_flow` | 1,118,786 / 340,457 | ✅ clipped |
| `water_aqueduct` | 68,506 (**34 Indiana basins**) | ✅ clipped |
| `fema_nri_counties` | 3,232 (**92 Indiana**) | ✅ drought risk clipped |
| `water_use` | 3,223 (**92 Indiana**) | ✅ clipped |
| `drought_by_state` · `water_drought_monitor` | small | 🟡 state grain — context only |
| `groundwater_sites` · `vw_state_water_dc_context` | 50 rows each | 🔴 **a COUNT, not the sites.** Indiana = **10,802 groundwater monitoring sites**, 165 data centres, 15.3 DCs per 1,000 sites (rank 12 for DCs, 39 for groundwater density). We hold **only the number** |

#### 🔴 NWIS / groundwater: searched for, and genuinely NOT held

Operator expected NWIS and groundwater data in the warehouse. **Measured: it is not there.** A full
sweep for `nwis|gw|ground|aquif|spring|depth|level|subsurf|geolog|soil` returns no USGS NWIS table —
the only groundwater objects are the two 50-row state-level rollups above, which carry a *count* of
Indiana's 10,802 monitoring sites and none of the sites themselves.

**What that means for siting:** we can say Indiana has a lot of groundwater monitoring and is a
data-centre-heavy state relative to it (rank 12 vs 39). We **cannot** answer *"is there groundwater
under this parcel, at what depth, and is the aquifer declining"* — which is the question a
DC using well water actually asks. `water_aqueduct.gtd_*` (groundwater table decline) is basin
grain and is the closest proxy we hold.

**Acquisition item:** USGS NWIS site inventory + groundwater levels for Indiana (public API,
`waterservices.usgs.gov`). ⚠ Per G25, re-check `energy.__TABLES__` and `registry_sources` first —
this sweep was name-based and a differently-named table could still exist.
| ⭐ **`echo_cwa_facilities`** | 524,512 (**13,217 Indiana**) | 🔴 **not used — and it is the discharge answer.** EPA ECHO Clean Water Act / NPDES permitted dischargers. **1,147 Indiana facilities carry a design flow, totalling 4,436 MGD.** The registry lists `epa_echo_dmr` as *"pending"* while **this is already loaded** |
| `epa_echo_dmr` | — | 🟡 `pending` — the per-outfall discharge *measurements*. The facility layer above is held; the monitoring series is not |

⛔ **AND `echo_cwa_facilities` IS ITSELF UNDER-CLIPPED — a G27 case, and a disabling one.**
Its 17 columns include **`faclong` but NO `faclat`**. Longitude without latitude cannot place a
single one of the 13,217 facilities on a map. Re-clip before building anything on it; a
"13,217 located facilities" claim would be false today.

#### ⚠ TWO TRAPS, BOTH SILENT, BOTH HIT ON THE FIRST BUILD

1. **`water_cwns_2022.facid` is NULL on every row.** The obvious join matched nothing and **all 92
   counties silently lost their wastewater data** while the script reported success. The real key is
   `af_nbr` → `facility_id`, keeping the 2-digit state prefix and dropping the next two characters —
   established by testing four candidate transformations, not by reasoning about the format.
2. ⭐ **`vw_cwns_flow` is one row per facility PER SURVEY YEAR** (`/contents/1984/`, `/1986/`, …),
   fan-out **9.79**. Summing across it adds each plant once per survey it ever appeared in and
   **inflates every flow roughly tenfold** — while looking entirely plausible. Fixed by taking the
   latest survey year per facility.

Also corrected: the first version hand-rolled its own CWNS aggregation and thereby **missed
`flow_existing_industrial_mgd`** — the most relevant flow for a data centre, which discharges as an
industrial user. G25 in its exact form: look at what we hold before rebuilding it badly.

#### ✅ G12c — PARCEL-GRAIN WATER **IS** LIVE, via the attributes. (Operator: *"can we not measure
against the attributes?"* — yes, and the answer was better than the blocker.)

`in_water_parcel`: **532,868 parcels, fan-out 1.000, 100% matched** to one of 34 basins.

| | |
|---|---:|
| parcels in a **high water-stress** basin | **57,380** |
| parcels in a **high groundwater-decline** basin | 2,708 |
| parcels straddling a basin boundary (**worst stress kept**) | 11,085 |

**How it works without NHD geometry.** `water_aqueduct` carries **`geometry_geojson`** and all 34
Indiana basins parse as valid GEOGRAPHY, so a parcel can be placed **inside a basin** by
`ST_INTERSECTS` even though no river can be measured to. Straddling parcels take the **worst**
stress — the same worst-wins logic as the county actions: a parcel spanning an easy and a contested
basin cannot be assumed to draw from the easy side.

⚠ **State plainly what this answers and what it does not.** It answers *"how contested is water
here, and is the groundwater declining"* — a real constraint that changes permitting difficulty. It
does **not** answer *"how far is the nearest river"*; that stays blocked below.

**And a second attribute route is open and unused:** `reachcode`'s first 8 digits are the HUC8
watershed, populated on **all 2,415,369 Indiana flowlines with zero missing (77 watersheds)**. So
rivers and lakes can be inventoried per watershed from attributes alone. The missing link is a HUC8
boundary polygon to place parcels in — **we hold none**; that is a smaller acquisition than
re-loading 39.5M NHD geometries.

#### ✅ G12d — DISTANCE-TO-WATER IS **UNBLOCKED AND BUILT**. Agent landed real NHD geometry.

`in_water_distance_parcel` — **532,868 parcels, fan-out 1.000, 100% with a water source within
10 miles.**

| | |
|---|---:|
| ⭐ **water physically ON the parcel (0.0 mi)** | **79,168** |
| within 1 mile | 430,928 |
| median distance | **0.38 mi** |
| nearest is a river / reservoir | 454,628 / 16,622 |
| nearest is a Great Lake (Lake & Porter) | 371 |

**This quantifies G29 exactly: 79,168 parcels have water on them, and the map's client-side
first-vertex method reports ~0.55 mi for every one of them.**

Sources: `in_nhd_flowline_geom` (160,128 named rivers) and `in_nhd_waterbody_geom` (6,415), both
with real `geog`. **Filtered on `water_role='source'`**, which drops 679 SwampMarsh polygons — a
wetland is something to permit *around*, not draw *from*, and counting it as supply would be the
opposite of the truth on the same parcel.

⚠ **Lake Michigan (57,743 km²) is a legitimate member** and dominates nearest-lake in Lake and
Porter counties. That is correct, not a bug — but it carries its own withdrawal compact, so
`nearest_is_great_lake` flags those 371 rather than letting a 0.2 mi distance imply an ordinary
permit.

⛔ **Written to a NEW table.** `in_water_parcel` holds water-STRESS scores and is consumed by the
front end; the earlier version of this script would have **overwritten it with an all-NULL distance
table** — it measured against `energy.nhd_*.SHAPE`, which is NULL on all 50M rows, so it matched
nothing and *failed without erroring*.

#### ⛔ G12b (historical) — why distance was blocked: **NHD HAD NO GEOMETRY**

Attempted 2026-08-17 on the operator's request. Built, ran, and returned a **confident zero** —
532,868 parcels, **not one within 10 miles of water**, in a state with 972,487 river segments. That
is the instrument, not the data, and measuring it found this:

| table | rows | rows carrying `SHAPE` geometry |
|---|---:|---:|
| `energy.nhd_flowline` | 39,542,980 | **0** |
| `energy.nhd_waterbody` | 10,431,981 | **0** |

**Both NHD tables are attribute-only. The `SHAPE:GEOGRAPHY` column exists in the schema and is NULL
on every row, nationally.** So the 2,415,369 Indiana flowlines and 186,667 waterbodies can be
counted and classified but **cannot be spatially joined to anything, by anyone, for any state.**
This is a platform-wide load defect, not an Indiana one — worth telling the platform session.

The empty result table was **dropped rather than kept**: 532,868 rows of NULL water columns
registered as a parcel-water answer reads as *"no Indiana parcel is near water"*, which is worse
than having no table at all.

**To unblock, in order:**
1. **Re-acquire NHD with geometry.** `registry_sources` records it as `bulk_gdb_zip` — the
   geometry is in the source and was dropped on load. This is a G27 case at 50M-row scale.
2. Alternative sources already held that DO carry geometry: `nfhl_flood_zones` (93 GB) and
   `nwi_wetlands` (231 GB) are used successfully for the flood and wetland gates, so geometry
   loading works in this estate — NHD specifically is the gap.
3. `echo_cwa_facilities` has `faclong` but **no `faclat`** — the same story on the discharge side.

**The script is written and correct** (`scripts/build_water_parcel.py`): exact
`ST_DISTANCE(parcel_geog, SHAPE)` so water crossing a parcel returns **0.0** — G29 done right — with
ftype decoded, a stated ≥10 ha lake gate, D85 excluded and fan-out asserted. It will work unchanged
the moment geometry exists.

#### 🔴 STILL OPEN — the parcel-grain half, which is the actual screening dimension

County totals tell you the context; they do not tell you whether **this parcel** can get water.
That needs NHD proximity, and we hold the data:

- **Water source** — nearest reservoir (ftype 436, 3,659 in Indiana), lake ≥10 ha (2,301 of 186,667)
  or named river (ftype 460). ⚠ **ftype 466 is swamp/marsh — a constraint, not a source**, and
  336/420/428/558 are ditches, conduits, pipelines and artificial paths. Decode the codes before
  screening on them.
- **Discharge** — `epa_echo_dmr` for permitted discharge volumes.
- Then a `nearest_water_mi` column beside `nearest bus`, subject to the **G29 distance fix**: use
  `ST_DISTANCE` against real geometry so a river crossing a parcel reads **0.0**, not 0.55.

### G12 (original note) — water data is in `energy` and reaches nothing (operator, 2026-08-17)

*"We also do not have water data currently hitting the app — this is located in energy BQ and should
be wired, as applicable."*

Water is a first-order siting constraint for a hyperscale data centre (cooling), so this is a
missing **screening dimension**, not a missing layer. Work: enumerate the water objects in
`energy`, clip the Indiana slice into `indiana_app` (**clip, do not duplicate** — see *"Should we
duplicate the ~140 energy source tables? No"* below), register it, export it, and surface it as its
own step in the screener and its own section on the map console. Apply the G8 plain-language rule:
whatever the publisher's acronyms are, they get expanded.

### G9 — date-gate the signals, but ONLY the ones that are events (operator, 2026-08-17)

*"We should really only take the SI events after Jan 2020, so the signals that are time-dependent,
and maintain the within-X-days filter... however, a remediation site will always be a remediation
site and a vacant property — parcel without a building — will likely always stay that way."*

**The distinction this creates, which the corpus does not currently make:**

| kind | behaviour | date gate? |
|---|---|---|
| **EVENT signals** — a thing happened on a date | goes stale. A 2014 foreclosure says nothing about today's owner | ✅ **cut before 2020-01-01**, keep the within-X-days filter |
| **STATE signals** — a standing condition | does not go stale. Contamination does not expire; a delisted plant stays delisted | ❌ **no date gate** — gating these would silently delete real, current inventory |

Classification to apply (from the measured 18-code vocabulary):
- **EVENT:** tax sale, tax delinquency, foreclosure, code violation, unsafe building, vacant board
  order, demolition order, structure fire, environmental violation, assessment appeal, loan
  maturity, SBA charge-off, mass-layoff notice
- **STATE:** brownfield / remediation, facility inactive, plant delisting, government surplus,
  abandoned building *(argue this one — an abandonment record is a finding, not a dated event)*

⚠ Measure the loss before shipping the cut, and report it: a date gate that removes most of the
corpus is a decision, not a filter, and 69% of the environmental corpus is already known to be more
than ten years old.

#### MEASURED 2026-08-17 — and the naive cut would have been destructive

27,711 signal attachments across 24,275 parcels:

| | attachments | share |
|---|---:|---:|
| latest event **2020 or later** | 11,491 | 41.5% |
| latest event **before 2020** | 6,396 | 23.1% |
| **UNDATED** | **9,824** | **35.5%** |

⛔ **A `last_event >= '2020-01-01'` predicate silently drops all 9,824 undated rows**, because
`NULL >= DATE` evaluates to NULL, not TRUE. That is the *"unpublished is NULL, never 0"* rule wearing
a new costume, and it would have looked like a clean 41.5% filter.

**Worse, it lands hardest on the biggest signal.** `D4_tax_delinquency` is the largest signal we
hold — 9,459 parcels — and **8,523 of them (90%) carry no date at all.** A post-2020 cut would
delete nine tenths of it *not because the delinquency is old but because we never captured a date.*
`D7_brownfield` is 263 undated of 311, and it is a STATE signal that must not be gated regardless.

**So the gate is THREE-WAY, not two-way:**

| case | treatment |
|---|---|
| EVENT signal, dated | apply the cut |
| **EVENT signal, undated** | **`recency unknown` — surfaced as itself, never silently dropped.** The user chooses whether to include it |
| STATE signal | never gated |

Follow-on finding worth its own item: **90% of our largest signal is undated.** That is an
acquisition gap, not a display problem — `D4` dates are the highest-value date work left.

#### G9 RULED, 2026-08-17 — keep undated, make it the user's choice, and go get the dates

Operator: *"You should probably keep the ones that are undated, but we should look to potentially
rescrape (if we haven't already) for the dates. Just because there isn't a date doesn't mean that
they are fully useless — this should probably be a filterable thing within the dashboard/screener
to give the user the option to see the undated events."*

**Binding:**
1. **Undated records are KEPT by default.** They are real inventory. Hiding them by default would
   remove ~9,800 attachments for a reason that is about our acquisition, not about the property.
2. **The user toggles them**, and they carry a `date unknown` badge so they can never be mistaken
   for recent. ✅ **Shipped in `screener.html`** (`s-keepundated`, default on). Still to apply to
   the map console and `si.html` — see G22.
3. 🔴 **NEW — G14: re-scrape for the missing dates.** Highest value by far is `D4_tax_delinquency`:
   **8,523 of 9,459 parcels undated (90%)**, and it is the largest signal we hold. Check first
   whether the source carries a date we simply did not capture, versus one it never publishes —
   those are different findings and only the first is fixable by us. Then `D7_brownfield`
   (263 of 311), `D5_abandoned_building` (268 of 645), `D22_environmental_violation` (246 of 1,086),
   `D1_tax_sale` (247 of 3,322), `D20_loan_maturity` (109 of 129).

**Measured effect of the shipped control** (all flagged parcels, screener):

| cutoff | undated kept | undated dropped |
|---|---:|---:|
| none | 24,275 | 15,118 |
| Jan 2020 | **19,272** | 10,115 |
| Jan 2024 | 14,173 | 5,016 |

A bare `last_event >= '2020-01-01'` would have returned **10,115 instead of 19,272 — a silent 47%
cut**. Verified in-browser that standing conditions survive the tightest cutoff (122 of 500 visible
rows at Jan 2024 with undated excluded).

### G10 — "vacant" currently means two different things, and nothing distinguishes them

Operator, 2026-08-17: *"we need to clarify the distinction between vacancy as a parcel without a
building and an actually abandoned building (the actual signal). This is not recognized
throughout."* **Correct, and the screener I built today commits the error on one screen:** a filter
reading *"Vacant land (nothing built on it)"* sits feet away from a signal named
`D5_vacant_board_order`, which is about a **building**. One word, two unrelated meanings, no cue.

Binding resolution — one word, one meaning:

| concept | the ONLY term to use | never call it |
|---|---|---|
| a parcel carrying no structure (a land attribute, **not** a signal, and not distress at all) | **undeveloped land** | vacant, vacant land, empty |
| a **building** that stands empty / boarded / abandoned (a real owner-motivation signal) | **vacant building** / **abandoned building** | vacant land, vacancy |

Also affects: `has_vacancy_signal` in `in_sites`, the `f-vac` control on the map console, the
`site_kind` vocabulary, and every "vacant" string in `si.html`. Sweep all of them.

### G8 — the binding translation table

**Rule: a label a user cannot decode is a broken feature, however correct the number behind it.**
Internal codenames stay in the code, the docs and the registry. They do NOT appear in the UI.

| we say internally | the user sees | why ours fails |
|---|---|---|
| P1 | **Owner motivation** — *is this owner likely to sell?* | "P1" names a slot in our build order, not a concept |
| P2 | **Power & grid** | |
| P3 | **Land & size** | already plain — keep |
| P4 | **Environmental constraints** | |
| P5 | **Community & local rules** | |
| P6 | **Market & cost** | |
| P7 | **State legislation** | |
| SI / SI Feed / seller-intent signal | **Owner-motivation signals** | "SI" is an acronym for an acronym |
| D4, D12, D22 … | the signal's plain name — *tax delinquency*, *code violation*, *environmental violation* | a code is not a name |
| class union | **screened sites** | set-theory vocabulary in a UI |
| occ_group = 'ci' | **commercial / industrial** | a database enum |
| cannot assess | **not measured here** (+ *"left out of the score, never counted as zero"*) | reads as a system failure rather than a coverage statement |
| denominator | **how many sites we counted** | |
| posture_renderable / evidence_grade | **verified at an official source** vs **reported, not yet verified** | our gating vocabulary, not the reader's question |
| fan-out, D85, parcel_source | never surfaced at all | internal integrity machinery |

### The operator's own reference implementations — read these, not the national screener

| what it teaches | url |
|---|---|
| bus-level headroom siting: direction toggle, **upgrade tiers 0–4**, lead-time horizon (Now / ≤3yr / ≤5yr / ~10yr), $/MW cost efficiency, upgrade risk, "quick wins" = big single-upgrade gains | `https://ahenderson0233.github.io/indiana-bus-analysis/` |
| capacity-vs-proximity framing, **planned grid investments as their own layer**, location-uncertainty rings for assets known only by town, one shared MW legend with an explicit exception callout | `https://ahenderson0233.github.io/illinois-analysis-map/` |

### Deferred by the operator, not blocked by us

| # | task | when |
|---|---|---|
| F3 | Re-check Howard + City of Elkhart council votes | ✅ **RE-CHECKED 2026-08-19** — Elkhart ADVANCED and verified; Howard still NOT_FOUND, and the re-check surfaced a CONFLICT | ⭐ **CITY OF ELKHART: verified at the city's own records, and NOT adopted.** **Proposed Ordinance 26-O-32** — *"AN ORDINANCE ... ESTABLISHING A TEMPORARY MORITORIUM ON THE ACCEPTANCE AND PROCESSING OF NEW APPLICATIONS FOR DATA CENTERS AND BATTERY ENERGY STORAGE FACILITIES..."* (the agenda's misspelling is verbatim) — was taken on **FIRST READING** on 2026-08-17. The council's own voting record for that meeting lists exactly two final actions, both 9-0, and 26-O-32 is not among them. **Final action is set for a SPECIAL MEETING on 2026-08-27**, where it is the single item. ⚠ **Note the scope: this one covers BATTERY ENERGY STORAGE as well as data centres**, which is broader than any other row we hold. ⛔ `verified_effective_to` stays **NULL**: December 31 2027 appears in Section 7 only as the LAST OF THREE alternative triggers, whichever comes first — recording it as an end date would have repeated the Marion error found the same morning. **HOWARD COUNTY: still NOT_FOUND, and now less certain than before.** The county's own 2026-08-17 packet contains no data-centre item at all; Howard publishes minutes one meeting in arrears, so 08-17's are due in the September packet. ⚠ **And a direct conflict emerged:** we stored *"two-year"* (itself only ever a news lead), while a newer report says **"a temporary moratorium that lasts for ONE year"**, adopted unanimously on 2026-08-18 — a date on which the county's published schedule has **no meeting**. Two unverified and inconsistent durations, so the instrument text now says the duration is UNKNOWN rather than repeating "two-year" as if we had it from the county. Our held lead URL **404s** and has been replaced. **City of Kokomo stays BLOCKED** by its own robots.txt and was deliberately not probed. Applied by `scripts/apply_action_verification.py`  
| F4 | Re-check Marion MDC final action, Proposal No. 238 | ✅ **RE-CHECKED 2026-08-19** — no MDC action yet (hearing is today), but the stored expiry was WRONG IN KIND and is fixed | **The MDC hearing on Proposal No. 238 / 2026-AO-001 (Amended) is 2026-08-19 at 1:00 PM and had not convened.** Its page carries agenda, notices and packet and **no minutes, hearing results or video** — unlike the 2026-08-05 meeting on the same portal. Item 10 sits under *Petitions for Public Hearing*: scheduled, not decided, so it stays `proposed`. ⭐ **But the re-check found a real defect in what we already held.** We stored `verified_effective_to = 2027-12-31` as a firm end date. The ordinance says *"a moratorium ending **no later than** December 31, 2027"* — **a CEILING, not a term.** It ends when staff-developed regulations arrive. The date stays (an outer bound is useful) but the verbatim condition now travels with it and a `date_note` says plainly what it is. **Verified independently** by pulling the MDC packet PDF and extracting it: the *"no later than"* clause, SECTION 24's *"passed by the City-County Council this \_\_\_\_\_ day of \_\_\_\_\_, 2026"* with the date **blank**, the heading *"CITY-COUNTY GENERAL ORDINANCE NO. , 2026"* with the number **blank** — which is the strongest reason this is not adopted — and *"On August 10, 2026, the City-County Council approved an amendment to 2026-AO-001 (Amended)"*, a Council action **we did not hold**. Also recorded: that amendment **struck** the Special Use provisions the MDC recommended on 07-01, and there are officially stated carve-outs for vested rights — **DC Blox, Metrobloks, Sabey**. Watch for `..._mdc_hearing_results_-_municode.pdf` on the meeting page  

> **Before starting ANY item, check it against the warehouse.** This session recommended work
> four times that was already done, already impossible, or misread — D4 was "NOT HELD" while
> 17,617 rows sat in the warehouse; owner data was "absent" while `in_marion_parcel_crosswalk`
> carried it on 346,919 parcels; D12 "admitted 228" conflated *admitted* with *reached* (10,370);
> and a bug was filed on `si.html` from a console polluted by my own probes.
> `docs/TABLE_INVENTORY.md` exists to stop the first two recurring. Read it first.

---

## COMPLETED THIS SESSION (nothing here is open)

| # | task | why | state |
|---|---|---|---|
| N1 | Load the 4 verification results | 50 verified; Brown/Clay out-of-state corrections; Marion downgraded to pending | ✅ **DONE** `8a5ee58` |
| N2 | Re-export + re-audit after N1 | standing ritual | ✅ **DONE** |
| N3 | D9 absentee + D18 for Marion | 340,765 parcels · 28,763 out-of-state · 7,194 non-residential · 272,606 named owners | ✅ **DONE** `2e76cab` |
| N4 | S1 D4 split out of D1 | 15,106 rows · 45 counties · 100% dated · 18 signals now reach a parcel | ✅ **DONE** `ff24c9e` |
| N5 | A2 operator alias table | found the TELCO regex was filing pinned Lumen colos as telephone exchanges | ✅ **DONE** `e7246d2` |
| N6 | **A3 upload parity** | the door DOES exist (`index.html:125`, my earlier doubt was wrong). Real defect found: uploads wrote `_sub_mi` while the scorer reads `_dsub_mi`, so **every uploaded row went unscored**, and line distance was never computed. Fixed by routing uploads through the same `enrichDistances` the map uses | ✅ **DONE** `35088b0` — §13(2) now PASSES, proven on 6 real Marion parcels |
| N7 | **Load the re-sweep** — 18 counties, 3 flipped to ACTION_FOUND | Henry (~1 GW Surge PUD) and Tipton (moratorium, CO-ZO-13-26) VERIFIED; Sullivan (~$65B Potentia/Heartland, no county zoning) REPORTED | ✅ **DONE** |
| N8 | Fold the re-sweep into the Community page + coverage | 3 counties were rendering as "nothing found" | ✅ **DONE** `45feac8` |

## THE LAST SESSION'S OPEN LIST — `PATH_TO_COMPLETE.md` §2a, audited 2026-08-16

| # | task | audited state | effort |
|---|---|---|---|
| A1 | Fold county DC-action table into P5 | ✅ **DONE** — `in_dc_actions_county_v2` (107) + coverage (92) landed, wired to Community | done |
| A2 | Alias table for the DC cross-check | ✅ **DONE** `e7246d2` — and it exposed worse: the TELCO regex was filing pinned Lumen colos as telephone exchanges | done |
| A3 | Upload parity test | ✅ **DONE** `35088b0`. My "the door may not exist" was WRONG — it is at `index.html:125`; I had searched only app.js. The real defect: uploads wrote `_sub_mi` while the scorer reads `_dsub_mi`, so every uploaded row went UNSCORED | done |
| A4 | C4 saved workspaces | ✅ **DONE** `9569d12` — 40 controls enumerated dynamically + map view; round-trip verified in-browser | done |
| A5 | D12 Indianapolis placement | ✅ **DONE** — **admitted 228 → 2,109 parcels, reached 10,370 → 23,140.** The "matches nothing" diagnosis predated the Marion address crosswalk: **85.9% of distinct addresses match `FULL_ADDRESS` exactly.** Widened 2 case types → 7, gated on INTENT per the operator | done |
| A6 | IDEM event dates | ✅ **DONE** `9ccb02a` — `in_si_d22_idem_dated`, all 22,565 dated (3,925 exact), 97.9% agreement between two routes. **69% of the corpus is >10 years old** | done |

## PHASE COMPLETION — what stands between here and A–E at 100%

| phase | state | what remains |
|---|---|---|
| **A** wiring | ✅ complete — count in the generated block | nothing |
| **B** integrity | ✅ complete | nothing |
| **C** functionality | C1 ✅ C2 ✅ C3 ✅ C4 ✅ | **C5 PMTiles BLOCKED** on a WSL/Docker install |
| **D** acquisition | ✅ A5 and A6 both landed | nothing actionable — see SIGNALS |
| **E** hardening | E1–E4 ✅ | the two structural §13 items below |

### The §13 items that remain — both structural, neither a defect

| criterion | closeable? | what it needs |
|---|---|---|
| §13(2) upload parity | ✅ **CLOSED** | passed; round-trip proven on 6 real parcels |
| §13(5) golden path | **NO, not as specified** | the dossier half is met; the **AI docket summary** half needs an LLM feature this app does not have. Closing it means building a feature, not finishing one |
| §13(8) P6 acceptance | ✅ **CLOSED 2026-08-18** | It was recorded as structural because no component-level Indiana tariff existed. One does now: `in_utility_tariff_riders`, **668 components / 73 utilities**, 22 costed from their own books at every service voltage with riders (G54/G55/G56). The acceptance run reports the estate instead of the impossibility |

**So "A–E 100%, no fails" is already true of FAILs (0).** Two of three PARTIALs are structural.
An honest finish is: **A, B, C(-C5), D, E complete; §13(5) and §13(8) recorded as
NOT-ACHIEVABLE-AS-SPECIFIED with the reason**, rather than quietly restated as passes.

## SIGNALS — measured 2026-08-16, `docs/SIGNAL_REALITY.json`

Counts live in the generated block above. Of the signals that do NOT admit a parcel:

| class | n | agent-actionable? |
|---|---:|---|
| ADMITTED | 17 | done |
| HELD_NOT_SPLIT | 2 | **yes** — D4 (17,617 rows), D23 |
| HELD_WRONG_GRAIN | 8 | no — owner/county/aggregate grain, or an operator exclusion |
| OPEN | 3 | **yes** — D9/D18 (one blocker), D22 IDEM dates |
| AWAITING_OPERATOR | 1 | no — D13 FOIA fax |
| BLOCKED_STRUCTURAL | 3 | **no — stop re-recommending**: A1 robots, D10 + D15 paywalled |

| # | task | note |
|---|---|---|
| S1 | **Split D4 out of D1_tax_sale** | 17,617 rows, 76 counties, 92% parcel-keyed, 16,325 auctions still upcoming, 10,178 non-residential. A split, not a scrape |
| S2 | **D9 absentee, Marion** | = N3 |
| S3 | D9 absentee statewide | needs the DLGF pull |
| S4 | D23 surplus split | low value; watch-list only |

## AWAITING THE OPERATOR — do not chase

| item | ask |
|---|---|
| American Legal licence | one email to `license@iccsafe.org`; unlocks **17 counties** whose only codified source is amlegal |
| robots-vs-terms standing policy | 30+ hosts disallow `ClaudeBot` by name while serving the public freely |
| WSL or Docker install | 10 minutes; **C5 is fully scripted the moment it exists** |
| D10 procurement | $600/yr INCite or $38/mo Doxpop |
| IRS ALS FOIA | your fax; drafted in `docs/FOIA_IRS_ALS_REQUEST.md` |
| IEDC data request | one email; the only lawful route to A1 |
| refresh-cadence venue | Task Scheduler vs cloud cron |

## FOLLOW-UPS RAISED AND NOT YET DONE

| # | task | source |
|---|---|---|
| F1 | Re-sweep the 4 single-query counties — Henry, Johnson, Union, Wayne | ✅ **ALREADY DONE — audited 2026-08-17, no work run.** All four are in `in_dc_actions_resweep_coverage` at **2 queries with the official site fetched**, which is exactly the standard F1 asked for. Henry flipped to ACTION_FOUND (~1 GW Surge PUD) |
| F2 | Fetch the official site for the 18 negative counties where it was never reached | ✅ **ALREADY DONE — F2 *was* the re-sweep.** The re-sweep's 18 counties are **exactly** the set `status='SEARCHED_NONE_FOUND' AND official_site_fetched='False'` — 18 of 18 covered, **0 remaining, and the re-sweep went nowhere wider (0 counties outside the target)**. F1 and F2 were the same job, already finished, written down twice as open |
| F3 | Re-check Howard + City of Elkhart — actions were scheduled for **2026-08-17** | ✅ **RE-CHECKED 2026-08-19** — Elkhart ADVANCED and verified; Howard still NOT_FOUND, and the re-check surfaced a CONFLICT | ⭐ **CITY OF ELKHART: verified at the city's own records, and NOT adopted.** **Proposed Ordinance 26-O-32** — *"AN ORDINANCE ... ESTABLISHING A TEMPORARY MORITORIUM ON THE ACCEPTANCE AND PROCESSING OF NEW APPLICATIONS FOR DATA CENTERS AND BATTERY ENERGY STORAGE FACILITIES..."* (the agenda's misspelling is verbatim) — was taken on **FIRST READING** on 2026-08-17. The council's own voting record for that meeting lists exactly two final actions, both 9-0, and 26-O-32 is not among them. **Final action is set for a SPECIAL MEETING on 2026-08-27**, where it is the single item. ⚠ **Note the scope: this one covers BATTERY ENERGY STORAGE as well as data centres**, which is broader than any other row we hold. ⛔ `verified_effective_to` stays **NULL**: December 31 2027 appears in Section 7 only as the LAST OF THREE alternative triggers, whichever comes first — recording it as an end date would have repeated the Marion error found the same morning. **HOWARD COUNTY: still NOT_FOUND, and now less certain than before.** The county's own 2026-08-17 packet contains no data-centre item at all; Howard publishes minutes one meeting in arrears, so 08-17's are due in the September packet. ⚠ **And a direct conflict emerged:** we stored *"two-year"* (itself only ever a news lead), while a newer report says **"a temporary moratorium that lasts for ONE year"**, adopted unanimously on 2026-08-18 — a date on which the county's published schedule has **no meeting**. Two unverified and inconsistent durations, so the instrument text now says the duration is UNKNOWN rather than repeating "two-year" as if we had it from the county. Our held lead URL **404s** and has been replaced. **City of Kokomo stays BLOCKED** by its own robots.txt and was deliberately not probed. Applied by `scripts/apply_action_verification.py`  
| F4 | Re-check Marion — MDC final action **2026-08-19** | ✅ **RE-CHECKED 2026-08-19** — no MDC action yet (hearing is today), but the stored expiry was WRONG IN KIND and is fixed | **The MDC hearing on Proposal No. 238 / 2026-AO-001 (Amended) is 2026-08-19 at 1:00 PM and had not convened.** Its page carries agenda, notices and packet and **no minutes, hearing results or video** — unlike the 2026-08-05 meeting on the same portal. Item 10 sits under *Petitions for Public Hearing*: scheduled, not decided, so it stays `proposed`. ⭐ **But the re-check found a real defect in what we already held.** We stored `verified_effective_to = 2027-12-31` as a firm end date. The ordinance says *"a moratorium ending **no later than** December 31, 2027"* — **a CEILING, not a term.** It ends when staff-developed regulations arrive. The date stays (an outer bound is useful) but the verbatim condition now travels with it and a `date_note` says plainly what it is. **Verified independently** by pulling the MDC packet PDF and extracting it: the *"no later than"* clause, SECTION 24's *"passed by the City-County Council this \_\_\_\_\_ day of \_\_\_\_\_, 2026"* with the date **blank**, the heading *"CITY-COUNTY GENERAL ORDINANCE NO. , 2026"* with the number **blank** — which is the strongest reason this is not adopted — and *"On August 10, 2026, the City-County Council approved an amendment to 2026-AO-001 (Amended)"*, a Council action **we did not hold**. Also recorded: that amendment **struck** the Special Use provisions the MDC recommended on 07-01, and there are officially stated carve-outs for vested rights — **DC Blox, Metrobloks, Sabey**. Watch for `..._mdc_hearing_results_-_municode.pdf` on the meeting page  
| F5 | Reconcile the two NW sweeps | ✅ **DONE** — `in_dc_actions_nw_reconciled`. **Both sweeps were wrong on Jasper PC-22-25**: the Plan Commission issued an unfavourable *recommendation* 2025-12-15 (advisory, not a denial) and the Commissioners **approved** it 2026-02-02 with nine commitments. "denied" was never true; "petition-pending" was true for seven weeks |
| F6 | DataBank IND2 pin offset | ✅ **DONE, and it was material, not cosmetic.** Tested all 6 resolved facilities by asking whether the two points land on the SAME PARCEL, not by how far apart they are. Five agree within 26 m. **IND2 was 96.8 m out and on the WRONG SIDE OF HENRY STREET** — our pin fell on `491111183001014101` (the 701/731/733 group, south side) when IND2 is 650 W Henry on the north side, parcel `491111138006000101`. A facility pinned to the wrong parcel corrupts every spatial join it takes part in. The layer build now lets a B4-resolved point override the held pin. Re-measured: **0 of 6 material** |
| F7 | `CLOUDSCENE_GAP.md` bad matches | ✅ **DONE, and worse than reported** — the matcher used operator tokens and PLACE NAMES alone. **8 fabricated matches**: three different Indianapolis Lifeline buildings all matched to *Fort Wayne*; three Global Access Point sites collapsed onto one via the acronym GAP; **"Indigital Fort Wayne" matched to "Google Fort Wayne Building 5"** — different companies, same city; "The Union 525" matched on the word *union*. A wrong match understates the gap and drops a site from follow-up. All flagged inline with a correction block |
| F8 | `si.html` MapLibre `ctx-osm-line` guard | **NOT A BUG** — verified clean on a fresh tab; a separate session was started on it in error |

## OPERATOR RULINGS — 2026-08-16, binding

**R1. KEEP EVERY PARCEL. Do not filter the rendered payload by occupancy class.**
The proposal was to drop parcels carrying a residential structure, on the grounds that a house
cannot host a hyperscale DC or a 5 MW BESS. Measured before acting, that cut would have removed
**165,003 parcels of which 160,669 fit a 25 MW datacentre** — because `occ_group` describes the
STRUCTURE, not the land, and 97.5% of them are **farmsteads**: 5–20 ac (87,541), 20–100 ac
(63,310), 100+ ac (9,953, largest **6,374 acres**), averaging 3–5 structures. A farmhouse on 200
acres classes residential and is exactly the site a developer wants.
Only 4,199 were genuine small lots — **0.35% of the 1.2M payload**, no meaningful performance win.
**Operator ruling: keep them all, because a 250 kW or 500 kW BESS may be installed later** and
needs a fraction of the land 5 MW does. Capability columns (`fits_min_bess_5mw`, `fits_dc_25mw`)
already filter on what the land can hold, which is the question that actually matters.

**R2. Vacant land stays** — 896,947 no-structure parcels, unchanged. (Restates the standing rule.)

**R3. STRUCTURAL DISTRESS MUST PLAUSIBLY PRODUCE INTENT. A minor incident does not.**
Operator, 2026-08-16. Applied to the 910,483-row Indianapolis code corpus as a two-tier gate
rather than a case-type list:
- **Tier 1, admitted on a single occurrence** — Unsafe Buildings, Vacant Board Order, Demolition.
  Each already means the building is unusable.
- **Tier 2, admitted only where chronic or unresolved** — Building violations, Repair orders,
  Environmental. Measured: **16,325 addresses have exactly one structural case; 2,176 have ten or
  more.** One citation is an incident; ten is an owner who has stopped maintaining the property.
- **Excluded outright regardless of type:** `Closed, No Violation` (60,449) and `Void` (13,987) —
  an inspector finding nothing is not distress, and counting it would be the "0 high-priority
  violators" error inverted.
- Also excluded, counted not dropped: High Weeds & Grass **363,844**, Trash 103,792, Zoning
  225,359, Vehicle 46,969. Admitting the corpus wholesale would have inflated D12 by ~750,000
  rows of lawn care — the South Bend error a third time.

## ⛔ WHERE THE DATA LIVES — read this before "rebuilding" anything

**Our data lives in `energy-platfrom.indiana_app`. All of it. There is nothing of ours in
`energy`.** The checkpoint proves it: `0 tables named in_* exist in energy`.

**The thing that confuses people** (it confused the author of this file): 157 of our 268 registered
objects record `energy.*` as their **SOURCE**. That is *provenance, not storage*. `in_sites` is a
table in `indiana_app`; `energy.vw_parcel_sites` is where its rows came from. Reading that source
field as "our data is in energy" leads straight to someone rebuilding a table we already hold.

**The dependency, measured by `scripts/checkpoint.py` on every run:**

| | |
|---|---|
| BUILD scripts that read `energy` | **65** — expected and correct; this is how clips are made |
| EXPORTS that read `energy` | **0** — enforced by a checkpoint check that FAILS if one appears |
| tables we created inside `energy` | **0** |
| writes to `energy` | APPEND-only rows to `energy.registry_sources`, the single permitted exception |

**Build scripts may read `energy`. Exports may not.** An export is on the path to what the user
sees, so the app must be rebuildable from `indiana_app` alone. The one violation
(`export_spine.py` counting unplottable SI rows live) was closed by clipping the slice into
`in_si_plottability` and reading that.

### Should we duplicate the ~140 energy source tables? **No.**

1. **The data we need is already here.** The clips ARE the duplication, already done, already
   Indiana-only, already registered.
2. **The sources are national and enormous.** `mat_parcel_attrs` is 107M rows, `cems_hourly` 36M,
   `mat_si_plottable` 80M+. Copying them whole to obtain an Indiana slice we already hold would
   cost storage and buy nothing.
3. **It would collide with the national buildout**, which owns those tables and rebuilds them.
4. **The correct pattern is the one already in use:** clip the slice, register it in the same run,
   and read the clip. `in_si_plottability` is the worked example — a national 80M-row table
   reduced to the four numbers Indiana actually needs.

**If a future session finds itself about to rebuild something because it saw `energy.x` in a
script: stop and check `docs/TABLE_INVENTORY.md` first.** All 269 objects are listed there.

## THE AUDIT SUITE — run these before trusting any number

| script | asks | catches |
|---|---|---|
| `scripts/audit_honesty.py` | is what we ship true? | 13 adversarial checks incl. **payload vs warehouse**, D85 in any spatial join, a cannot-assess shipped as 0, and **every committed loader having a table** |
| `scripts/audit_wiring_census.py` | does every registered object reach a surface? | the denominator MOVES on every build — never quote a past figure |
| `scripts/audit_frontend.py` | do the pages work, **and are they current?** | missing element ids, dead UI, absent payload keys, redeclared consts, duplicate ids, and **a surface reading a superseded table** |
| `scripts/acceptance_run.py` | §13, part by part | now **runs** the wiring census instead of quoting its document |
| `scripts/audit_signal_reality.py` | what is actually left on the signals? | separates done / impossible / awaiting-operator from genuinely actionable |
| `scripts/build_table_inventory.py` | what do we hold, and what does it CARRY? | 26 objects with owner data, 137 with a status vocabulary — where hidden signals live |

**The "is it current" check earned its place the hard way.** The map showed 4 ordinances while the
warehouse held 153 candidates, 19 admitted and 73 verified county actions. Nothing errored; the
page rendered what it was told, and what it was told was two generations old. Every other check
asked "does it work".

## RULES EARNED THIS SESSION

1. **A completeness shortfall must be an `assert`, not a `print`.** The sweep loader reported "79 of 92" to a console nobody re-read and would have loaded anyway.
2. **An agent's deliverable is a file IT writes.** A parent collecting sub-agent replies lost 13 counties once and a whole NW pass entirely.
3. **For a scraper, "committed" and "safe" are different properties.** `.gitignore` excludes `scrapers/**/*.json` by design, so only the warehouse proves the second.
4. **A generated document must not carry hand-typed claims.** `SI_COVERAGE.md` asserted D4 absent for a whole session. Every NOT-HELD claim now carries a build-time probe; unprobed claims print as UNVERIFIED.
5. **Read the console in a fresh tab, or not at all.** A reused tab accumulates your own probes and reads as page defects.
6. **Check the state slice, not the national number.** `mat_parcel_attrs` has 69M owner names and **zero** for `parcel_source='parcels_in'`.
7. **Never assume a date format.** A `%Y-%m-%d` parse over `MM/DD/YYYY` reported "0 upcoming auctions" when 16,325 were upcoming; the corrected parse then threw on `00/00/0000` and needed `SAFE.PARSE_DATE`.
8. `r.keys` collides with `Row.keys()` — **third recorded occurrence**. Alias it `n_keys`.
9. **An audit that cries wolf gets ignored.** `audit_frontend.py` opened with 56 findings and
   roughly zero real ones. Four false-positive classes had to go before it was worth reading:
   runtime-created ids, brace-depth vs indentation, template-literal ids (`` $(`w-${k}`) ``, which
   reported the entire scoring UI as dead), and property scanning inside string literals. Same
   failure as the wiring census counting build scripts as features.
10. **"Does it work" and "is it current" are different questions.** A page can be perfectly
    functional and two generations out of date, with no error anywhere.
11. **A wall is an observation, not a property of a host.** Four robots-403 walls did not
    reproduce on re-test. Re-check a BLOCKED record before treating it as final — including
    amlegal, which currently gates 17 counties.
12. **A disagreement between two instruments means at least one is wrong.** It is not a tie to
    break by preference. Both NW sweeps reported Jasper PC-22-25 as verified; both were wrong.
13. **Distance is not the test for a coordinate — the PARCEL is.** Five of six pin offsets were
    cosmetic; the sixth put a data centre on the wrong side of the street and the wrong parcel.
14. **Never guess a column name, including in a throwaway probe.** Cost four separate zero-result
    queries this session: `ADDRESS_ID` for `FULL_ADDRESS`, `geog` for `lat`/`lon`, `facility_name`
    then `already_pinned_as` (which carries coordinates in parentheses).
15. **ASCII in console output.** cp1252 cannot encode `≈ → ✗ ↔ ⚠`, and three scripts died on
    their own `print()` — including the honesty audit crashing on its FAILURE path. 44 scripts now
    reconfigure stdout; keep new ones ASCII or add the shim.
16. **Use a commit-message FILE.** Backticks and double quotes in `-m` get eaten by the shell.
17. **A coverage field records ONE pass, not the state of the world.**
    `in_dc_actions_coverage_v2.official_site_fetched` reads `'False'` for **50 counties**, which
    reads as "we never checked". For **32 of them the official source WAS subsequently reached**,
    by the verification pass in `in_dc_actions_resolved` — every one of the 32 has a verification
    verdict, 21 carry a `VERIFIED_AT_OFFICIAL_SOURCE` row that renders and 11 were verified and
    then **suppressed**. Zero unverified claims render. That stale field is what generated F1/F2 as
    phantom open work. **Ask the table that gates rendering (`posture_renderable`), never the
    table that logged the first sweep.**
18. **Two open items describing one job will both survive an audit.** F1 and F2 were written as
    separate follow-ups from separate observations; measured, they are the *same 18-county
    re-sweep*, which had already run. A ledger dedupes by *work*, not by wording.

## TYPE TRAPS IN THIS WAREHOUSE — read before writing a WHERE clause

Booleans are **not** consistently BOOL across our own tables, and guessing costs a query each time:

| table | column | actual type | correct predicate |
|---|---|---|---|
| `in_dc_actions_coverage_v2` | `official_site_fetched` | **STRING** `'True'`/`'False'` | `= "False"` |
| `in_dc_actions_resweep_coverage` | `official_site_fetched` | **STRING** | `= "True"` |
| `in_dc_actions_resolved` | `posture_renderable` | **BOOL** | `COUNTIF(posture_renderable)` |

`IS TRUE` against the STRING columns fails with *"No matching signature for operator IS TRUE"*, and
`= "True"` against the BOOL fails with *"BOOL, STRING"*. Both happened on 2026-08-17. Rule 14 said
never guess a column NAME; this extends it: **never guess its TYPE either — `get_table().schema`
costs nothing.** Note also that `get_table().num_rows` is **0 for every view** — that is metadata,
not emptiness. `COUNT(*)` the views.
