# HANDOFF — 2026-08-17 front-end revamp session

> **Read `docs/SESSION_START.md` first, run `python scripts/checkpoint.py`, then this file.**
> This is the session-specific handoff. `SESSION_START.md` carries the standing rules.

---

## ⚠ 1. AGENTS THAT WERE STILL RUNNING WHEN THIS SESSION ENDED

**This section exists because a previous handoff omitted it and the next session did not know work
was in flight.** Resume an agent by name; do not re-dispatch the same job.

| agent task | state at handoff | what it owns |
|---|---|---|
| **"Acquire PJM injection + MISO withdrawal"** | **Reported, but its AEP footprint harvest continues in the background** | `in_pjm_queuescope_injection`, `in_pjm_bus_injection`, `vw_pjm_bus_injection_ladder`, `in_miso_poi_ladder`, `in_bus_headroom_miso_ladder`, `docs/RTO_DIRECTIONS.md`, `scripts/pull_pjm_injection.py`, `scripts/build_*_injection*.py` |
| **"Acquire Indiana rivers and lakes geometry"** | **STILL RUNNING at handoff — no report received** | Will write `docs/WATER_GEOMETRY.md` and a rivers/lakes table. **Check for it before assuming water geometry is still blocked.** |

**Consequences you must respect:**
- ⛔ **Never run two QueueScope instances at once.** `scripts/pull_pjm_injection.py` may still be
  mid-harvest. Check with `Get-CimInstance Win32_Process -Filter "Name like 'python%'"` before
  starting any PJM pull. This is why **G23 (49 missing withdrawal buses) is blocked**, not forgotten.
- The wiring census will keep reporting a handful of unwired objects while those agents' tables
  exist and no page shows them. **That is the check working**, not a regression — a half-loaded
  table wired to a UI is worse than one nobody can see.

---

## 2. FILES AND REFERENCES THE OPERATOR SUPPLIED THIS SESSION

All of these informed decisions recorded in `docs/BACKLOG.md`. **They are not in the repo** — they
live on the operator's machine at the paths below.

### Dossier format — the model for the Power Plan (G3)
| file | what it gave us |
|---|---|
| `C:\Users\ahend\Downloads\PG_LA-Cajun_Power Plan.pdf` | The 4-page *Power Plan* structure: status, load ramp, key takeaways, next steps, Figure 1 stakeholders, Figure 2 parcel diagram, Figure 3 path-to-power, Figure 4 interconnection checklist, Figure 5 documentation, appendix |
| `C:\Users\ahend\Downloads\PG_Level 2_NJ-Jetstream.pdf` | Same format, deregulated-market variant (PJM) |
| `C:\Users\ahend\Downloads\PG_WI-Maple_Power Plan.pdf` | Same format, regulated-market variant (MISO/WEPCO) |

### Grid benchmark — **yardstick only, NEVER a source** (see G20/G22)
| file | what it gave us |
|---|---|
| `...\Bus Analysis\Data Input Files\Greenfield Interconnection Capacity, Buses-2026-06-23T16-58-00.csv` | The baseline: 19,846 rows, both directions, upgrade tiers 0–4, `Powerflow Case = DPP-2025-Cycle_SUM_D_ERIS-mitigated_Final` — the finding that explains our zeros |
| `...\Greenfield Interconnection Cost, Buses-2026-06-23T16-57-46.csv` | Cost per upgrade, proxy MW ladder, **Min/Max Lead Time (Years)** |
| `...\Substations-2026-06-23T14-49-04.csv` | 2,751 Indiana substations — the completeness benchmark (we hold 98.5%) |
| `...\Transmission Lines-2026-06-23T14-50-32.csv` | Voltage class banding, and the `NOT AVAILABLE` pattern that informed G13 |

⛔ **Operator ruling, binding:** *"we should never use Orennia data within our tools, but we should
use it as a comparison measurement."* `scripts/benchmark_vs_orennia.py` writes **only** a markdown
report — no BigQuery table, no `data/` payload, by design.

### Tariff model — the spec for G17
| file | what it gave us |
|---|---|
| `C:\Users\ahend\Downloads\CPS_35MW_Rate_Model.xlsx` | The full component build-up: service availability, energy charge, **fuel base embedded in rates**, demand $/kW-mo split summer/non-summer, **per service voltage**, riders, then effective $/kWh. Reclassifies §13(8) from *impossible* to *acquirable* |

### Reference implementations the operator built
| url | what it teaches |
|---|---|
| `https://ahenderson0233.github.io/indiana-bus-analysis/` | Bus-level siting: direction toggle, upgrade tiers 0–4, lead-time horizon, $/MW, risk, "quick wins" |
| `https://ahenderson0233.github.io/illinois-analysis-map/` | Capacity-vs-proximity framing, planned grid investments as their own layer, **location-uncertainty rings** for assets known only by town |
| `https://ahenderson0233.github.io/indiana-application-decennial/index.html` | The deployed app itself |

⛔ **Do NOT reference the national buildout's screener** — operator instruction: it holds P1 only and
is materially incomplete.

---

## 3. WHAT THIS SESSION SHIPPED

| # | item | commit |
|---|---|---|
| G1 | **Site screener** (`screener.html`) — grid as CAPACITY not proximity | `d1e2c95` |
| G2 | **Insights tab** (`insights.html`) | `d1e2c95` |
| G3 | **Power Plan dossier** — 4 pages, prints to PDF | `92475c5` |
| G5 | **Rail → 8 plain-named accordions** + non-collapsing filter bar | `f40a153` |
| G11 | **Verified county actions reach the map** — 33 counties, incl. a ban shown as "quiet" | `d8abd5f` |
| G12 | **Water** — county + parcel grain, watersheds + stress basins on the map | `023718b`, `ac581e3` |
| G13 | **Voltage audited, repaired, coloured, filterable** | `350dd25` |
| G16 | Registry rows must be re-runnable | `e38f32c` |
| G18 | **Charts rebuilt** — axes, units, zero-based, partial-period guard | `d092400` |
| G19 | **Substation dedupe** + `asset_class` | `53141b1` |
| G24 | **Content-hashed assets** (`scripts/stamp_assets.py`) | `92475c5` |
| G25 | **Check-the-warehouse-first** standing rule | `5973a2a` |
| G27 | **Re-clipped `percent_loading_before`** — the app can now say WHY a bus reads zero | this session |
| — | HUC8 watershed boundaries acquired from USGS | `0813266` |

---

## 4. THE FOUR FINDINGS THAT MATTER MOST

1. ⭐ **Our MISO zeros are pre-existing overloads, not full buses.** 26.3% of Indiana facility rows
   are already over their rating before any request; the *most permissive* facility at those same
   POIs averages **45,731 MW**. Their case is `ERIS-**mitigated**` DPP-2025; ours is unmitigated
   DPP-2021. **Operator ruling: reach parity with their method first, then unify PJM and MISO —
   do not unilaterally drop overloads** (that swings Indiana from 0.2% to 100%).
2. **Headroom is request-INVARIANT** in both RTOs — proven on 38,381 keys. One harvest per direction
   answers every rung; the ladder premise I recorded was wrong.
3. **NHD has no geometry, nationally** — 50M rows, `SHAPE` NULL on every one. Platform-side defect
   worth telling the platform session. Parcel-grain water works anyway via basin polygons.
4. **401 columns under-clipped across 24 tables** (`docs/CLIP_COMPLETENESS.json`). One is now fixed;
   the gas asset tables still have no coordinates, and `echo_cwa_facilities` has `faclong` and
   **no `faclat`**.

---

## 5. START HERE NEXT SESSION

1. `python scripts/checkpoint.py` — expect unwired objects if the agents' tables are still bare.
2. **Check both agents** (section 1). The water agent may have landed river geometry.
3. Read `docs/BACKLOG.md` — the **governing principle** at the top, then the **G-index**, which has
   a row for every item G1–G29.
4. Highest-value open work: **G21** (the ~14 surfaces still without a "so what"), **G29** (distance
   measured from the parcel's first vertex — a river crossing a parcel reads 0.55 mi, not 0.0),
   **G27** (the remaining under-clipped columns), **G17** (tariff model).

⚠ **Verify, do not inherit.** Every number in this file was true when written. The checkpoint is
the only thing that is true now.
