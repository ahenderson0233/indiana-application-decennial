# HANDOFF — 2026-08-17 (session C): DPP-2025 resolved, grid + front-end advanced

> **Read `docs/SESSION_START.md`, run `python scripts/checkpoint.py`, then this file, then
> `docs/BACKLOG.md` (governing principle → G-index).** This supersedes the "agents still running"
> section of `HANDOFF_2026-08-17b.md`; everything else there still holds.

---

## ⚠ 1. PROCESSES STILL RUNNING AT HANDOFF

| process | state | notes |
|---|---|---|
| **PJM QueueScope, case 23, WITHDRAWAL** → `in_pjm_qs_tc2phii_wd` | **~81%** (1,475 / 1,826 AEP buses) | writes its FINAL registry row on completion |
| **PJM QueueScope, case 23, INJECTION** → `in_pjm_qs_tc2phii_inj` | **~62%** (1,125 / 1,826) | ⚠ carries a **PROVISIONAL** registry row (written by hand so the checkpoint passes); the wrapper overwrites it with the final count when the harvest ends |

⭐ **Case 23 = "2028 TC2 Phase II Case (Summer Peak)" = the benchmark's EXACT PJM case.** These two
harvests are the PJM half of grid parity. ⛔ **Owner id for case 23 is `1568`, NOT 739** — QueueScope
renumbers owner ids per case; passing 739 loads 0 rows and exits *successfully*. ⛔ **Never run two
QueueScope instances beyond these two**; do not start a third.

**When both finish:** rebuild `in_bus_capacity_tier0` to use case 23 for PJM (it currently holds the
stale case-4 PJM), attach the DPP-2025 cost join, and benchmark against the vendor file.

**No discovery agents are running.** All four finished (§3).

---

## ⭐⭐ 2. DPP-2025 IS RESOLVED — read this before reopening the hunt

The full route matrix is in `docs/MISO_DPP2025_ROUTE.md`; the verdict is in `BACKLOG.md` §G7m. Short
version:

**✅ Cost/upgrade dimension LANDED** (the piece the benchmark's second file has and we had nowhere):
`in_miso_dpp2025_ph1_project_costs` — 202 projects, **$29.52B**, via MISO's own open
find→CDN route (`api/find/…_search` → `cdn.misoenergy.org`). **21 of 27 Indiana DPP-2025 queue
projects carry a Phase-1 upgrade cost, $1.7B total, ~$373/kW of ERIS.** Loader
`scripts/pull_miso_dpp2025_study.py`.

**❌ Bus headroom (FCITC/DFAX/constraint table) is CEII-WALLED — a legal classification, not a scrape
gap.** Proven every way: the SIS says its constraint appendices are CEII in its own words; CartoVista
returns 403 `ProtectedData` **even from inside the viewer's own anonymous session**; FERC docket
ER24-2046 confirms the per-POI numbers are owed publicly *only as interactive query responses, not
bulk export*. ⛔ **This wall is not to be circumvented.** Record BLOCKED; do not probe for auth holes.

**THE PATH FORWARD for headroom — model it ourselves.** MISO publishes the recipe
(**BPM-015-r33 §3.1.1**: FCITC, 3% DF cutoff, transfer per bus decremented by existing+queued gen).
We hold the DPP-2021 constraint inputs AND MISO's published DPP-2021 capacity GeoTIFF (3.39M pixels)
to **validate the method against**. If our recreation reproduces MISO's own 2021 surface, the method
is proven and only vintage separates us from the vendor.

**⚠ THE HONEST PARITY CEILING (tell the operator plainly, do not oversell):** with the 2021 data we
hold we can match the vendor's **schema (~90%)**, **method (fully, and validatably)**, and **PJM
numbers (near-exact, same case)** — but **NOT the MISO numbers** (ours 0.16% vs their 39.3% tier-0;
that gap is DPP-2021 vs DPP-2025 and no method-tuning closes it). Tiers 1–4 need the CEII upgrade
study. The strongest *truthful* claim is *"our MISO figures are MISO's own current public position;
the vendor holds a CEII-gated 2025 cycle with no open route"* — not "we match Orennia."

**DPP-2023 (operator asked):** headroom is CEII the same way — skip. **Cost** is a cheap grab: a 2023
SIS is likely public via the same route and its **Phase 2 already exists** (2025's does not until
~April 2027), so it gives a fuller cost picture. One `--discover` call. Not a headroom hunt. (§G7n)

---

## 3. WHAT SHIPPED THIS SESSION

| # | item | verified |
|---|---|---|
| **G29** | Exact `ST_DISTANCE` for transmission / substations / water | ⭐ filters were showing **38%** of qualifying parcels (103,159 of 269,961 within 1 mi of a line); 41,986 parcels have a line ON them |
| **G32** | All 18 map layers moved into the themed 1–6 dropdowns | real-browser: 21 ids present, 0 duplicates, every control in its section |
| **G9** | Undated-signal toggle reaches the map console (`f-keepundated`) | in-browser: keep-ON passes+counts undated, keep-OFF drops it, stale-dated dropped either way |
| **G28** (display) | Derived MW banded + relabelled ("land supports up to") | `mwReality()` in `common.js`; 25,428 MW no longer reads as a site capacity |
| **G10** | "undeveloped land" vs "vacant building" swept app-wide | quotes preserved as quotes |
| **G35** | Queue pressure on the parcel panel (both RTOs already held) | 49 of 87 counties ≥50% queue attrition |
| **G7h** | PJM case 23 identified + harvesting | smoke test: 11,191 rows, case label confirmed |
| **G7m** | DPP-2025 cost table landed; headroom CEII verdict | table verified in BQ |
| — | `common.js` `.sowhat` treatment + `mwReality()` (reusable for G21) | live on production |

**Corrections I made and recorded (don't let them recur):**
- **G30 was NOT our defect** — a GitHub 503 outage, not a size limit. The queued "move data/sites/"
  fix would have broken the map. `data/sites/` also shrank 334→219 MB when a dormant coordinate-
  rounding bug (`rc()` called on a dict) was fixed.
- **I re-acquired a table we already held** (`in_queue_miso`) — the G25 error I'd been citing. Dropped
  the duplicate; kept a freshness *check* that writes no table. **`in_queue` already has both RTOs'
  Indiana queue** (361 PJM + 583 MISO) — no acquisition needed.

---

## 4. HIGHEST-VALUE OPEN WORK

1. **Finish the grid parity build** (once harvests land): rebuild `in_bus_capacity_tier0` with case
   23 PJM + DPP-2025 cost join; then the **self-model** headroom path (§2) validated against the 2021
   GeoTIFF. This is the through-line of the whole session.
2. **G21** — ~14 surfaces still without a "so what". `.sowhat` + `mwReality()` exist to reuse.
3. **G33** — rebuild `si.html` (it's a build log: 26 tables, 24 flat sections, 7 are provenance).
4. **G34** — layer-visibility bug (`L-fac` wind/solar); needs a real-browser repro. Structural cause
   recorded (two registries: `LAYER_MAP` vs `CONTEXT_LAYERS`).
5. **G27 nonattainment** — ONE real re-clip, recipe ready in `BACKLOG.md` §G27 (new table
   `in_nonattainment_classified`, source deduped, parent measured clean). The other three "gaps" were
   name-match false alarms.
6. **G20** substation parity; **G17** tariff model.

---

## 5. TWO LOOSE ENDS + THE CHECKPOINT STATE

- **Checkpoint fails ONLY on wiring census (282/297)** — the standing expected state. The 15 unwired
  objects are the in-flight harvest tables + just-landed DPP tables + the older agent tables; none is
  wired to a surface yet. Honesty audit, registration, payload-agreement all PASS. This is the check
  working, not a regression.
- **`in_miso_dpp2025_counties` (1,000/1,003) + `in_miso_dpp2025_footprint` (1)** — MISO-wide
  reference/boundary tables an agent loaded while proving DataRows works; partial (1,000-row cap) and
  duplicate boundaries we already hold. **Drop candidates** — kept only because binning another
  agent's output unilaterally felt wrong. Not bus headroom.
- **`build_nonattainment_classified.py` was a 443-byte stub — removed.** The fix lives in the backlog.

⚠ **Verify, do not inherit.** Every figure here was true when written; the checkpoint is the only
thing true now — and G30 this session is the standing proof of what inheriting a diagnosis costs.
